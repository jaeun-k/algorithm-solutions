# 재귀(Recursion) 입문 — 3문제 정리

지금까지 다룬 알고리즘 패턴(투 포인터, 명단 기록, 러닝 값 갱신)과는 완전히 다른 사고방식인 재귀를 처음 다룬 기록이다. 파이썬 문법 커리큘럼(`python_curriculum_v2.md`)에는 속하지 않는, 별도의 알고리즘 트랙으로 분류한다.

---

## 사전 개념: 재귀의 구조

재귀는 **함수가 자기 자신을 다시 호출하는 것**이다. 모든 재귀 함수는 두 부분으로 이루어진다.
1. **종료 조건(base case)**: 더 이상 쪼갤 필요 없이 바로 답을 낼 수 있는 가장 작은 경우. 없으면 무한 호출된다.
2. **재귀 호출(recursive case)**: 지금 문제를 "더 작은 같은 종류의 문제 + 약간의 추가 계산"으로 바꿔, 그 작은 문제를 자기 자신에게 다시 맡기는 부분.

### 막혔던 지점: "return 전에 재호출되면 계산이 성립 안 하는 거 아닌가"
`return n * factorial(n-1)`을 보고, "재호출 함수가 값을 반환하기도 전에 다음 코드가 실행되어야 하는데, 그럼 계산 자체가 안 되는 거 아니냐"는 의문이 있었다.

**해소**: `return n * factorial(n-1)`은 하나의 return문이다. 실행 순서는 ① 먼저 `factorial(n-1)`을 호출해 값을 받아옴 → ② `n`과 그 값을 곱함 → ③ 그 결과를 이번 함수가 `return`함. 즉 호출은 `n=3 → n=2 → n=1 → n=0` 순으로 내려가고, `return`은 `n=0 → n=1 → n=2 → n=3` 순으로 거슬러 올라온다. 호출 중인 함수는 "아무것도 안 하는 게 아니라, 재귀 호출이 값을 돌려줄 때까지 그 자리에서 계산을 잠시 미뤄둔 채 대기 중"인 상태다.

---

## 문제 1: 팩토리얼 (Factorial)

**예상 난이도: 브론즈 5**

```python
factorial(5)   # 120
factorial(0)   # 1
```

### 정답 코드
```python
def factorial(n):
    if n == 0:
        return 1
    return n * factorial(n - 1)
```

### 시행착오: 종료 조건을 `n == 1`로 잡았던 실수
```python
if n == 1:
    return 1
```
이렇게 하면 `factorial(0)`을 호출했을 때 `n`이 `1`을 그냥 지나쳐(`0 → -1 → -2 → ...`) 종료 조건에 영원히 도달하지 못해 `RecursionError: maximum recursion depth exceeded`가 발생한다. 종료 조건은 실제로 함수가 받을 수 있는 가장 작은 입력값을 정확히 커버해야 한다는 것을 확인.

### 부수적으로 정리된 개념: 왜 `0! = 1`인가
"1부터 n까지의 곱"이라는 정의를 `n=0`에 대입하면, "0이라는 숫자를 곱한다"가 아니라 **"곱할 범위 자체가 텅 비어버린다"**(range(1,1)이 빈 것과 같은 원리)는 뜻이 된다. 곱할 대상이 하나도 없을 때는 곱셈의 항등원인 `1`로 정의하는 것이 수학적 약속이며, 이는 조합론 공식들이 예외 없이 성립하기 위한 필수 조건이기도 하다.

---

## 문제 2: 피보나치 수열 (Fibonacci)

**예상 난이도: 브론즈 4~3**

```python
fibonacci(7)   # 13
```
점화식 `fibonacci(n) = fibonacci(n-1) + fibonacci(n-2)`를 그대로 코드로 옮기는 문제. 종료 조건이 `n==0`, `n==1` **두 개** 필요하다는 것과, 재귀 호출이 한 번이 아니라 **두 갈래**로 뻗어나간다는 것이 `factorial`과의 차이.

### 정답 코드
```python
def fibonacci(n):
    if n == 0:
        return 0
    if n == 1:
        return 1
    return fibonacci(n-1) + fibonacci(n-2)
```

### 발견: 로직은 맞아도 치명적으로 느리다
`n`이 20 → 25 → 30 → 32로 조금씩만 커져도 실행 시간이 기하급수적으로 늘어난다(`O(2ⁿ)`). 원인은 같은 하위 값(예: `fibonacci(3)`)이 재귀 트리 안에서 몇 번이고 중복 계산되기 때문이다. 이를 해결하려면 계산 결과를 저장해 재사용하는 메모이제이션(DP)이 필요한데, 이는 `max_subarray_sum_notes.md`에서 이미 언급된 DP 개념과 직결되는 별도의 큰 주제라 이번엔 다루지 않고 다음 학습으로 미뤄둠.

---

## 문제 3: 하노이의 탑 (Tower of Hanoi)

**예상 난이도: 실버 5~4**

```python
hanoi(2, 'A', 'B', 'C')
# Move disk 1 from A to B
# Move disk 2 from A to C
# Move disk 1 from B to C
```
`n`개의 원판을 규칙(한 번에 하나씩, 큰 원판이 작은 원판 위로 못 감)을 지키며 옮기는 과정을 출력한다.

### 핵심 분해 규칙
n개를 start에서 end로 옮기는 것은 ① 위 n-1개를 start→aux로 옮기고, ② 맨 아래 1개를 start→end로 옮기고, ③ 다시 n-1개를 aux→end로 옮기는 것으로 분해된다.

### 정답 코드
```python
def hanoi(n, start, aux, end):
    if n == 0:
        return
    hanoi(n-1, start, end, aux)
    print(f"Move disk {n} from {start} to {end}")
    hanoi(n-1, aux, start, end)
```
이 문제는 알고리즘(3단계 분해) 자체는 손으로 완전히 이해했음에도, 그걸 코드 구조(매개변수 자리를 바꿔 넘겨 역할을 재배정하는 것)로 번역하는 지점에서 막혔다. 재귀 호출 시 인자 순서를 바꾸는 것만으로 "이번 호출에서 무엇이 start/aux/end 역할을 맡는지"가 자동으로 재배정된다는 걸 확인.

---

## 핵심 논의: 재귀는 왜 지금까지의 방법론과 다른가

이 문제를 계기로 나눈 논의가 이 문서에서 가장 중요한 부분이다.

### 1. "겉모습을 일반화"하는 것과 "분해 구조를 정의"하는 것은 다르다
지금까지(다이아몬드, 부분정사각행렬)는 **결과물(겉모습)**을 여러 개 늘어놓고 그 사이의 공통 규칙을 관찰해 공식으로 일반화했다. 그런데 하노이의 탑은 `n=2`와 `n=3`의 실제 움직임 나열(겉모습) 자체가 완전히 다르다 — 공통된 겉모습 패턴이 애초에 존재하지 않는다.

재귀에서 일반화해야 할 대상은 겉모습이 아니라 **"n개 문제를 n-1개 문제로 바꾸는 분해 규칙 그 자체"**다. 이 규칙 한 겹은 `n`이 몇이든 완전히 동일하며, 이 동일한 규칙이 재귀적으로 반복 적용되면서 n마다 전혀 다른 복잡한 겉모습이 저절로 "펼쳐져 나오는" 것이다. 우리는 그 펼쳐진 결과를 예측하거나 일반화할 필요가 없다.

### 2. 재귀적 신뢰(recursive leap of faith)
숙련자가 재귀 코드를 설계할 때, 재귀 호출(`hanoi(n-1, ...)`)의 내부에서 실제로 무슨 일이 벌어지는지 절대 따라가지 않는다. 대신 "이 함수는 n-1개를 start에서 aux로, end를 거쳐 옮겨준다고 이미 약속되어 있다"고 **믿고**, 그 약속이 지금 이 지점에서 필요한 것과 논리적으로 맞아떨어지는지만 확인한다. 실행을 손으로 추적하는 것(워킹 메모리 부담이 크고 실수하기 쉬움)과, 이 논리적 약속만 확인하는 것(빠르고 부담이 적음)은 완전히 다른 검증 방식이다.

### 3. 재귀 문제를 만났을 때의 실전 절차
1. 이 함수가 정확히 무엇을 하는지 한 문장으로 정의한다.
2. 그 정의를 "n-1짜리 같은 함수"로 어떻게 조립할 수 있는지, 딱 한 겹만 생각한다 (결과가 아니라 구조를 본다).
3. 종료 조건(가장 작은 경우)을 정한다.
4. 2, 3번을 코드로 옮긴다.
5. 작은 값(n=1, n=2)으로 그 분해 규칙이 실제로 맞는지 검산한다 (이 검산은 "일반화"가 아니라 "이미 세운 일반 규칙에 대한 확인"이다).

### 4. 정직한 현재 위치 평가
이 대화를 통해 얻은 것은 재귀에 대한 **개념적 이해**(구조, 종료 조건의 필요성, 겉모습이 아닌 분해 규칙을 봐야 한다는 것)이지, 새로운 재귀 문제를 마주쳤을 때 스스로 분해 규칙을 떠올리는 **실전 감각**이 아니다. 이 둘은 완전히 다른 종류의 습득이며, 후자는 설명을 듣는 것으로 생기지 않고 여러 재귀 문제에 직접 부딪혀 종료 조건을 잘못 잡아보고 분해가 틀려보는 시행착오를 반복해야 얻어진다. 재귀는 알고리즘 학습자 대부분이 가장 오래 걸린다고 꼽는 영역이라는 것을 감안하고, 이번 세션은 "지도를 얻은 정도"로 정직하게 평가하고 넘어간다.
