# 부분 정사각 행렬 확인 (Check Sub-square) — 풀이 과정 정리

**예상 난이도: 실버 5~4**

## 문제 정의

`n × n` 크기의 정사각 행렬과 정수 `k`가 주어졌을 때, 이 행렬 안에서 `k × k` 크기의 정사각형 구간 중 **모든 값이 같은 구간이 하나라도 존재하는지** 판별하는 함수 `has_uniform_block(matrix, k)`를 작성한다.

```python
matrix = [
    [1, 1, 1, 4],
    [1, 1, 1, 4],
    [1, 1, 1, 4],
    [5, 3, 2, 6]
]
has_uniform_block(matrix, 3)  # → True (왼쪽 위 3x3 구간이 전부 1)
```

- `k × k` 구간은 행렬 안에서 가능한 모든 시작 위치에 대해 검사해야 한다.
- 하나라도 발견되면 그 즉시 `True`를 반환해도 된다.

이 문제는 오늘 다룬 문제들 중 체감 난이도가 가장 높았다. 처음 문제를 봤을 때 "행렬 전체를 훑으면서, 그 안의 모든 부분 구간을 또 훑어야 한다"는 요구가 한 번에 그림이 그려지지 않아 막막했다. 하지만 이 문제는 결과적으로 **문제 해결 방법론(분해) 자체가 핵심 소득이었던 문제**였다.

---

## 핵심 방법론: 복잡한 문제를 "가장 작은 구체적 사례"로 축소하기

문제를 보자마자 전체 구조(4중 반복문)를 한 번에 설계하려는 시도는 포기했다. 대신 DP나 재귀에서 "가장 작은 부분 문제부터 푼다"는 접근과 같은 방향으로, 아래 절차를 밟았다.

### 1단계: 딱 하나의 구체적 사례만 구현해본다
전체 문제("모든 시작 위치에서 확인하라")를 붙잡지 않고, **"시작점이 `(0,0)`으로 고정된 경우 하나만" 구현하는 것**부터 시작했다.

```python
save_value = matrix[0][0]
for i in range(k):
    for j in range(k):
        if matrix[i][j] == save_value:
            total = total + 1
```
이 하나의 사례가 실제로 맞게 동작하는지부터 확인했다 (`k=3`인 예시에서 `total`이 `9`가 나와야 정답).

### 2단계: 사례를 하나씩 더 늘려가며, 반복되는 패턴을 손으로 확인한다
`(0,0)` 하나만으로는 "무엇이 변하는 값인지" 보이지 않는다. 그래서 시작점을 하나씩 옆으로, 아래로 옮긴 사례를 손으로 하나씩 더 만들어봤다.

**시작점 `(1,0)`으로 한 칸 아래:**
```python
save_value = matrix[1][0]
for i in range(k):
    for j in range(k):
        if matrix[i+1][j] == save_value:
            total = total + 1
```

**시작점 `(0,1)`로 한 칸 오른쪽:**
```python
save_value = matrix[0][1]
for i in range(k):
    for j in range(k):
        if matrix[i][j+1] == save_value:
            total = total + 1
```

**시작점 `(1,1)`로 대각선 아래:**
```python
save_value = matrix[1][1]
for i in range(k):
    for j in range(k):
        if matrix[i+1][j+1] == save_value:
            total = total + 1
```

네 개의 사례를 나란히 놓고 보니, `matrix[i][j]`가 `matrix[i+n][j+m]`으로 바뀌는 것뿐이고, `n`, `m`이라는 두 값만 다르게 대입하면 같은 로직이 그대로 재사용된다는 게 눈에 보였다. **하나의 사례만 봐서는 안 보였을 이 규칙성이, 서로 다른 사례를 여러 개 나란히 놓고 비교하자 비로소 드러났다.**

### 3단계: 반복되는 패턴을, 그 패턴을 만들어내는 반복문으로 승격시킨다
네 가지 사례가 "시작점(`n`, `m`) 두 개만 바뀌는 같은 로직의 반복"이라는 걸 확인한 순간, "이 초기값(`save_value`)을 설정하는 과정 자체를 반복문으로 감싸면 되겠다"는 발상에 도달했다. 시작점이 두 축(행, 열)으로 움직여야 하니 자연스럽게 이중 for문이 필요했고, 그 이중 for문 안에 이미 만들어둔 "구간 내부 검사용 이중 for문"을 넣으면 4중 반복문 구조가 완성됐다.

이 과정에서 "초기값이 바뀌면, 그 초기값과 비교되는 `matrix` 인덱스도 같은 만큼 움직여야 한다"는 것(`matrix[i][j]` → `matrix[i+n][j+m]`)도 자연스럽게 함께 정리됐다.

**이 절차(구체적 사례 하나 → 사례를 늘려 패턴 관찰 → 패턴을 반복문으로 역산)를 밟은 결과, 최초 완성 코드가 약 30분 만에 나왔고, 그 코드는 이미 버그 두 개만 고치면 되는 거의 완성된 상태였다.**

---

## 버전 1: 최초 완성본 (버그 2개 포함)

```python
def has_uniform_block(matrix, k):
    total = 0
    for n in range(len(matrix) - k):
        for m in range(len(matrix) - k):
            save_value = matrix[n][m]
            for i in range(k):
                for j in range(k):
                    if matrix[i+n][j+m] == save_value:
                        total = total + 1
                        if total == k * k:
                            return True
    return False
```

### 버그 1: `total`이 구간마다 리셋되지 않음
`total = 0`이 반복문 전체 바깥, 딱 한 번만 실행되는 위치에 있었다. 그 결과 `n`, `m`이 바뀌어 완전히 새로운 구간을 검사하기 시작해도 `total`이 초기화되지 않아, "이번 구간이 균일한가"가 아니라 "지금까지 검사한 모든 구간에 걸쳐 우연히 값이 일치한 횟수의 누적 총합"을 세는 셈이 됐다.

**반례로 확인:** 체스판처럼 값이 번갈아 배치된 행렬(`[[1,2,1,2],[2,1,2,1],[1,2,1,2],[2,1,2,1]]`, `k=2`)은 어떤 2×2 구간을 봐도 균일하지 않아 정답은 `False`여야 하는데, 버전 1은 `True`를 반환했다 — 서로 다른 구간에서 조금씩 일치한 게 누적되어 `total`이 `k*k`에 도달해버린 것.

**수정:** `total = 0`을 `n`, `m` 반복문 안쪽(구간이 바뀔 때마다)으로 옮겨서, 매 구간마다 새로 세도록 함.

### 버그 2: 시작점 범위 계산 오류 (경계값 누락)
```python
for n in range(len(matrix) - k):
```
`k×k` 구간이 행렬 안에 들어가려면 시작점 `n`은 `n + k <= len(matrix)`를 만족해야 하므로, 유효한 시작점은 `0`부터 `len(matrix) - k`까지(포함)다. 그런데 `range(len(matrix) - k)`는 `0`부터 `len(matrix) - k - 1`까지만 만들어내, **가장 마지막 유효 시작점 하나가 통째로 누락**됐다.

**반례로 확인:** `4×4` 행렬에서 `k=2`일 때, 균일한 구간이 오른쪽 아래 끝(시작점 `(2,2)`)에만 존재하는 경우 `False`(오답)가 나왔다 — 마지막 유효 시작점이 순회 대상에서 빠졌기 때문.

**수정:** `range(len(matrix) - k + 1)`로 바꿔서 마지막 유효 시작점까지 포함.

---

## 버전 2: 최종 정답 (버그 2개 모두 수정)

```python
def has_uniform_block(matrix, k):
    for n in range(len(matrix) - k + 1):
        for m in range(len(matrix) - k + 1):
            total = 0
            save_value = matrix[n][m]
            for i in range(k):
                for j in range(k):
                    if matrix[i+n][j+m] == save_value:
                        total = total + 1
                        if total == k * k:
                            return True
    return False
```

### 검증
```
[[1,1,1,4],[1,1,1,4],[1,1,1,4],[5,3,2,6]], k=3   -> True   (원래 예시)
체스판(균일 구간 없음), k=2                        -> False  (total 리셋 버그 검증)
균일 구간이 오른쪽 아래 끝(마지막 시작점)에만 존재, k=2  -> True   (경계값 버그 검증)
3x3 행렬, 균일 구간 없음, k=2                       -> False
1x1 행렬, k=1                                     -> True  (극단 케이스)
```
전부 정답과 일치함을 확인했다.

---

## 이 문제에서 얻은 핵심 교훈: "분해"의 감각

이 문제를 통해 얻은 가장 중요한 소득은 코드 자체가 아니라 **복잡한 문제를 마주쳤을 때 손을 대는 방법**이었다.

### 절차 요약
1. 전체 문제를 한 번에 설계하려 하지 않고, **가장 작고 구체적인 사례 하나**로 범위를 좁힌다.
2. 그 사례를 하나만 보고 끝내지 않고, **비슷하지만 조금씩 다른 사례를 몇 개 더** 손으로 만들어본다.
3. 여러 사례를 나란히 놓고 비교해, **무엇이 고정이고 무엇이 변하는 값인지** 패턴을 관찰한다.
4. 그 변화하는 값을 만들어내는 **반복문을 거꾸로 역산**해서 씌운다.

### 왜 이 절차가 결정적이었는가
문제가 요구하는 최종 구조(4중 반복문, 시작점 계산, 구간 내부 검사)를 처음부터 통째로 그리려 했다면 시작조차 못 했을 가능성이 높다. 실제로 이 문제를 처음 봤을 때 "너무 복잡해서 뭘 해야 할지" 막막했던 지점이 정확히 그 지점이었다. 이를 극복한 방법은 "더 열심히 고민해서 한 번에 큰 그림을 그리는 것"이 아니라, **문제의 스케일 자체를 의도적으로 줄여서, 손으로 확인 가능한 크기까지 쪼갠 것**이었다.

### 오늘 이전 문제들과의 연결
이 절차는 사실 오늘 이미 여러 번 등장했던 원리(구체적 사례를 손으로 확인 → 패턴 관찰 → 일반화)의 연장선이다.
- 다이아몬드: 표를 그려서 줄마다 별 개수 패턴을 관찰
- 전치 행렬: 숫자 몇 개를 넣어봐서 `matrix[i][j] ↔ matrix[j][i]` 규칙을 확인

다만 이번 문제는 그 스케일이 훨씬 커진 경우(4중 반복문이 필요한 문제)였는데도 **똑같은 절차가 그대로 통했다**는 점이 확인됐다. 즉 이 절차는 특정 문제 유형에 종속된 요령이 아니라, 문제의 복잡도와 무관하게 적용 가능한 **범용적인 문제 해결 전략**이라는 것이 이번 문제를 통해 명확해졌다.

### 부가적으로 확인된 것: 좋은 반례를 만드는 능력
`total` 리셋 버그와 경계값 버그 모두, 그냥 "다시 봐서" 찾은 게 아니라 **버그가 명확히 드러나는 구체적인 반례(체스판 행렬, 마지막 시작점에만 블록이 있는 행렬)를 직접 설계**해서 검증했다. 이는 "분해해서 문제에 접근하는 능력"과 "분해해서 검증하는 능력"이 같은 뿌리(복잡한 전체를 작고 통제된 사례로 쪼개는 사고)를 공유한다는 것을 보여주는 사례이기도 하다.
