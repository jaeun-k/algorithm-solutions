# 회전된 정렬 배열의 최솟값/최댓값 위치 찾기 — 경계 조건 심층 분석

회전된 정렬 배열에서 값을 탐색하는 문제(`search_rotated`)를 풀던 중, 정작 어려웠던 부분은 "탐색" 자체가 아니라 **"회전 지점(최솟값 또는 최댓값)을 O(log n)으로 찾는 것"**이었다. 이 문서는 그 지점에 집중해서, 여러 번의 실패와 그 원인 진단, 그리고 최종적으로 도달한 일반 원칙을 정리한다.

---

## 문제 정의

`[4,5,6,7,0,1,2]`처럼, 원래 오름차순이던 배열이 한 지점을 기준으로 통째로 회전된 상태다. 이 배열은 항상 **두 개의 오름차순 구간**으로 나뉜다 — `구간A(4,5,6,7)`와 `구간B(0,1,2)`. 이때 회전 지점, 즉 **최솟값(구간B의 시작)** 또는 **최댓값(구간A의 끝)**의 위치를 이진 탐색으로 O(log n)에 찾는다.

---

## 실패 사례들: 각각 정확히 무엇이 문제였는가

### 실패 1: `numbers[left]`와 비교 + 내림(`//`) `mid`
```python
if numbers[mid] > numbers[left]:
    left = mid + 1
else:
    right = mid - 1
```
`(left+right)//2`는 **항상 내림**되어 `mid`가 구조적으로 `left` 쪽에 치우친다. 구간에 원소가 2개 남으면(`right=left+1`) `mid`는 항상 `left`와 같아진다. 그 상태에서 `numbers[left]`와 비교하면 **자기 자신과 자기 자신을 비교**하는 셈이 되어(`numbers[left] > numbers[left]`는 항상 거짓), 실제 값과 무관하게 판단이 고장 난다.

반례로 실측: `[26, 28, 6, 15]`에서 최댓값(28, 인덱스 1)을 놓치고 엉뚱한 답이 나옴. 20,000개 랜덤 검증에서 약 16%가 틀림.

### 실패 2: `else`에서 `right = mid - 1` 대신 `left = mid` 사용 (반대쪽 시도)
`mid == left`가 될 때 `numbers[mid] <= numbers[left]`도 자기 자신 비교로 항상 참이 되어, 매번 강제로 `right`만 줄어든다. 무한 루프는 안 나지만(자기 자신 비교가 항상 한쪽 분기로만 가서 `right`가 매번 줄어들긴 하므로 종료는 보장됨), 그 대가로 진짜 답이 있는 자리를 검증 없이 건너뛰어 틀린 결과를 낸다. `[3,1,2]`에서 target=3(인덱스 0)을 놓치는 것으로 확인.

### 실패 3: 내가 만든 "최댓값 찾기" 변형 (우변 비교 + `left=mid+1`)
```python
if numbers[mid] > numbers[right]:
    right = mid
else:
    left = mid + 1
return right
```
5개의 작은 사례로는 우연히 다 맞아서 "정답"으로 오판했으나, 10,000개 랜덤 검증에서 대량 실패(`[20,23,28,-29,...]`에서 최댓값 28을 놓침). 우변과 비교하는 것 자체가 최댓값 찾기에는 구조적으로 안 맞는 접근이었다 (아래 "핵심 원칙" 참조).

**교훈: 몇 개의 작은 테스트 케이스를 통과했다고 알고리즘이 옳다는 보장이 되지 않는다.** 이 문제 하나에서만 세 번 다른 형태로 반복 확인됨.

---

## 핵심 원칙 1: 비교 기준(anchor)은 "그 값이 속한 구간을, 반복 내내 흔들림 없이 대표하는 지점"이어야 한다

### 왜 `right`가 최솟값 찾기에서 안전한 기준인가
배열의 맨 마지막 원소는 회전 여부와 무관하게 **항상 구간B에 속한다**는 게 최초의 고정된 사실이다. 반복 중 `right`는 오직 `right = mid`로만 움직이는데, 이 이동은 정확히 `numbers[mid] <= numbers[right]`(mid가 이미 구간B로 판정된 순간)에만 일어난다. 즉 **`right`는 절대 "구간A로 확인된 자리"로는 이동하지 않는다** — 반복 내내 구간B의 대표성을 유지한다. 이를 실제로 추적해 확인함: 반복 중 `numbers[right]`는 항상 원본 배열의 마지막 값 이하로 유지됨.

### 왜 `left`가 최댓값 찾기에서 (조건부로) 안전한 기준인가
같은 논리로, 배열의 맨 처음 원소는 항상 구간A에 속한다. `left`는 오직 `left = mid`(최댓값 버전)로만 움직이며, 이 이동은 `numbers[mid] >= numbers[left]`(mid가 구간A로 판정된 순간)에만 일어나 `left`가 항상 구간A의 대표성을 유지한다.

### 왜 서로 바꿔 쓰면 안 되는가
최솟값을 찾을 때 `left`를 기준으로 삼으면, `left`는 `mid+1`로 옮겨가며 "아직 검증 안 된 새 영역"으로 계속 전진한다 — 구간A라는 보장을 유지하지 못하고 불안정해진다. 최댓값을 찾을 때 `right`를 기준으로 삼는 것도 대칭적으로 불안정하다.

---

## 핵심 원칙 2: `mid`를 어느 방향으로 계산할지는, 선택한 비교 기준과 짝을 맞춰야 한다

`(left+right)//2`(내림)는 항상 `left` 쪽으로 치우친다. 그 결과:
- **`right`와 비교하는 경우(최솟값 찾기)**: `mid`가 `right`와 같아지는 일은 **반복 조건이 성립하는 한 절대 없음**(수학적으로 확인: `left<right`일 때 `mid <= right-1`이 항상 성립). 그러므로 내림 그대로 사용해도 안전.
- **`left`와 비교하는 경우(최댓값 찾기)**: `mid`가 `left`와 자주 같아짐 → 자기 자신 비교 문제 발생. 이를 피하려면 `mid = (left+right+1)//2`로 **의도적으로 올림 처리**해 `mid`를 오른쪽으로 밀어야 한다.

**이 두 원칙(비교 기준 선택, mid 방향 보정)은 서로 완전히 독립된 두 개의 결정이다.** 하나는 "무엇과 비교할지", 하나는 "mid를 어떻게 계산할지"이며, 이 둘을 뒤섞어 생각하면(예: "왼쪽이든 오른쪽이든 대칭이니 상관없다") 실패로 이어진다.

---

## 핵심 원칙 3: 포인터를 옮길 때 `mid` 자신을 포함할지 말지는 "이 mid가 아직 후보인가, 확실히 아닌가"로 결정한다

- **mid가 확실히 답이 아니라고 판명됨** → 그쪽 경계는 `mid`를 완전히 건너뛰며 이동 (`left = mid+1` 또는 `right = mid-1`)
- **mid가 아직 답일 수도 있음(후보로 유지)** → 반대쪽 경계는 `mid` 자신을 포함해서 이동 (`right = mid` 또는 `left = mid`)

이 원칙은 최솟값/최댓값과 무관하게 항상 성립하는 절대 규칙이다.

---

## 등호(`>` vs `>=`)는 언제만 문제가 되는가

`left < right`(등호 없는 반복 조건) 하에서는 `mid`가 `right`와 같아지는 일이 수학적으로 절대 없음이 확인되었다. 그래서 **중복값이 없는 배열이라면 등호를 붙이든 안 붙이든 결과가 완전히 동일**하다(5000개 검증, 불일치 0). 다만 **중복값이 있는 배열에서는 등호 유무에 따라 실제로 다른(때로는 틀린) 결과가 나올 수 있음**을 확인했다(`[0,-10,-5,-5,-5]` 등에서 5000개 중 102개 불일치). 이 문제(회전 배열)는 보통 "값이 모두 다르다"고 가정하는 경우가 많아 실전 영향은 제한적이지만, 이 가정이 없는 변형에서는 등호가 결과를 바꿀 수 있다는 것을 실측으로 확인해두었다.

---

## 반복 조건 `<` vs `<=`: 왜 `<`여야만 하는가

```python
while left < right:   # 정답
while left <= right:  # 무한루프 위험
```
`left == right`가 된다는 것은 이미 구간이 원소 1개로 좁혀졌다는 뜻이며, 이는 "답을 찾았다"는 종료 신호여야 한다. `<=`를 쓰면 이 상태에서도 반복문에 다시 들어가는데, 원소 1개짜리 구간에서는 `mid`도 그 자리를 가리키고 비교도 자기 자신과 하게 되어 `left`, `right` 둘 다 더 이상 변하지 않는다 — 실측으로 무한 루프 확인 (`[6,4]`에서 20단계 넘게 `left=1,right=1` 상태로 정지).

`while left < right`를 쓰는 한, `left`는 `mid+1`로만 늘고 `right`는 `mid`(항상 `right`보다 작음)로만 줄어 서로를 앞지르지 않으므로, 반복 종료 시점엔 항상 `left == right`가 보장된다(20,000개 검증, 불일치 0). 따라서 이 구조에서는 `return left`와 `return right`가 완전히 동일한 값을 낸다.

---

## 최종 검증된 정답 코드

```python
def find_min_index(numbers):
    left = 0
    right = len(numbers) - 1
    while left < right:
        mid = (left + right) // 2
        if numbers[mid] > numbers[right]:
            left = mid + 1
        else:
            right = mid
    return left

def find_max_index(numbers):
    left = 0
    right = len(numbers) - 1
    while left < right:
        mid = (left + right + 1) // 2   # 오른쪽으로 치우치게
        if numbers[mid] >= numbers[left]:
            left = mid
        else:
            right = mid - 1
    return left
```
각각 20,000개의 랜덤 회전 배열로 검증해 완전히 일치함을 확인.

---

## 메타 정리: 이 문제에서 얻은 학습법에 대한 교훈

이 문제는 다이아몬드나 최대곱부분배열처럼 "핵심 통찰 하나"로 풀리는 유형이 아니라, **여러 개의 독립적인 경계 조건(비교 기준, mid 방향, 후보 포함 여부, 등호, 반복 조건)이 동시에 맞아떨어져야 하는 유형**이었다. 이런 유형은 "혼자 힘으로 전부 재발명"하려 하기보다, 검증된 템플릿을 가져다 쓰되 **각 조각이 왜 그렇게 되어 있는지 하나씩 뜯어서 이해**하는 방식이 현실적이라는 결론에 도달했다. 다만 오늘 확인한 세 가지 원칙(비교 기준 선택 / mid 방향 보정 / 후보 포함 여부)을 명확히 분리해서 이해했으므로, 이후 "산 모양 배열에서 정점 찾기" 등 같은 계열의 다른 변형 문제를 몇 개 더 겪으며 이 분리된 원칙들을 다시 적용해보는 것이 다음 단계로 필요하다.
