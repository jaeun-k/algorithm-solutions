# 자릿수 합 (Digit Sum) — 풀이 과정 정리


## 문제 정의

양의 정수 `n`이 주어졌을 때, **각 자릿수를 모두 더한 값**을 반환하는 함수 `digit_sum(n)`을 작성한다.

```python
digit_sum(1234)   # 1+2+3+4 = 10
digit_sum(9)      # 9
digit_sum(100)    # 1+0+0 = 1
```

- 문자열로 변환하지 않고, 순수하게 숫자 연산(`%`, `//`)만으로 푼다.
- `n`은 항상 0 이상의 정수라고 가정한다.

지금까지 다뤄온 문제들은 전부 리스트/문자열을 순회하거나 대조하는 유형이었는데, 이 문제는 처음으로 **정수 자체를 나누고 쪼개는** 전혀 다른 결의 문제였다. 이 문서는 최종 정답 코드보다, 그 코드에 도달하기까지 **반복문의 종료 조건을 어떻게 설계할 것인가**를 두고 겪은 사고 과정에 집중해서 정리한 것이다.

---

## 버전 1: `while a > 9` — "여러 자릿수인 동안만 반복"

사람이 자연스럽게 떠올리는 방식대로, "아직 두 자릿수 이상 남아있는 동안만 나머지를 떼어낸다"는 조건으로 접근했다.

```python
def digit_sum(n):
    a = n
    total = 0
    if a < 10:
        total = a
    else:
        while a > 9:
            remain = a % 10
            total = total + remain
            a = a // 10
            if a < 10:
                total = total + a
    return total
```

이 버전은 정답이지만, "마지막 한 자릿수"가 `while` 조건(`a > 9`) 때문에 반복문 밖으로 밀려나면서, 그걸 처리하기 위한 예외 처리가 두 군데(`if a < 10: total = a` — 애초에 한 자릿수인 경우, `if a < 10: total = total + a` — 반복 도중 한 자릿수가 된 경우)나 필요해졌다.

---

## 버전 2: `while a > 0`으로 전환 — 조건은 바꿨지만 로직을 안 맞춤

"한 자릿수 예외 처리를 없애려면 조건 자체를 바꿔야 하지 않을까"라는 방향으로 조건만 `a > 0`으로 교체했다.

```python
def digit_sum(n):
    a = n
    total = 0
    while a > 0:
        if a > 9:
            remain = a % 10
            total = total + remain
            a = a // 10
        else:
            total = total + a
    return total
```

**결과: 무한 루프.** 조건은 바꿨지만 `else` 블록(한 자릿수가 됐을 때) 안에서 `a` 값 자체가 갱신되지 않아, `a`가 영원히 그 값에 머물러 `while a > 0`이 계속 참으로 유지됐다. 이를 급하게 `break`로 막아 정답은 나왔지만, 여전히 `if`/`else`로 두 경우를 나누는 구조 자체는 남아있는 상태였다.

---

## 버전 3: 최종 — 조건과 로직을 함께 정리

```python
def digit_sum(n):
    a = n
    total = 0
    while a > 0:
        remain = a % 10
        total = total + remain
        a = a // 10
    return total
```

`if`/`else`/`break` 없이, `while a > 0` 하나와 `%`/`//` 두 줄만으로 한 자릿수든 여러 자릿수든 `0`이든 전부 균일하게 처리된다.

### 왜 이게 되는가
`a`가 한 자릿수(예: `9`)일 때도 `a % 10`은 그냥 `9`이고 `a // 10`은 `0`이다. 즉 이 두 연산 자체는 자릿수 개수와 무관하게 항상 똑같이 동작한다. "9보다 크냐 아니냐"로 경우를 나눌 필요가 애초에 없었던 것이다. 매 반복 나머지를 더하고 몫으로 줄이기만 하면, 몫이 자연스럽게 0에 도달하는 순간 `while a > 0`이 스스로 거짓이 되며 끝난다.

### 검증
```
digit_sum(10)    → 1
digit_sum(9)     → 9
digit_sum(12345) → 15
digit_sum(1234)  → 10
digit_sum(100)   → 1
digit_sum(0)     → 0
```
전부 정확히 일치함을 확인했다.

---

## 핵심 논의: 왜 `while a > 0`을 곧바로 떠올리지 못했는가

이 문제의 진짜 핵심은 코드 자체가 아니라, 버전 1에서 버전 3으로 곧장 가지 못했던 이유를 스스로 진단한 부분이다.

### 문제 제기
"몫을 계속 나누다 보면 결국 0이 된다"는 사실은, 사실 코드를 짜기 전에 이미 로직 자체(`remain = a % 10`, `total = total + remain`, `a = a // 10`) 안에 들어있는 정보였다. 이 세 줄을 손으로 몇 번만 굴려봐도(`12345 → 1234 → 123 → 12 → 1 → 0`) 몫이 결국 0에 도달한다는 게 자연스럽게 드러난다. 그런데 이 조건(`a > 0`)을 곧바로 떠올리지 못했다.

### 원인 진단
막힌 원인은 **"반복문에 대한 집착"**이었다. 파이썬 문법상 `while 조건:` 이 항상 로직보다 **위에** 쓰여야 하다 보니, 코드를 작성하는 순서(위 → 아래: 반복문 선언 → 내부 로직)와 코드를 설계하는 사고 순서를 동일한 것으로 취급해버렸다. 즉 "반복문 조건부터 정하고, 그다음 안의 로직을 짜야 한다"는 순서로 접근했고, 정작 조건을 결정할 단서가 반복문 **안**의 로직 쪽에 있다는 가능성 자체를 고려하지 못했다. 그 결과 안의 로직을 끝까지 손으로 굴려서 "결국 0이 되는구나"를 직접 확인하는 단계도 건너뛴 채, 조건을 추상적으로만 고민하게 됐다.

### 또 다른 원인: "한 자릿수는 특별하다"는 암묵적 전제
반복문 집착과는 별개로, 더 근본적인 원인이 하나 더 있었다. **"한 자릿수가 되면 더 이상 나눠지지 않으니, 여러 자릿수일 때와는 뭔가 근본적으로 다르게 취급해야 한다"**는 사고가 거의 당연한 전제처럼 깔려 있었다. 한 자릿수 시점에서 "이제 더는 쪼갤 게 없다"는 사실 자체는 맞지만, 그 사실에서 곧바로 "그러니 이 경우는 반드시 별도로 분기해서 처리해야 한다"로 건너뛰어 버린 것이다.

실제로는 한 자릿수에 대해서도 `%`, `//` 연산 자체는 아무 문제 없이 똑같이 작동한다(`9 % 10 == 9`, `9 // 10 == 0`). 즉 "더 이상 안 나눠진다"는 건 로직이 멈춰야 할 이유가 아니라, 오히려 몫이 자연스럽게 0이 되어 반복문이 스스로 끝나는 신호일 뿐이었다. 그런데 "한 자릿수 = 특별 케이스"라는 전제가 너무 강하게 자리 잡고 있어서, **한 자릿수까지 포함해 모든 경우를 동일한 로직 하나로 처리해버릴 수 있다는 가능성 자체를 처음부터 멀리 두고 있었다.** 반복문 조건을 어떻게 짤지 고민하기 이전에, 애초에 "예외 분기가 필요 없을 수도 있다"는 선택지 자체를 검토 대상에 넣지 않았던 셈이다.

### 일반화한 원칙
이 문제는 `max_subarray_sum_notes.md`에서 정리했던 원칙의 **반대 방향 버전**이다.
- 그때: "초기값은 이후 로직이 검증해줄 임시값일 뿐이니, 아무 원소로나 시작해도 로직 안에서 정직하게 검증된다" (초기값 ← 로직에 의해 정당화됨)
- 이번: "종료 조건은 로직을 몇 번 굴려보면 스스로 드러난다" (종료 조건 ← 로직을 실행해보고 역으로 결정됨)

두 경우 모두 공통적으로, **값(초기값이든 조건이든)을 미리 완벽하게 정해두고 나서 로직을 짜는 것**이 아니라, **로직을 먼저 몇 번 굴려보고 그 결과가 조건/초기값이 무엇이어야 하는지를 알려준다**는 사고 순서가 필요했다는 점에서 같은 원리를 공유한다.

### 앞으로의 적용
반복문 조건이 바로 안 떠오를 때는, 조건부터 추상적으로 고민하기보다 **안에 들어갈 로직을 먼저 손으로 몇 번 굴려보고, 그게 자연스럽게 끝나는 지점을 확인한 뒤 조건을 거꾸로 정하는** 순서로 접근하는 것이 유효한 전략으로 정리됐다.

---

## 사용된 핵심 도구

- `%` (나머지 연산자) — 맨 오른쪽 자릿수를 떼어냄
- `//` (정수 나눗셈) — 떼어낸 자릿수를 제외한 나머지 부분으로 축소
- `while` 반복문 — 종료 조건을 로직 자체의 결과(몫이 0에 도달함)에 맞춰 설계

## 이 문제의 의의

리스트/문자열을 순회·대조하는 기존 패턴들과 달리, **정수 자체를 자릿수 단위로 쪼개는 전혀 다른 뼈대**(`%`+`//` 반복)를 처음 다뤄봤다는 점에서 이번 학습의 "넓이"를 채우는 문제였다. 이 뼈대는 이후 자릿수 뒤집기, 자릿수 기반 회문 판별, 자릿수 개수 세기 등 숫자를 다루는 여러 문제의 공통 기반이 된다.
