# 스택 응용 2문제 정리

문자열 치환의 함정에서 출발해 스택 구조를 다시 확인한 문제와, 그 스택 감각을 회전 문제에 적용하며 인덱스/딕셔너리 방향을 정리한 문제를 함께 기록한다.

---

## 문제 1: 햄버거 만들기

재료가 순서대로(빵-야채-고기-빵) 쌓일 때마다 상수는 완성된 햄버거를 포장한다. 최종적으로 몇 개의 햄버거를 포장하는지 구한다.

```python
solution([2, 1, 1, 2, 3, 1, 2, 3, 1])  # 2
solution([1, 3, 2, 1, 2, 1, 3, 1, 2])  # 0
```

### 시행착오: 문자열 이어붙이기 + `in` 방향 오류 + `.replace()` 재할당 누락
```python
def solution(ingredient):
    s = ""
    count = 0
    for i in ingredient:
        s += str(i)
        if s in "1231":
            count += 1
            s.replace("1231", "")
    return count
```
두 가지 문제가 겹쳐 있었다.
1. `s in "1231"`은 "s가 '1231'의 부분 문자열인가"를 확인하는 것이라, 원래 의도(문자열 s 안에 "1231"이 포함되어 있는가)와 방향이 반대였다. 이대로면 s가 "1", "12", "123", "1231" 같은 짧은 경우만 True가 되고, s가 길어지면 절대 조건을 만족하지 못한다.
2. 파이썬 문자열은 불변(immutable)이라 `.replace()`는 원본을 바꾸지 않고 새 문자열을 반환만 한다. 반환값을 받지 않고 그냥 호출만 하면 s는 전혀 바뀌지 않은 채 다음 반복으로 넘어간다.

### 핵심 걸림돌: "중간을 지우면 인덱스가 밀리는 것 아닌가"
문자열 중간 부분("1231")이 삭제되면 그 뒤에 오던 문자들의 인덱스가 앞으로 당겨지는데, 이를 어떻게 처리해야 할지 감이 잡히지 않았다. 그러나 이는 애초에 원본 배열의 인덱스로 접근하는 방식이 아니라, **별도의 스택(리스트)에 순서대로 쌓으면서 그 스택의 끝(맨 위) 4개만 매번 확인**하면 해결되는 문제였다. 스택에서 4개를 pop하면 스택이 그만큼 짧아질 뿐이고, "몇 번째 인덱스가 몇 번으로 밀렸는가"라는 개념 자체가 발생하지 않는다. 다음 재료는 항상 그 짧아진 스택의 새로운 끝에 쌓이므로, "이어붙인 결과가 또 조건을 만족하는 경우"도 매번 append 직후 즉시 검사하는 절차 안에서 자동으로 처리된다.

### 정답 코드
```python
def solution(ingredient):
    stack = []
    count = 0
    for item in ingredient:
        stack.append(item)
        if len(stack) >= 4 and stack[-4:] == [1, 2, 3, 1]:
            count += 1
            for _ in range(4):
                stack.pop()
    return count
```
`stack[-4:]`(리스트 슬라이싱)는 스택에 4개 미만이 쌓여 있어도 인덱스 에러 없이 있는 만큼만 반환하기 때문에 `len(stack) >= 4` 체크와 함께 안전하게 동작한다. 슬라이싱으로 매번 새 리스트를 잘라내는 대신, 조건이 맞을 때만 `pop()`을 4번 호출해 실제 스택을 줄여나가는 방식이 반복 슬라이싱보다 효율적이다.

---

## 문제 2: 괄호 회전하기

문자열을 왼쪽으로 x칸 회전시켰을 때 올바른 괄호 문자열이 되는 x의 개수를 구한다.

```python
solution("[](){}")   # 3
solution("}]()[{")   # 2
solution("[)(]")     # 0
```

### 시행착오 1: 스택이 비는 순간마다 카운트해버림
```python
for i in range(len(s)):
    ...
    if not l:
        result += 1   # 안쪽 for문 내부에서 매번 체크
```
스택이 비는 시점을 "문자 하나 처리할 때마다" 확인하면, `"(){}"`처럼 문자열 안에서 짝이 여러 번 완성되는 경우 한 번의 회전에서 `result`가 여러 번 증가해버린다. "문자열 전체를 끝까지 본 뒤, 최종적으로 스택이 비어있는가"를 판단해야 하므로, 이 체크는 안쪽 for문이 완전히 끝난 뒤 단 한 번만 이루어져야 한다.

### 시행착오 2: 짝이 안 맞는 경우를 명시적으로 처리하지 않음
```python
elif left.index(l[-1]) == right.index(s[i]):
    l.pop()
```
이 조건이 거짓일 때(짝이 안 맞는 괄호가 나왔을 때) 아무 처리도 하지 않고 그냥 다음 문자로 넘어가버리는 허점이 있었다. 짝이 틀렸다면 그 즉시 실패로 처리하고 검사를 중단해야, 이후 문자들 때문에 스택이 우연히 비어 잘못 카운트되는 사고를 막을 수 있다.

### 회전을 실제로 만들 필요가 없다는 것
```python
s = s[1:] + [s[0]]   # 매 회전마다 새 리스트 생성 (O(n) 비용)
```
회전마다 리스트를 새로 만드는 대신, 원본 문자열은 그대로 두고 인덱스 계산(나머지 연산)만으로 회전 효과를 낼 수 있다.
```python
ch = s[(start + i) % n]
```
괄호 짝 검사 로직은 "지금 보고 있는 문자가 무엇인지"만 필요로 할 뿐, 그 문자가 "실제로 새로 만들어진 리스트의 원소"인지 "인덱스 계산으로 얻어낸 문자"인지와는 무관하다. 즉 회전 방식과 검사 로직은 서로 독립적인 문제였다.

### 딕셔너리로 짝 맞추기 — 키의 방향을 정하는 법
`left.index(...) == right.index(...)`처럼 두 리스트에서 각각 위치를 찾아 번호를 비교하는 간접적인 방식 대신, 짝 관계를 직접 매핑하는 딕셔너리를 쓸 수 있다. 이때 중요한 것은 **어느 쪽을 키로 삼느냐에 따라 조회 시 무엇을 넣어야 하는지가 달라진다**는 점이다.

- 키를 "닫는 괄호"로 잡으면(`{")": "(", ...}`), 지금 보고 있는 문자(`ch`, 닫는 괄호)를 그대로 키로 넣어 `pairs[ch]`로 짝이 되는 여는 괄호를 얻는다.
- 키를 "여는 괄호"로 잡으면(`{"(": ")", ...}`), 조회할 때 키로 넣어야 하는 것은 `ch`가 아니라 스택 맨 위(`stack[-1]`)다. `pairs[stack[-1]]`로 짝이 되는 닫는 괄호를 얻어 `ch`와 비교해야 한다.

두 방향 다 정답에 도달할 수 있지만, 딕셔너리를 만든 방향과 조회 시 무엇을 키로 넣을지가 반드시 일치해야 한다는 점을 놓치면 KeyError나 잘못된 비교로 이어진다.

### 최종 정답
```python
def solution(s):
    n = len(s)
    pairs = {")": "(", "]": "[", "}": "{"}
    result = 0

    for start in range(n):
        stack = []
        valid = True
        for i in range(n):
            ch = s[(start + i) % n]
            if ch in pairs:
                if not stack or stack[-1] != pairs[ch]:
                    valid = False
                    break
                stack.pop()
            else:
                stack.append(ch)
        if valid and not stack:
            result += 1

    return result
```
회전을 인덱스 계산으로 대체해 리스트 재생성 비용을 없앴고, 짝이 안 맞는 경우 즉시 `break`로 끊어 잘못된 카운트를 방지했으며, 스택이 비어있는지 판단은 안쪽 루프가 끝난 뒤 한 번만 수행한다.

---

## 이 두 문제에서 얻은 교훈

### "삭제/변형"이 필요한 문제에서 원본 인덱스를 신경 쓸 필요가 없는 경우가 많다
문자열이나 배열 중간을 지우면 인덱스가 밀린다는 걱정은, 원본을 직접 인덱스로 조작하려 할 때만 발생한다. 별도의 스택(리스트)에 순서대로 쌓고 그 끝만 검사/제거하는 방식으로 바꾸면, "인덱스가 밀리는 것"이 아니라 "스택이 짧아지고 다음 원소가 그 위에 쌓이는 것"이 되어 문제 자체가 사라진다.

### 회전/이동을 다루는 문제는 "실제로 자료구조를 재생성"하지 않아도 된다
배열이나 문자열을 회전시켜야 하는 문제에서, 매번 새 리스트를 만드는 대신 나머지 연산(`% n`)으로 인덱스만 옮기는 방식이 가능한 경우가 많다. 이후에 오는 로직(스택 검사 등)이 "값 하나"만 필요로 한다면, 그 값이 실제로 재구성된 자료구조에서 온 것인지 인덱스 계산으로 얻어낸 것인지는 무관하다.

### 딕셔너리는 키의 방향을 정하는 것이 핵심이다
딕셔너리를 쓸 때 헷갈리는 지점은 딕셔너리 자체의 복잡성이 아니라, "무엇을 키로 두고 무엇을 값으로 둘 것인가"를 정하는 판단이다. 방향을 한 번 정했다면, 이후 조회 시 어떤 변수를 키 자리에 넣어야 하는지는 그 방향에 종속적으로 결정된다.
