# 달리기 경주(Running Race) — "이름 → 등수" 매핑으로 O(n) 만들기

리스트 탐색(`index()`)으로 접근하면 시간 초과가 나는 이유를 확인하고, 딕셔너리로 "이름 ↔ 등수"를 동시에 관리하는 방식으로 해결한 문제.

---

## 문제 정리

**출처: 프로그래머스**

1등부터 등수 순서대로 담긴 문자열 배열 `players`와, 해설진이 부른 이름(직전 선수를 추월한 선수)을 담은 배열 `callings`가 주어질 때, 경주가 끝난 뒤의 최종 등수를 반환한다.

```python
players = ["mumu", "soe", "poe", "kai", "mine"]
callings = ["kai", "kai", "mine", "mine"]
# → ["mumu", "kai", "mine", "soe", "poe"]
```

제약사항 중 핵심은 이것:
- `players` 길이 최대 50,000
- `callings` 길이 최대 1,000,000

`callings`가 최대 100만 번 호출된다는 게 이 문제의 시간복잡도를 결정짓는 지점이다.

---

## 핵심 관찰 1: 한 번의 호출은 "인접 교환" 하나일 뿐이다

문제를 처음 보면 "추월"이라는 말 때문에 여러 등수를 한꺼번에 밀어내야 할 것 같은 착각이 든다. 하지만 정의를 다시 읽으면:

> 해설진은 선수들이 **자기 바로 앞의 선수**를 추월할 때 그 이름을 부른다.

즉 한 번의 `calling`은 항상 **자신과 바로 한 등수 위 선수, 이 둘만의 자리 교환**이다. 여러 명을 제치는 경우도 결국 "바로 앞 선수 추월"이 여러 번 연달아 불릴 뿐이지, 한 번의 호출이 여러 칸을 이동시키는 게 아니다.

이 관찰이 없으면 매 호출마다 리스트를 끼워 넣고 빼는(`insert`/`pop`) 방식으로 접근하게 되는데, 애초에 그럴 필요가 없다 — **매번 정확히 두 원소만 스왑**하면 된다.

```python
players[c], players[c - 1] = players[c - 1], players[c]
```

---

## 핵심 관찰 2: `list.index()`로 등수를 찾으면 왜 안 되는가

가장 직관적인 접근은 이것이다:

```python
# 시간 초과 버전
for name in callings:
    c = players.index(name)
    players[c], players[c - 1] = players[c - 1], players[c]
```

`players.index(name)`은 리스트를 처음부터 순회하며 이름을 찾는다 — O(n). 이걸 `callings` 100만 번 안에서 매번 호출하면 전체 시간복잡도는 **O(n × m)**, 즉 최악의 경우 50,000 × 1,000,000에 근접한다. 명백히 시간 초과.

여기서 필요한 건 "이름을 알면 등수를 즉시 아는" 구조, 즉 **이름 → 등수를 키-값으로 미리 매핑해둔 딕셔너리**다. 딕셔너리 조회는 O(1)이므로, `callings`를 한 번 순회하는 전체 흐름이 O(n + m)으로 줄어든다.

---

## 핵심 관찰 3: 리스트와 딕셔너리를 "동시에" 정합성 있게 갱신해야 한다

단순히 "이름 → 등수" 딕셔너리 하나만 만들어서는 부족하다. 왜냐하면:

- `players` 리스트는 **등수 → 이름**을 표현하고
- `p_dict`는 **이름 → 등수**를 표현하는데

교환이 일어날 때마다 이 둘을 **같이** 갱신하지 않으면 서로 어긋난다. 스왑되는 두 선수는 다음 네 가지가 한 세트로 움직여야 한다:

1. 추월한 선수(`name`)의 등수를 1 낮춘다 (`p_dict[name] -= 1`)
2. 추월당한 선수(`players[c-1]`)의 등수를 1 높인다 (`p_dict[players[c-1]] += 1`)
3. `players` 리스트에서 두 이름의 위치를 스왑한다

이 중 하나라도 순서가 꼬이면(예: 리스트를 먼저 스왑한 뒤 `players[c-1]`을 참조하면 이미 바뀐 값을 참조하게 됨) 잘못된 결과가 나온다. 그래서 딕셔너리 갱신을 리스트 스왑보다 먼저, `c-1`이 "아직 스왑 전의 등수"를 가리키는 시점에 처리해야 한다.

---

## 정답 코드

```python
def solution(players, callings):

    p_dict = {}
    rank = 0

    for name in players:
        p_dict[name] = rank
        rank += 1

    for name in callings:
        c = p_dict[name]
        p_dict[name] -= 1
        p_dict[players[c - 1]] += 1
        players[c], players[c - 1] = players[c - 1], players[c]

    return players
```

### 트레이스 (`kai`가 처음 불렸을 때)
```
players = ["mumu", "soe", "poe", "kai", "mine"]
p_dict  = {"mumu":0, "soe":1, "poe":2, "kai":3, "mine":4}

name = "kai"
c = p_dict["kai"] = 3
p_dict["kai"] -= 1        → 2
p_dict[players[2]] += 1   → p_dict["poe"] = 3
players[3], players[2] = players[2], players[3]
→ players = ["mumu", "soe", "kai", "poe", "mine"]
```
딕셔너리와 리스트가 서로 어긋나지 않고 한 세트로 갱신되는 것을 확인할 수 있다.

---

## 이 문제에서 얻은 교훈

### "존재 여부 vs 개수"에 이어 "값 조회 vs 위치(인덱스) 조회"라는 축이 하나 더 있다
앞서 애너그램 문제들에서는 "있냐 없냐(set)"와 "몇 개 있냐(dict의 값)"를 구분하는 게 핵심이었다면, 이 문제는 그 다음 단계다 — **"값을 알 때 그 값의 위치를 즉시 구할 수 있는가"**. 리스트는 위치를 알면 값을 O(1)에 구하지만(`players[i]`), 값을 알 때 위치를 구하려면 `.index()`로 O(n) 탐색을 해야 한다. 이 비대칭을 없애기 위해 "값 → 위치"의 역방향 매핑을 딕셔너리로 별도로 들고 있는 것이 이 문제의 핵심 트릭이다.

### 자료구조를 두 개 쓸 때는 "누가 먼저 갱신되어야 하는가"까지 설계해야 한다
딕셔너리 하나만 쓰는 문제들과 달리, 이 문제는 리스트와 딕셔너리 **두 개를 동시에 정합성 있게 유지**해야 했다. 자료구조를 추가하는 것 자체보다, 두 자료구조 사이의 갱신 순서(딕셔너리를 먼저 — 아직 스왑 전 값을 참조할 수 있을 때)를 정확히 설계하는 것이 이 문제의 실질적인 난이도였다.
