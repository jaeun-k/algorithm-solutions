# 그리디(Greedy) 알고리즘 입문

지금까지의 DP류(최댓값/최솟값 누적)와 다른 새 알고리즘 패러다임. "매 순간 당장 가장 좋아 보이는 선택을 하고, 그 선택을 절대 무르지 않는다"는 전략. 회의실 배정(정렬 기반) → 최소 점프 횟수(범위 갱신 기반) 순서로 다뤘다.

---

## 개념: 그리디란

DP가 "여러 가능성을 다 열어두고 비교"하는 반면, 그리디는 "한 번 정한 선택은 다시 안 돌아본다." 항상 정답이 보장되지는 않고, 문제가 "탐욕적 선택이 최종적으로 최적이 되는 성질"을 가질 때만 통한다.

### 그리디가 통하는지 어떻게 아는가 — 완벽한 판별법은 없다
문제 텍스트만 보고 100% 확신하는 공식은 없으며, 실전에서는:
1. **탐욕적 선택 속성**과 **최적 부분 구조**가 이론적 조건이지만, 최적 부분 구조는 DP도 요구하므로 구분 기준이 되지 못한다. 진짜 구분은 "당장의 선택이 절대 후회를 안 만든다"는 게 보장되는가.
2. 실전에서 가장 현실적인 방법은 **직접 반례를 몇 개 만들어보는 것** — 다른 정렬 기준(예: 시작 시간, 길이)으로도 시도해보고 반례가 나오는지 확인.
3. 몇 가지 전형적 패턴(구간 최대 선택 → 끝나는 시간 정렬, 구간 최소 커버 → 시작 기준 확장 등)이 있으며, 이는 패턴 노출을 통해 서서히 체득되는 감각이다.

---

## 문제 1: 최대 이익을 내는 회의실 배정

한 회의실에서 겹치지 않게 최대한 많은 회의를 진행할 때, 최대 몇 개까지 가능한지 구한다.

```python
max_meeting([(1,4),(3,5),(0,6),(5,7),(3,9),(5,9),(6,10),(8,11),(8,12),(2,14),(12,16)])  # 4
```

### 정답 코드
```python
def max_meeting(meetings):
    meetings.sort(key=lambda x: (x[1], x[0]))
    count = 0
    last_end_time = 0
    for start, end in meetings:
        if start >= last_end_time:
            count += 1
            last_end_time = end
    return count
```

### 왜 "끝나는 시간" 기준인가
회의가 일찍 끝날수록 남은 시간이 가장 많이 확보되어 뒤에 더 많은 회의를 배치할 수 있다. 시작 시간이나 길이 기준으로는 반례(짧지만 애매한 위치의 회의를 먼저 잡으면, 뒤에 두 개를 넣을 수 있었던 공간을 하나로 막아버림)가 존재한다.

### `lambda`와 `sort(key=...)` 정리
- **`lambda x, y: x+y`**: 이름 없는 즉석 함수. `def`로 풀면 `def 이름없음(x,y): return x+y`와 동일 — 콜론 뒤 표현식이 자동으로 반환값이 됨. 한 번 쓰고 버릴 함수를 그 자리에서 만들 때 사용.
- **`key`에는 왜 `()` 없이 함수 자체를 넘기는가**: `key=get_sort_key()`처럼 괄호를 붙이면 그 자리에서 즉시 실행되어 결과값 하나만 넘어간다. `sort()`는 리스트의 **각 원소마다 반복적으로** 이 함수를 호출해야 하므로, "그 자리에서 실행된 값"이 아니라 "필요할 때마다 실행할 함수 자체"를 받아야 한다.
- **`x`는 리스트 전체가 아니라 원소 하나**: `sort()`는 리스트를 순회하며 원소를 하나씩 꺼내 `key` 함수에 넘긴다. `x = (1,4)`처럼 이미 낱개로 분리된 튜플이 넘어오므로, `x[0][0]`이 아니라 `x[0]`, `x[1]`로 바로 접근한다 (`for start, end in meetings:`에서 이미 낱개로 분해받은 것과 같은 원리).
- **역할 분담**: `key` 함수는 각 원소의 "정렬 기준값"만 계산해서 반환하고, 실제 비교와 순서 변경은 전적으로 `sort()`가 담당한다. `(x[1], x[0])`이 튜플이므로, `sort()`의 튜플 비교 규칙(첫 값 우선, 같으면 다음 값)이 자동으로 "끝시간 1순위, 시작시간 2순위" 정렬을 만들어준다.

---

## 문제 2: 최소 점프 횟수

`numbers[i]`가 인덱스 `i`에서 최대 몇 칸 점프 가능한지를 나타낼 때, 마지막 인덱스까지 도달하는 최소 점프 횟수를 구한다.

```python
min_jumps([2, 3, 1, 1, 4])   # 2
min_jumps([1, 1, 1, 1])      # 3
min_jumps([0])               # 0
```

### 시행착오 1: 판단 기준을 "그 자리의 점프 능력값"으로 착각
```python
if numbers[i] > max_value:   # numbers[i] 자체의 크기로 판단
    max_value = numbers[i]
    index = i
```
"점프 능력값(`numbers[i]`) 자체가 큰 곳"을 고르는 것과 "실제로 가장 멀리 도달하는 곳(`i + numbers[i]`)"을 고르는 것은 다르다. 반례: `[3,5,0,4,0,0,0,1]`에서 인덱스1(값5, 실제 도달점 6)이 인덱스3(값4, 실제 도달점 7)보다 능력값은 크지만 실제로는 덜 멀리 간다. 이 기준으로는 정답(2)보다 많은 점프(3)가 나왔다.

### 철학적 질문: "i+numbers[i]로 판단하면 이미 다음 단계까지 보는 것 아닌가, 그럼 그리디가 아니지 않나?"
`i+numbers[i]`(다음 점프의 도달 범위)를 기준으로 삼는 게 "두 걸음을 미리 계산하는 것" 아니냐는 의문이 나왔다. 결론: 여전히 그리디가 맞다. 그리디의 "당장의 최선"이란 "미래를 전혀 안 본다"는 뜻이 아니라 "여러 선택지를 열어두고 비교하지 않고, 그 자리에서 하나로 확정한다"는 뜻이다. 지금 점프의 착지점(여러 후보 중 하나)을 정하는 유일한 합리적 기준이 "다음 기회의 크기"일 뿐, 3번째·4번째 점프까지 전부 계산해서 비교하는 DP식 접근과는 다르다. 결정 이후의 일은 전혀 안 보고, 그다음 반복에서 또 같은 잣대로 그 자리에서 결정할 뿐이다.

### 최종 정답 코드 (독자적 구조)
```python
def min_jumps(numbers):
    start = 0
    max_value = 0
    index = 0
    result = 0
    count = 0
    while start < len(numbers) - 1:
        for i in range(start + 1, start + numbers[start] + 1):
            count += 1
            if i == len(numbers) - 1:
                index = i
                break
            if numbers[i] + count > max_value:
                max_value = numbers[i] + count
                index = i
        start = index
        count = 0
        max_value = 0
        result += 1
    return result
```
`max_value = numbers[i] + count`로 비교 기준과 저장값의 단위를 일치시켜 해결. 3000개 랜덤 케이스로 BFS 정답과 대조 검증 완료.

### 참고: "정석" 단일 패스 버전과의 비교
```python
def min_jumps(numbers):
    jumps = 0
    current_end = 0
    farthest = 0
    for i in range(len(numbers) - 1):
        farthest = max(farthest, i + numbers[i])
        if i == current_end:
            jumps += 1
            current_end = farthest
    return jumps
```

**핵심 차이 — 왜 이 버전은 `i`를 재배치(초기화)할 필요가 없는가**: `i`는 점프해서 이동한 위치를 반영하지 않고 그냥 `0,1,2,...`로 순서대로 한 칸씩만 전진한다. "점프해서 어디로 갔는지"라는 정보는 `farthest`(지금까지 스친 모든 인덱스에서 얻은 최선의 도달 가능 지점)라는 별도 변수가 대신 누적해서 기억한다. 반면 사용자 버전은 `start = index`로 포인터를 물리적으로 재배치하는 방식이다.

### 메타 발견: 여러 알고리즘에 걸쳐 반복되는 하나의 뼈대
정석 버전의 구조("포인터는 한 방향으로만 전진, 지나오며 얻은 정보는 별도 누적 변수가 대신 기억")는 알고리즘의 종류(그리디/DP/슬라이딩 윈도우)와 무관하게 반복적으로 나타나는 패턴임을 확인했다:
- `max_subarray_sum`: `subarray_sum`(국소 누적) + `max_sum`(전체 최고 기록, 절대 안 줄어듦)
- `max_product_subarray`: `max_end`/`min_end`(국소 최대·최소) + `total`(전체 최고 기록)
- 슬라이딩 윈도우: `left`/`right`가 절대 되돌아가지 않고, 지나온 정보(`window_sum`)를 그대로 들고 전진
- 이번 문제: `farthest`(누적된 최선의 정보) + `i`(절대 되돌아가지 않는 포인터)

**"어떤 알고리즘을 쓸지 판단하는 층위"(그리디냐 DP냐)와 "그걸 실제로 O(n)에 구현하는 층위"(누적 변수 + 단방향 포인터)는 서로 다른 층위이며, 후자는 알고리즘 종류를 넘어 재사용되는 공통 뼈대라는 것을 확인했다.** 다만 "물리적으로 위치를 재배치하며 눈으로 상태를 확인하는" 직관적 스타일도, 반례를 스스로 진단하고 고치는 과정에서 실질적으로 유효했다는 점에서 결함이 아니라 트레이드오프임을 재확인.
