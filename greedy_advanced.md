# 그리디 심화 — 동전 거스름돈, 분수 배낭, 0/1 배낭 반례, 최소 회의실

`greedy_intro.md`(회의실 배정, 최소 점프) 이후 이어서 다룬 그리디 문제들. "그리디가 통하는 경우"뿐 아니라 "그리디가 왜 안 통하는가"까지 직접 반례로 확인한 세션.

---

## 문제 1: 동전 거스름돈

큰 단위 동전부터 최대한 채우는 그리디로 최소 동전 개수를 구한다.

```python
def min_coins(amount):
    coins = [500, 100, 50, 10]
    count = 0
    for coin in coins:
        count += amount // coin
        amount = amount % coin
    return count
```

### 리팩터링 과정
처음엔 `five100s`, `one100s`, `five10s`, `one10s`처럼 각 동전 단위마다 변수를 따로 만들고, `% 500 % 100 % 50`처럼 나머지 계산을 길게 이어붙인 형태로 완성했다. "압축할 여지가 있다"는 감은 있었으나 방법을 못 찾았는데, 원인은 도구 선택의 문제였다 — 최근 성공적으로 써온 "함수로 묶기"(하노이의 탑, `reverse()` 재사용 등) 쪽으로 먼저 생각이 쏠렸기 때문. 실제로 필요했던 건 "같은 로직을 서로 다른 값(동전 단위들)에 반복 적용"하는 상황이었으므로, 그 값들을 리스트에 담고 `for`로 순회하는 게 맞는 도구였다.

**구분 기준**: "똑같은 로직을 다른 값에 반복 적용" → 값들을 리스트+`for`로 순회. "서로 다른 지점에서 같은 복잡한 절차를 여러 번 호출" → 함수로 분리.

---

## 문제 2: 분수 배낭 채우기 (Fractional Knapsack)

물건을 쪼갤 수 있는 배낭 문제. 무게당 가치가 높은 순으로 채운다.

### 정답 코드 (원본 방식: 누적 무게와 용량 비교)
```python
def max_value(capacity, items):
    items.sort(key=lambda x: x[1] / x[0])
    current_weight = 0
    current_value = 0
    for weight, value in items[::-1]:
        current_weight += weight
        current_value += value
        if current_weight > capacity:
            current_value -= (current_weight - capacity) * (value / weight)
            break
    return current_value
```

### 최적화 버전 (남은 용량을 거꾸로 소진)
```python
def max_value(capacity, items):
    items.sort(key=lambda x: x[1]/x[0], reverse=True)
    remaining = capacity
    total_value = 0
    for weight, value in items:
        if weight <= remaining:
            total_value += value
            remaining -= weight
        else:
            total_value += value * (remaining / weight)
            break
    return total_value
```
"누적해서 한계와 비교"하는 대신 "남은 자원을 직접 줄여나가는" 방식으로 바꿔 계산을 한 단계 줄임. 두 버전 다 3000개 랜덤 케이스로 정답과 정확히 일치함을 검증.

### `sort(key=..., reverse=True)` — 두 인자를 동시에 지정
`key`(비교 기준)와 `reverse`(오름/내림차순)는 서로 독립된 키워드 인자라 동시에 지정 가능. `[::-1]`로 뒤집는 것과 결과는 같지만, `sort` 하나로 정렬 방향까지 지정하는 더 관용적인 형태.

### 메타 관찰: 최적화된 풀이일수록 직관과 멀어진다
"누적 후 비교"가 사람이 자연스럽게 떠올리는 방식이고, "자원을 거꾸로 소진"은 이미 정답을 알고 있는 상태에서 한 번 더 다듬은 결과에 가깝다는 것을 재확인 — 최소 점프, 회전 배열 이진 탐색 등에서 이미 반복 확인된 패턴.

---

## 문제 3: 0/1 배낭 채우기 — 그리디가 왜 안 통하는지 반례로 확인

**개념 확인용 (풀이가 아니라 검증이 목적)**

물건을 쪼갤 수 없을 때, 무게당 가치 기준 그리디를 그대로 적용하면 최적해를 놓칠 수 있음을 직접 반례로 구성해 확인.

### 반례 구성 과정
1차 시도(`(6,12),(4,7),(4,7)`, capacity=10)는 우연히 그리디와 최적해가 일치해 반례가 되지 못함. 2차 시도에서 성공:

```python
capacity = 6
items = [(5, 20), (2, 7), (2, 7), (2, 7)]
# 무게당 가치: (5,20)→4.0, (2,7)→3.5 (세 개 동일)

# 그리디: (5,20)만 담고 남은 1칸엔 아무것도 못 넣음 -> 20
# 최적: (2,7) 세 개로 정확히 6칸을 꽉 채움 -> 21
```
그리디(20) < 진짜 최적(21)임을 브루트포스(모든 부분집합 확인)로 검증.

### 왜 이런 일이 생기는가
분수 배낭에서는 자투리 공간을 물건의 일부로 정확히 채울 수 있어 낭비가 없지만, 0/1 배낭은 "통째로 담거나 아예 안 담거나"만 가능해 무게당 가치가 가장 높은 물건을 먼저 담는 게 자투리 공간의 낭비로 이어질 수 있다. "무게당 가치가 높다"는 게 "전체 조합에서 최적"임을 보장하지 못한다는 것이 그리디와 DP를 가르는 핵심 차이. 이 문제는 그리디가 아니라 DP로 풀어야 하는 대표 사례이며, DP 자체는 이번 세션에서 다루지 않고 존재만 확인.

---

## 문제 4: 최소 회의실 개수 (Minimum Meeting Rooms)

모든 회의를 동시에 진행하는 데 필요한 최소 강의실 수를 구한다.

```python
min_rooms([(0, 30), (5, 10), (15, 20)])   # 2
min_rooms([(1, 5), (5, 10)])              # 1
```

### 시행착오 1: 직전 회의 하나만 추적
```python
if end > current_start:
    count += 1
current_start = start
```
바로 직전 회의와만 비교했으나, "직전이 아니라 그 이전에 이미 끝난 강의실을 재사용할 수 있는 경우"를 놓친다는 것을 스스로 발견.

### 시행착오 2: 하나의 "가장 늦은 시작 시간"만 추적
```python
if end > max_start:
    count += 1
if max_start < start:
    max_start = start
```
빈 강의실을 재사용하는 경우는 해결했으나, 여러 개의 강의실이 각각 다른 상태를 가질 수 있다는 근본 문제에 부딪힘 — 새 강의를 기존 강의실에 배정했을 때 "그 강의실의 새로운 최대 시작 시간"을 어떻게 갱신할지, 매번 전체를 다시 비교하지 않고는 알 수 없어 막힘. 세 번째 시도(튜플 리스트로 각 강의실 상태 기록)도 같은 근본 문제(상태를 매번 갱신해야 함)에 부딪혀 중단.

### 발상 전환: "어느 강의실인지" 정보를 완전히 버린다
막힌 이유는 "회의 하나하나가 어느 강의실을 쓰는지" 계속 추적하려 했기 때문. 실제로 필요한 건 "지금 이 순간 동시에 열려 있는 회의가 몇 개인가"라는 숫자 하나뿐이며, 이는 어느 회의가 몇 번째 강의실인지와 무관하게 구할 수 있다.

### 최종 정답 코드 (스위핑 기법)
```python
def min_rooms(meetings):
    starts = sorted([m[0] for m in meetings])
    ends = sorted([m[1] for m in meetings])

    i = 0
    j = 0
    ongoing = 0
    max_rooms = 0

    while i < len(starts):
        if starts[i] < ends[j]:
            ongoing += 1
            max_rooms = max(max_rooms, ongoing)
            i += 1
        else:
            ongoing -= 1
            j += 1

    return max_rooms
```
회의 정보를 통째로 다루지 않고 "시작 시간들"과 "끝 시간들"로 완전히 분리해 각각 정렬한 뒤, 두 정렬된 리스트를 투 포인터로 동시에 훑으며 "다음으로 벌어질 사건이 시작인지 끝인지"를 판단해 카운터를 증감시킨다. `<`(등호 없음)를 사용해 "끝나는 시간과 시작 시간이 같으면 겹치지 않는다"는 조건을 정확히 반영. 3000개 랜덤 케이스로 검증.

**이 발상(스위핑/이벤트 기반 처리)은 스스로 재발명하기 어려운, 그 자체로 이름이 있는 표준 기법**이라는 것을 확인 — 다이아몬드류(작은 사례에서 패턴을 관찰해 일반화)와 달리, "데이터를 통째로 다루던 관점을 사건 단위로 완전히 분해하는" 관점 전환 자체가 미리 알고 있어야 떠오르는 유형이라는 점에서 하노이의 탑, 회전 배열 경계 조건과 같은 계열로 분류됨.

### 부수 개념
- **리스트 컴프리헨션**: `[m[0] for m in meetings]`는 `result=[]; for m in meetings: result.append(m[0])`의 한 줄 축약형. `python_curriculum_v2.md`에서 의도적으로 미뤄온 문법이 실전에서 처음 자연스럽게 등장.
- **`sorted()` vs `.sort()`**: `.sort()`는 리스트 메서드로 원본을 직접 바꾸고 반환값이 `None`. `sorted()`는 내장 함수로 원본은 그대로 두고 정렬된 새 리스트를 반환 — `len()`, `max()`처럼 어떤 리스트든 즉시 넘겨 결과를 바로 받을 수 있어 한 줄로 이어 쓰기 편리함.
