# 문자 빈도 짝 찾기 (Character Pair Count) — 풀이 과정 정리

**예상 난이도: 브론즈 3~2**

## 문제 정의

문자열 `s`가 주어졌을 때, 이 문자열 안에서 **정확히 2번 등장하는 문자가 몇 종류**인지 세는 함수 `count_paired_chars(s)`를 작성한다.

- 대소문자는 구분한다 (`'A'`와 `'a'`는 다른 문자).
- 공백/특수문자도 하나의 문자로 취급한다.
- 3번 이상 등장하거나 1번만 등장하는 문자는 세지 않는다.

```python
count_paired_chars("banana")
# b: 1번, a: 3번, n: 2번 → 정확히 2번인 문자는 n 하나 → 결과: 1
```

이 문서는 최종 정답 코드뿐 아니라, 거기 도달하기까지 몇 시간에 걸쳐 시도했던 접근들과 각각이 왜 실패했는지, 그리고 어떤 계기로 방향을 바꿔 해결했는지까지 기록한 것이다.

---

## 막혔던 지점: "이미 센 문자를 다시 세지 않는 법"

문제 자체의 로직(각 문자가 몇 번 나오는지 세고, 정확히 2번인 것만 카운트)은 금방 이해했지만, **"한 번 처리한 문자를 어떻게 표시해서 중복 계산을 막을 것인가"**에서 몇 시간을 소비했다. 시도했던 접근들은 다음과 같다.

### 시도 1: 문자를 다른 값으로 치환하기
처리한 문자를 다른 문자(혹은 특수 기호)로 바꿔서 "이미 처리했다"는 표시를 하려는 접근.
**실패 이유:** 원본 문자열 자체를 바꾸면, 그 변경이 이후 다른 문자의 카운트(`.count()` 등)에도 영향을 준다. 즉 표시를 위한 조작이 데이터 자체를 오염시켜, 다른 문자를 셀 때 부작용이 생긴다.

### 시도 2: 매 반복마다 시작 인덱스를 한 칸씩 미루기
"이미 지나간 앞부분은 다시 안 본다"는 취지로, 매번 순회 시작점을 오른쪽으로 한 칸씩 옮기는 접근.
**실패 이유:** 이 방식은 "앞에서 이미 나온 문자를 다시 안 세는" 효과는 있지만, 동시에 뒤에서 세는 카운트 범위 자체가 계속 줄어든다. 그 결과 실제로는 3번 이상 등장하는 문자가, 시작점이 밀린 상태에서 세어지면 "2번"으로 잘못 집계될 수 있다.

### 시도 3: 이전에 풀었던 "연속된 구간 상태 체크" 방식 재활용
최장 증가 구간/최대 부분합 문제에서 썼던 "지금 상태를 변수로 들고 가면서 매 스텝 갱신"하는 패턴을 그대로 적용하려는 시도.
**실패 이유:** 그 패턴은 "연속된 구간"을 다루는 문제에 특화된 방식인데, 이 문제는 문자의 등장이 연속적이지 않고 문자열 전체에 흩어져 있어 애초에 적용 대상이 아니었다.

### 시도 4: 수학적 경우의 수(순열/조합) 접근
**실패 이유:** 이 문제는 "존재하는 경우의 수를 계산"하는 문제가 아니라 "실제 데이터를 순회하며 세는" 문제라, 순열/조합 공식으로 환원되지 않는다.

---

## 막막함의 정체: 몰라서가 아니라, 안 배운 도구가 필요했던 것

네 가지 시도가 전부 막힌 뒤 느낀 감정은 "발상이 안 떠오른다"는 것이었다. 그런데 실제 원인은 발상력의 문제가 아니라, **"원본 데이터를 건드리지 않고, 별도의 목록에 처리 이력을 기록해두는" 방식 자체를 한 번도 써본 적이 없었다는 것**이었다. 이 방식에 필요한 도구(빈 리스트 생성, `append`, `in`/`not in`, `.count()`)를 그때까지 배운 적이 없었기 때문에, 아무리 시간을 들여도 혼자 힘으로 재발명하기 어려운 상황이었다.

이 지점에서 얻은 교훈: **"몇 시간을 고민해도 안 풀릴 때, 그게 발상이 안 떠올라서인지 아니면 애초에 안 배운 도구가 필요한 것인지 구분하는 게 중요하다.** 전자는 시간을 들이면 뚫리지만, 후자는 아무리 시간을 줘도 혼자서는 못 뚫는다.

---

## 해결의 실마리: 원본을 건드리지 않고, 별도의 "명단"을 만든다

핵심 발상 전환은 이것이었다:

> 원본 문자열은 전혀 건드리지 않는다. 대신 완전히 새로운 빈 리스트를 하나 만들어서, "이미 처리한 문자들의 명단"을 그 리스트에 따로 적어둔다. 새 문자를 만날 때마다, 그 명단에 이미 있는지만 확인한다.

이 발상에 필요했던, 이번에 처음 배운 도구들:

- **빈 리스트 만들기**: `save_chars = []`
- **`.append()`**: `save_chars.append(value)` — 리스트 맨 뒤에 값 추가
- **`in` / `not in`**: `value not in save_chars` — 리스트(또는 문자열) 안에 값이 있는지 확인 (참/거짓을 돌려주는 비교 연산자)
- **`.count()`**: `s.count(value)` — 문자열(또는 리스트) 안에서 특정 값이 몇 번 등장하는지 세기

### 부수적으로 짚은 개념: `for ... in ...`과 `in` 연산자의 차이
같은 `in`이라는 단어가 두 가지 완전히 다른 문법에 쓰인다는 점도 이 과정에서 헷갈렸던 부분이다.
- `for value in s:` → **반복문 문법의 일부.** `s`를 한 글자씩 순회하며 `value`에 대입하는 동작. 참/거짓과 무관.
- `value not in save_chars` → **비교 연산자.** `<`, `==`처럼 참/거짓(`bool`) 값 하나를 돌려주는 독립적인 표현식.

---

## 최종 정답 코드

```python
def count_paired_chars(s):
    save_chars = []
    total = 0

    for value in s:
        if value not in save_chars:
            save_chars.append(value)
            if s.count(value) == 2:
                total = total + 1

    return total

print(count_paired_chars("asfawr1234fdqaassd"))  # → 2
```

### 메커니즘
1. `save_chars`: 원본과 완전히 별개인 빈 리스트. "이미 처리한 문자들의 명단" 역할.
2. `for value in s:`: 문자열을 한 글자씩 순회.
3. `if value not in save_chars:`: 이 글자를 이미 처리한 적 있으면 통째로 건너뛴다. 처음 마주친 글자만 아래 로직을 탄다.
4. `if s.count(value) == 2:`: 원본 문자열 전체에서 이 글자가 정확히 2번 등장하는지 확인하고, 맞으면 `total`을 늘린다.
5. `save_chars.append(value)`: 이 글자를 명단에 등록해, 이후 다시 마주쳐도 3번 조건에서 걸러지게 한다.

### 검증
- `count_paired_chars("asfawr1234fdqaassd")` → `2` (정확히 2번 등장하는 문자는 `f`, `d` 두 종류)
- `count_paired_chars("banana")` → `1`
- `count_paired_chars("")` → `0`
- `count_paired_chars("aabbccddee")` → `5` (전부 정확히 2번씩)
- `count_paired_chars("AaAa")` → `2` (대소문자 구분 확인)

전부 정확히 일치함을 확인했다.

---

## 이 문제에서 얻은 교훈

1. **원본 데이터를 건드리지 않고, 별도의 "기록"을 곁에 두는 패턴**은 이번이 처음이었지만, 앞으로 "중복 제거", "이미 방문한 곳 체크" 같은 유형에서 계속 반복해서 등장하는 뼈대다.
2. **막힘의 원인 진단이 우선이다.** 발상이 안 떠오르는 것과, 애초에 안 배운 도구가 필요한 것은 겉보기에 똑같이 "막막함"으로 느껴지지만 대처법이 다르다 — 전자는 시간을 들여 파고들 가치가 있고, 후자는 학습 진도를 먼저 확인하는 게 우선이다.
3. **문자열 문제가 유독 어렵게 느껴졌던 이유**는 문자열 자체의 특수성이 아니라, 문자열 관련 문제들이 우연히 "아직 안 배운 리스트 심화 도구(append, in, count)"를 필요로 하는 유형과 자주 겹쳤기 때문이었다.
