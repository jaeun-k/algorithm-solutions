# N진법 변환과 인덱스 매칭 2문제 정리

임의의 진법 변환에서 나머지-역순 처리 원리를 정리한 N진수 게임 문제와, 스택에 인덱스를 저장해 결과 배열에 직접 매칭시키는 방식으로 순서 문제를 없앤 주식가격 문제를 정리한다.

---

## 문제 1: N진수 게임 (임의 진법 변환 + 인덱스 슬라이싱)

여러 사람이 돌아가며 0부터 숫자를 한 자리씩 말하는 게임을 n진법으로 진행할 때, 특정 순서(p번째)의 사람이 t개 동안 말하게 되는 숫자를 구한다.

```python
solution(2, 4, 2, 1)     # "0111"
solution(16, 16, 2, 1)   # "02468ACE11111111"
```

### 최종 코드
```python
def solution(n, t, m, p):
    digits = "0123456789abcdef"
    answer = "01"
    for i in range(2, t*m + 1):
        re = ""
        while i > 0:
            re = digits[i % n] + re
            i //= n
        answer += re

    return answer[p-1::m][:t]
```

### 시행착오와 정리한 개념

**진법 변환은 밑(base)이 상수든 변수든 원리가 같다.** 10진수를 n진법으로 바꿀 때 `나머지 = num % n`, `num //= n`을 반복하는 로직 자체는 n이 2든 16이든 동일하다. "진법마다 다르게 처리해야 한다"는 감각은 착각이며, 달라지는 건 나눗셈에 쓰이는 숫자 하나뿐이다.

**16진법에서 10~15는 자리올림 이후의 값이지, 별개의 숫자 체계가 아니다.** `F(=15)` 다음은 자리올림이 일어나 `10(=16)`이 되는 것으로, 10진법에서 9 다음 자리올림으로 10이 되는 것과 동일한 구조다. `13`은 `1×16 + 3 = 19`처럼 왼쪽 자리에 밑값을 곱하고 오른쪽 자리를 더하는 계산으로 풀어야 하며, "F 다음이 그냥 10부터 다시 시작한다"는 직관은 자리값 계산을 생략한 오해다.

**나머지를 구하는 과정은 항상 역순으로 나온다.** `num % n`으로 자릿수를 뽑으면 마지막 자리부터 나오므로, `re = str(i%n) + re`처럼 결과 앞에 계속 붙이는 방식으로 순서를 뒤집어야 한다.

**필요한 글자 수를 구하기 위해 숫자 개수를 넉넉히 잡는 것은 안전하게 정당화된다.** 숫자가 10, 11...로 커질수록 자릿수(글자 수)는 늘어나기만 하고 줄어드는 일이 없으므로, "숫자 k개를 만들면 글자는 항상 k개 이상"이라는 관계가 성립한다. 따라서 t*m개의 숫자를 만들면 t*m개 이상의 글자가 확보되어 t개를 뽑기에 충분하다.

**등차수열 위의 인덱스를 뽑는 방법은 여러 개이며, 슬라이싱이 가장 간결하다.** `p-1, p-1+m, p-1+2m, ...`에 해당하는 인덱스만 뽑아내는 문제는, `count` 변수로 항을 하나씩 세어가며 비교하는 방식, `(index - (p-1)) % m == 0`으로 즉시 판별하는 방식, 그리고 `answer[p-1::m]`처럼 슬라이싱 문법 자체로 표현하는 방식이 모두 같은 결과를 낸다. 셋 다 "p-1부터 시작해서 m칸씩 건너뛴다"는 동일한 조건을 표현만 다르게 한 것이며, 슬라이싱은 그 조건을 별도 반복문 없이 한 줄로 표현한다.

---

## 문제 2: 주식가격 (단조 스택 + 인덱스 매칭)

초 단위로 기록된 주식가격 배열에서, 각 시점의 가격이 몇 초 동안 떨어지지 않았는지를 구한다.

```python
solution([1, 2, 3, 2, 3])   # [4, 3, 1, 1, 0]
```

### 최종 코드
```python
def solution(prices):
    n = len(prices)
    result = [0] * n
    stack = []  # 인덱스를 저장

    for j in range(n):
        while stack and prices[stack[-1]] > prices[j]:
            i = stack.pop()
            result[i] = j - i
        stack.append(j)

    while stack:
        i = stack.pop()
        result[i] = (n - 1) - i

    return result
```

### 시행착오와 정리한 개념

**"몇 번째 항까지 안 떨어졌는지"는 pop되는 순간의 인덱스 차이 하나로 확정되며, 누적해서 전달할 필요가 없다.** 처음에는 "pop될 때 뒤에 있던 값에게 +1씩 넘겨준다"는 누적 전달 방식을 떠올렸으나, 실제로는 각 인덱스 `i`가 자신을 pop시킨 시점의 인덱스 `j`와의 차이(`j - i`)만으로 정확한 답이 즉시 확정된다. 앞에 몇 명이 쌓여 있는지는 이 계산과 무관하다.

**결과를 순서대로 이어붙이는 방식은 스택의 pop 순서가 원래 인덱스 순서와 다르기 때문에 실패한다.** `result.append()`로 하나씩 쌓으면 pop되는 순서(값이 뒤섞인 순서)와 원래 순서가 일치하지 않아 최종 배열을 원래 순서로 복원할 수 없다. 대신 `result = [0]*n`을 미리 만들고 `result[i] = ...`처럼 인덱스 자리에 직접 값을 꽂아넣으면, pop되는 순서와 무관하게 항상 올바른 위치에 값이 채워진다.

**루프가 끝난 뒤 스택에 남은 인덱스들은 "마지막 인덱스를 마치 자신을 떨어뜨린 시점처럼" 취급해 처리한다.** 끝까지 자신보다 낮은 값을 만나지 못한 인덱스들은 `result[i] = (n-1) - i`로 마무리한다. 이 부분은 `result[i] = j - i`라는 계산 자체가 인덱스 위치에 직접 매칭되는 방식이었기에, "남은 것들을 어떻게 원래 순서로 되돌릴지"를 별도로 고민할 필요 없이 자연스럽게 이어지는 결론이었다.

**인덱스가 정답의 핵심 재료인 문제는 손으로 그릴 때 값과 인덱스를 함께 적어야 한다.** 값(`[1,2,3,4,1x]`)만 그리고 인덱스를 암묵적으로 넘어가면, 정답이 인덱스 차이에 있는 문제(거리, 간격, 몇 번째)에서는 정작 답의 재료가 그림에서 빠지게 된다. "몇 초", "몇 번째", "거리" 같은 표현이 나오는 문제는 값 아래에 인덱스를 별도 줄로 적어두는 습관이 필요하다.

---

## 이 두 문제에서 얻은 교훈

### 헷갈리는 계산은 머릿속으로 굴리지 말고 표로 늘어놓고 확인한다
16진법의 자리올림처럼 직관이 틀리기 쉬운 부분은, 몇 개의 구체적 사례를 표로 나열해 계산 과정을 눈으로 직접 확인하는 것이 가장 확실하다.

### 값 하나로 확정되는 계산인지, 누적해서 전달해야 하는 계산인지를 먼저 구분한다
스택에서 pop되는 시점의 인덱스 차이처럼, 그 순간의 값 하나로 완전히 확정되는 계산에 불필요하게 "이전 값을 넘겨받아 쌓는" 로직을 추가하면 오히려 문제가 복잡해진다.

### 결과를 인덱스 자리에 직접 매칭시키면 순서 문제 자체가 사라진다
처리 순서(pop 순서)와 원래 순서(인덱스 순서)가 다른 문제에서는, 결과를 순서대로 이어붙이는 대신 미리 크기를 확보한 배열의 해당 인덱스에 직접 대입하는 방식이 순서 복원 문제를 근본적으로 없앤다.
