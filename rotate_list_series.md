# 리스트 회전 (Rotate List) — 3문제 정리

리스트를 오른쪽/왼쪽으로 회전시키는 문제를 난이도를 올려가며 세 가지 방식(슬라이싱 → 나머지 연산 통합 → 뒤집기 3번)으로 풀었다.

---

## 문제 1: 오른쪽 회전 (기본)

**예상 난이도: 브론즈 4**

```python
rotate_right([1, 2, 3, 4, 5], 2)  # → [4, 5, 1, 2, 3]
```

### 정답 코드
```python
def rotate_right(numbers, k):
    left = numbers[len(numbers) - k:]
    right = numbers[:len(numbers) - k]
    return left + right
```
뒤쪽 `k`개와 앞쪽 나머지를 슬라이싱으로 잘라, 리스트끼리 `+`로 순서를 바꿔 이어붙였다. 리스트도 문자열처럼 `+`로 이어붙일 수 있다는 것(`merge_sorted`에서 이미 써본 것)을 재확인.

---

## 문제 2: 양방향 회전 + 범위를 벗어난 k

**예상 난이도: 브론즈 3~실버 5**

`k`가 음수(왼쪽 회전)이거나 리스트 길이보다 클 수 있는 경우까지 하나의 통일된 계산식으로 처리한다.

```python
rotate(numbers, 2)    # 오른쪽 2칸
rotate(numbers, -2)   # 왼쪽 2칸
rotate(numbers, 7)    # 길이(5)보다 큰 값도 처리
rotate(numbers, -7)   # 음수인데 길이보다 큰 값도 처리
```

### 정답 코드
```python
def rotate(numbers, k):
    ln = len(numbers)
    left = numbers[ln - k % ln:]
    right = numbers[:ln - k % ln]
    return left + right
```

### 핵심 발견: 파이썬의 음수 나머지 연산
```python
-2 % 5   # → 3   (음수인데도 결과가 항상 0 이상)
-7 % 5   # → 3   (-7과 -2가 같은 나머지)
```
파이썬은 나머지 연산에서 결과가 항상 **0 이상, 나누는 수 미만**이 되도록 조정한다. 이 성질 덕분에 `k`가 음수든 리스트 길이보다 훨씬 크든, `k % ln`을 거치기만 하면 항상 "0부터 `ln-1` 사이의 실제로 의미 있는 회전 칸수"로 자동 변환되어, 방향에 따른 `if`/`else` 분기 자체가 필요 없어졌다.

### 부수적으로 정리된 개념: 음수 슬라이싱 인덱스
```python
numbers[:-2]   # 뒤에서 2개를 뺀 나머지 (앞부분)
numbers[-2:]   # 뒤에서 2개 (뒷부분)
```
음수 인덱스는 "위치를 표현하는 또 다른 방식"일 뿐이다. `-2`는 `len(numbers) - 2`와 같은 위치를 가리키고, 일단 위치가 정해지면 슬라이싱 규칙(`[시작:]`은 거기부터 끝까지, `[:끝]`은 처음부터 거기 직전까지)은 양수든 음수든 완전히 동일하게 적용된다. 나머지 연산의 음수 처리와 슬라이싱의 음수 인덱스 둘 다, "음수도 항상 유효한 범위로 매끄럽게 흡수시킨다"는 파이썬의 일관된 설계 원칙 위에 있다.

---

## 문제 3: 뒤집기 3번으로 회전하기 (슬라이싱 없이)

**예상 난이도: 실버 4~3**

슬라이싱 없이, 아래 세 단계만으로 회전을 구현한다: ① 전체를 뒤집는다 → ② 앞쪽 k개를 다시 뒤집는다 → ③ 나머지 뒷부분을 다시 뒤집는다.

```python
rotate_by_reverse([1, 2, 3, 4, 5], 2)
# ① 전체 뒤집기:      [5, 4, 3, 2, 1]
# ② 앞 2개 다시 뒤집기: [4, 5, 3, 2, 1]
# ③ 나머지 다시 뒤집기: [4, 5, 1, 2, 3]
```

### 막힌 지점: `range(길이 // 2)`라는 틀에 갇힘
초기 시도에서 세 단계 각각을 별도의 `for`문(`range(...)//2` 형태)으로 짜려 했으나, 계속 오류가 났다.

```python
# 실패한 시도 (2단계 부분)
for i in range((len(numbers)-k-1)//2):
    numbers[i], numbers[k-1-i] = numbers[k-1-i], numbers[i]
```
원인 진단: 지금까지 뒤집기에 써온 `range(len(numbers)//2)`라는 형태는 **"범위가 항상 0부터 시작하는 경우"**에만 맞는 계산이었다. 그런데 2단계(`0`부터 `k-1`까지), 3단계(`k`부터 `len-1`까지)는 시작점이 0이 아니거나 범위 자체가 다르므로, "전체 길이를 2로 나눈 횟수"라는 개념 자체가 애초에 안 맞는 틀이었다. "반복 횟수를 미리 계산해서 range에 넣어야 한다"는 사고에 갇혀, 횟수 계산이 필요 없는 조건으로 전환할 생각을 못한 것이 근본 원인이었다.

### 발견: `left < right`는 범위와 무관하게 항상 통하는 조건
`range(뭔가 // 2)`는 "정확히 몇 번 반복해야 하는지"를 매번 새로 계산해야 하는 반면, `while left < right:`는 "왼쪽 포인터와 오른쪽 포인터가 아직 서로를 지나치지 않았는가"라는, **횟수와 무관하게 항상 똑같이 성립하는 조건**이다. 범위가 `0~4`든 `0~2`든 `3~4`든, `left`와 `right`를 그 범위의 양 끝에 놓기만 하면 조건 자체는 형태를 바꾸지 않고 그대로 작동한다. 이건 palindrome 문제의 포인터(범위) 방식 대안 버전(`is_palindrome_range(s, left, right)`)에서 이미 한 번 쓴 조건이지만, 그때는 범위 하나에만 적용해서 이 조건의 일반성(범위가 달라져도 형태가 안 바뀐다는 것)이 뚜렷이 드러나지 않았었다. 이번처럼 같은 조건을 서로 다른 세 범위에 반복 적용해야 하는 상황을 만나면서 비로소 그 일반성이 확인됐다.

### 최종 정답 코드
```python
def reverse(numbers, left, right):
    while left < right:
        numbers[left], numbers[right] = numbers[right], numbers[left]
        left = left + 1
        right = right - 1
    return numbers

def rotate_by_reverse(numbers, k):
    reverse(numbers, 0, len(numbers) - 1)   # 전체
    reverse(numbers, 0, k - 1)              # 앞 k개
    reverse(numbers, k, len(numbers) - 1)   # 나머지
    return numbers
```
"범위를 뒤집는다"는 동작을 `reverse(numbers, left, right)`라는 함수 하나로 재사용 가능하게 만들고, 세 단계는 각자 다른 범위만 넘겨서 그 함수를 세 번 호출하는 것으로 완성했다.

### 검증
`k=0, 1, 2, 3, 5`와 길이 1, 2짜리 리스트까지 모든 경계 케이스에서 정답과 일치함을 확인했다.

---

## 사용된 핵심 도구

- 리스트 슬라이싱 + `+` 이어붙이기
- 나머지 연산(`%`)의 음수 처리 방식
- 음수 인덱스 슬라이싱(`[:-n]`, `[-n:]`)
- 함수를 새로 정의해서, 그 함수를 다른 함수 안에서 여러 번 호출하기 (`reverse`를 `rotate_by_reverse` 안에서 3번 호출)

---

## 핵심 교훈

### 1. "하나의 반복문으로 압축" vs "여러 개로 나누기" — 판단 기준
다이아몬드 문제 때 "이 반복이 원래 하나의 흐름인지, 원래 여러 개의 하위 문제인지"가 기준이라는 걸 확인했는데, 이번 문제(뒤집기 3번)에서 그 판단 기준을 다시 적용해볼 기회가 있었다. 결론은 같았다 — **"전체 뒤집기 → 앞부분 뒤집기 → 뒷부분 뒤집기"는 문제 자체가 정의하는 세 개의 독립된 하위 작업이지, 억지로 쪼갠 것이 아니다.** 억지로 하나의 반복문에 욱여넣으려 하면 "지금 몇 번째 단계인지"를 추적하는 새로운 상태와 분기가 필요해져서, 원래 없던 우발적 복잡도가 오히려 생겨난다. 시간복잡도도 나누든 합치든 동일(O(n))하므로, 합치는 데서 오는 실질적 이득도 없다.

이 판단(대칭/다단계 구조를 반복문 여러 개로 표현하는 것이 정석일 수 있다는 것)이 다이아몬드 한 문제에서 우연히 나온 예외가 아니라, **여러 단계로 구성된 문제군 전반에 적용되는 재사용 가능한 판단 기준**이라는 것이 이번 문제를 통해 다시 확인됐다.

### 2. 복잡한 문제는 "함수를 작성하기 위한 함수"가 필요할 수 있다
이 문제는 처음으로 **함수 안에서 다른 함수를 호출**하는 구조(`rotate_by_reverse`가 `reverse`를 호출)를 썼다. "하나의 동작(범위 뒤집기)을 재사용 가능한 형태로 별도 함수로 뽑아내고, 그 위에서 더 큰 문제(회전)를 조립한다"는 접근은, 문제가 복잡해질수록(특히 같은 동작을 여러 번, 다른 매개변수로 반복해야 할 때) 점점 더 필요해질 것으로 예상된다. 이번 문제에서는 "함수로 안 묶어도" 반복문 3개로 풀 수 있었지만, 문제가 더 복잡해지면 "먼저 작은 동작을 함수로 분리하고, 그 함수들을 조합해서 큰 문제를 푼다"는 접근 자체를 고려 대상에 넣어야 한다는 것을 확인했다.
