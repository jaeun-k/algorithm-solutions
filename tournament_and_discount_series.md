# 토너먼트 라운드 계산 / 할인 행사 슬라이딩 윈도우 정리

논리적으로 동치처럼 보이지만 실제로는 다른 조건 때문에 생긴 반례, 그리고 딕셔너리 비교를 이용한 슬라이딩 윈도우에서 검사 시점을 정확히 맞추는 과정을 정리한다.

---

## 문제 1: 토너먼트에서 두 참가자가 몇 번째 라운드에 만나는가

N명이 토너먼트로 경기하며, 이긴 사람은 다음 라운드에서 1번부터 다시 번호를 배정받는다. A번과 B번 참가자가 몇 번째 라운드에서 만나는지 구한다.

```python
solution(8, 4, 7)  # 3
```

### 처음 풀이와 반례
```python
def solution(n, a, b):
    count = 1
    while abs(a - b) != 1:
        a = (a+1)//2
        b = (b+1)//2
        count += 1
    return count
```
"번호 차이가 1이면 같은 경기에서 만난다"는 조건으로 접근했으나, `n=8, a=2, b=3`에서 오답이 발생했다. 1라운드 대진은 (1↔2), (3↔4)로 나뉘어 있어 2번과 3번은 번호는 인접해도 서로 다른 경기에 속한다. 즉 **"번호 차이가 1이다"와 "같은 경기에 속한다"는 동치가 아니었다** — 인접한 두 쌍의 경계(2와 3, 4와 5, 6과 7 등)에서만 이 차이가 드러나기 때문에, 준 예제만으로는 반례가 걸리지 않아 발견하기 어려웠다.

### 정답: 같은 경기인지를 그룹핑으로 직접 판별
두 참가자가 같은 경기에 속하는지는 번호를 2개씩 묶은 그룹이 같은지로 판별해야 한다. 1번부터 세는 번호를 2개씩 묶는 공식은 `(번호+1)//2`(또는 `(번호-1)//2`)이며, 이는 이미 반복문 안에서 "다음 라운드 번호"를 계산하는 데 쓰던 식과 동일하다 — 같은 연산이 "다음 값을 만드는 용도"뿐 아니라 "지금 두 값이 같은 그룹인지 판별하는 용도"로도 쓰일 수 있다는 것이 핵심이었다.
```python
def solution(n, a, b):
    count = 1
    while (a+1)//2 != (b+1)//2:
        a = (a+1)//2
        b = (b+1)//2
        count += 1
    return count
```

### 추가 압축: 조건 판별 시점을 한 박자 늦추기
"a, b가 같은 그룹인가"는 "다음 라운드로 넘기면 두 값이 같아지는가"와 동치라는 점을 이용하면, 조건 자체를 미리 계산해 비교할 필요 없이 갱신 후 같아졌는지만 확인하면 된다.
```python
def solution(n, a, b):
    answer = 0
    while a != b:
        answer += 1
        a, b = (a+1)//2, (b+1)//2
    return answer
```
계산량 자체는 동일하지만, "갱신하고 나서 같아졌는지 확인"으로 관점을 뒤집어 조건문을 극단적으로 단순화한 형태다.

### 시간복잡도
N이 2의 거듭제곱이므로 라운드 수는 최대 log₂N번뿐이라 O(log N)이며, 이보다 더 줄일 수 없다(라운드 수 자체가 log N개이므로).

---

## 문제 2: 할인 행사 — 원하는 제품 조합과 일치하는 회원가입 날짜 수

10일 연속 할인 목록 중, 원하는 제품과 수량이 정확히 일치하는 10일 구간(윈도우)의 개수를 구한다.

```python
solution(["banana","apple","rice","pork","pot"], [3,2,2,2,1],
          ["chicken","apple","apple","banana","rice","apple","pork","banana","pork","rice","pot","banana","apple","banana"])  # 3
```

### 핵심 트릭: 딕셔너리끼리 `==` 비교
파이썬 딕셔너리는 `==`로 비교하면 순서와 무관하게 key-value 쌍이 완전히 일치하는지 확인해준다. "원하는 제품별 수량"과 "현재 10일 구간의 제품별 개수"를 각각 딕셔너리로 만들어 `==`로 비교하면 "제품과 수량이 정확히 일치하는가"를 한 줄로 판별할 수 있다.

```python
d = dict(zip(want, number))   # want, number를 짝지어 바로 딕셔너리로
```
`zip()`이 두 리스트의 원소를 같은 위치끼리 튜플로 묶어주고, `dict()`는 그 (키, 값) 형태의 튜플 나열을 그대로 딕셔너리로 변환해준다.

### 시행착오 1: 통째로 매번 새로 세기 (통과는 하지만 최적은 아님)
```python
def solution(want, number, discount):
    answer = 0
    d = dict(zip(want, number))
    sums = sum(number)
    for i in range(len(discount) - sums + 1):
        nd = {}
        for j in discount[i:i+sums]:
            nd[j] = nd.get(j, 0) + 1
        if nd == d:
            answer += 1
    return answer
```
윈도우가 한 칸 옮겨갈 때마다 `nd`를 처음부터 다시 만든다. `sums`가 최대 10으로 고정되어 있어 이 문제 규모에서는 시간제한 내에 통과하지만, 매번 O(sums) 재계산이 들어가는 구조다.

### 시행착오 2: 슬라이딩으로 최적화하는 과정에서 겪은 세 가지 오류
증분 갱신(빠지는 원소 -1, 들어오는 원소 +1)으로 바꾸는 과정에서 다음 오류들을 순서대로 겪고 고쳤다.

**a) 새로 들어오는 원소의 인덱스를 고정값으로 씀**
```python
nd[discount[sums]] = nd.get(discount[sums], 0) + 1   # 항상 같은 원소만 더해짐
```
반복문 변수 `i`를 안 쓰고 상수 `sums`를 그대로 써서, 매 반복마다 같은 원소 하나만 계속 더해지는 오류. `discount[sums]`를 `discount[i]`로 고쳐야 한다.

**b) 개수가 0이 된 key를 지우지 않아 `==` 비교가 깨짐**
```python
nd[out] -= 1   # 0이 되어도 key가 남아있음 → nd == d 비교 시 항상 다르다고 판정될 수 있음
```
`d`에는 등장하지 않는 제품에 대한 key 자체가 없는데, `nd`는 값이 0이어도 key가 남아있으면 두 딕셔너리가 내용상 같아도 `==`가 `False`가 된다. 값이 0이 되면 `del`로 key 자체를 지워야 한다.

**c) 첫 윈도우 또는 마지막 윈도우에 대한 검사 누락**
"검사 → 갱신" 순서로 루프를 짜면 매 반복이 항상 "갱신 이전 상태"만 검사하게 되어, 루프의 마지막 갱신 이후 상태(가장 마지막 윈도우)는 루프 안에서 검사할 기회가 없다. 반대로 첫 윈도우를 루프 진입 전에 별도로 세팅했다면 그 몫의 검사도 루프 밖에서 별도로 해줘야 한다. 즉 **전체 윈도우 개수(`len(discount)-sums+1`)만큼 검사가 정확히 이뤄지도록, 루프 경계에 걸리는 윈도우의 검사를 어디선가 반드시 별도로 책임져야 한다.**

### 최종 정답: 첫 윈도우 세팅 후, 슬라이딩마다 갱신 직후 바로 검사
```python
def solution(want, number, discount):
    answer = 0
    d = dict(zip(want, number))
    sums = sum(number)

    nd = {}
    for i in discount[:sums]:
        nd[i] = nd.get(i, 0) + 1
    if nd == d:
        answer += 1

    for i in range(sums, len(discount)):
        nd[discount[i]] = nd.get(discount[i], 0) + 1
        out = discount[i - sums]
        nd[out] -= 1
        if nd[out] == 0:
            del nd[out]
        if nd == d:
            answer += 1

    return answer
```
"첫 윈도우 세팅 + 검사"와 "이후 각 반복마다 갱신 직후 바로 검사"로 흐름을 나누면, 모든 윈도우가 정확히 한 번씩 검사되면서도 조건 분기 없이 각 단계가 단순하게 유지된다. 하나의 반복문 안에서 채우기와 슬라이딩을 조건문(`if i >= sums` 등)으로 합치는 것도 가능하지만, 그 경우 경계값 조건이 헷갈리기 쉬워 오히려 실수 위험이 커진다. 시간복잡도는 O(n)으로, discount 배열을 한 번은 훑어야 하므로 이론적 하한선과 같다.

---

## 두 문제를 관통하는 공통 원리

- **논리적으로 같아 보이는 두 조건이 실제로는 다를 수 있다.** 경계(그룹과 그룹 사이)에서만 차이가 드러나는 경우, 준 예제만으로는 걸러지지 않으므로 "이 조건이 정말 내가 원하는 것과 동일한가"를 의심하는 습관이 필요하다.
- **같은 연산식이 여러 문맥에서 다른 역할(다음 값 계산 vs 그룹 판별)을 할 수 있다.** 이미 알고 있는 식이라도 다른 용도로 쓸 수 있는지 한 번 더 살펴볼 가치가 있다.
- **슬라이딩 윈도우는 "전체 윈도우 개수만큼 검사가 정확히 이뤄지는가"로 정확성을 검증한다.** 어디서 시작하고 어디서 끝내는지를 정할 때마다, 그 경계에 걸리는 윈도우 하나의 검사를 루프 밖에서 별도로 책임져야 하는 상황이 반드시 생긴다.
- **딕셔너리끼리의 `==` 비교는 순서 무관하게 key-value 전체 일치를 확인해주므로, "구성이 정확히 일치하는가"를 판별하는 문제에서 강력한 도구가 된다.** 단, 값이 0이 된 key는 명시적으로 지워야 비교가 정확해진다.
