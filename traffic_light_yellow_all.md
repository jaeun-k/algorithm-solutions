# 신호등 문제 정리 (주기적 이벤트의 동시 발생 시점 찾기)

여러 신호등이 각자 다른 주기로 초록→노랑→빨강을 반복할 때, 모든 신호등이 동시에 노란불이 되는 가장 빠른 시각을 구한다. 없으면 -1을 반환한다.

```python
solution([[2,1,2],[5,1,1]])  # 13
solution([[1,1,4],[2,1,3],[3,1,2],[4,1,1]])  # -1
```

### 최종 코드
```python
import math

def solution(signals):
    n = len(signals)
    cycles = [sum(signal) for signal in signals]
    length = math.lcm(*cycles)
    l = [0] * length

    for signal in signals:
        start = signal[0] + 1
        cycle = sum(signal)
        while start <= length - signal[1]:
            for i in range(signal[1]):
                l[start + i] += 1
            start += cycle

    if n not in l:
        return -1

    return l.index(n)
```

### 시행착오와 정리한 개념

**"시작점 → 다음 시작점" 간격은 전체 주기(G+Y+R)여야 한다.** 처음엔 "노란불 끝 → 다음 노란불 시작" 간격(R+G)을 주기로 착각했는데, 이건 노란불 자신의 지속 시간(Y)이 빠진 값이다. 시작점 기준 주기는 반드시 G, Y, R을 모두 더한 값이다.

**"처음 시작"과 "그 다음 반복"을 별도 케이스로 나눠서 생각한 것이 병목의 핵심이었다.** `start = G+1`로 초기화하고 루프 안에서 `start += cycle`로 갱신하는 상태-갱신 방식으로 접근하면, "처음"과 "그 이후"가 코드상 물리적으로 다른 줄에 있어 머릿속에서도 자동으로 분리된 케이스처럼 느껴진다. 반면 `t_start(k) = G + k*(G+Y+R)`처럼 k에 대한 하나의 함수로 정의하면, k=0일 때가 자연히 "처음 시작"이 되어 애초에 나눌 필요가 없다. 이 문제뿐 아니라 반복 패턴이 나오는 문제 전반에서, 코드를 짜기 전에 "이걸 파라미터 k의 함수로 한 줄에 쓸 수 있는가"를 먼저 자문하는 습관이 필요하다.

**배열 크기(체의 길이)는 모든 신호등 주기의 최소공배수(LCM)여야 한다.** 각 신호등은 자기 주기 T마다 완전히 같은 상태로 돌아온다. 여러 신호등이 있을 때 "전체 조합이 처음 상태로 완전히 되돌아오는 시점"은 모든 개별 주기의 공배수 중 가장 작은 값, 즉 LCM이다. 그 시점 이후로는 이전과 완전히 같은 패턴이 반복되므로, LCM까지 살펴봐도 답이 없다면 그 이후에도 존재하지 않는다고 확정할 수 있다. 이 결론은 손으로 하나씩 재생해서 확인할 수 없는 규모(여러 신호등이 동시에 상태를 바꾸는 조합)라서, 직접 시뮬레이션이 아니라 "왜 그래야만 하는가"를 논리로 짚어야 납득할 수 있는 종류의 사실이다.

**경계값 조건은 "지금 무엇을 보장하려는가"를 부등식으로 먼저 세워야 헤매지 않는다.** 노란불 구간은 `start`부터 `start+Y-1`까지이며, 이 구간 전체가 배열 범위(0 ~ length-1) 안에 들어가려면 `start + Y - 1 <= length - 1`, 즉 `start <= length - Y`가 성립해야 한다. 이 부등식을 먼저 세우지 않고 감으로 접근하면 "빨간불 구간도 여유로 잡아줘야 하나", "초록불도 빼야 하나" 같은 문제와 무관한 변수가 계속 끼어들게 된다.

---

## 이 문제에서 얻은 교훈

### 상태-갱신 방식과 닫힌 형태 함수 방식은 서로 다른 도구다
패턴이 파라미터(k)에 대해 규칙적으로 반복되는 구조라면, 상태를 정의하고 루프마다 갱신하는 방식보다 `f(k) = ...` 형태의 닫힌 함수로 먼저 정의하는 편이 "처음"과 "그 이후"를 인위적으로 나누는 실수를 막아준다.

### 상태공간이 커서 직접 재생할 수 없는 문제는 연역적 사고로 접근해야 한다
여러 개체가 동시에 주기적으로 상태를 바꾸는 문제는 눈으로 순차 재생하기엔 조합이 너무 많다. 이런 경우 "A가 성립하면 B가 반드시 성립한다"는 논리적 필연 관계를 짚어야 하며, 완벽한 수학적 증명까지는 필요 없더라도 "이게 틀리는 반례가 있는가"를 스스로 찔러보는 것만으로도 충분한 확신을 얻을 수 있다.

### 경계값과 범위는 감이 아니라 목적을 먼저 문장으로 명시해야 한다
"대충 넉넉하게 잡자"는 접근은 오히려 무관한 변수를 끌어들여 혼란을 키운다. 코드를 짜기 전에 "이 조건이 정확히 무엇을 보장해야 하는가"를 한 문장(또는 부등식)으로 먼저 확정하면 +1/-1 같은 세부 조정은 자동으로 따라온다.
