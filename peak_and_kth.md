# 이진 탐색 원칙의 전이 확인 + K번째 값 찾기 (투 포인터 버전)

`rotated_array_boundary.md`에서 확립한 세 가지 원칙(비교 기준의 안정성, mid 방향 보정, 후보 포함 여부)을 완전히 다른 구조의 문제(산 모양 배열)에 적용해 전이 여부를 확인하고, 이어서 K번째 값 찾기 문제를 투 포인터로 완성한 기록.

---

## 문제 1: 산 모양 배열에서 정점 찾기 (Peak Finding)

**예상 난이도: 실버 3~2**

배열이 어떤 지점까지 증가하다가 그 이후 감소하는 "산 모양"일 때, 정점의 인덱스를 O(log n)에 찾는다.

```python
find_peak([1, 3, 5, 8, 6, 4, 2])   # 3
find_peak([1, 2, 3])               # 2 (오르기만 함)
find_peak([3, 2, 1])               # 0 (내리기만 함)
```

### 회전 배열과의 근본적 차이
회전 배열은 "두 개의 오름차순 구간"이라는 전역적 구조를 가져서, `numbers[left]`나 `numbers[right]`처럼 **멀리 떨어진 고정 지점**과 비교해도 안전했다. 산 모양 배열은 그런 전역 구조가 없고, "오르막인지 내리막인지"는 **국소적 기울기**로만 판단 가능하다. 그래서 비교 대상이 멀리 있는 고정점이 아니라 **바로 옆 이웃**(`mid-1` 또는 `mid+1`)이어야 한다는 것이 핵심 전이 지점이었다.

### 검증된 네 가지 조합 (모두 20,000개 랜덤 케이스로 O(log n) 정답 확인)

| 비교 대상 | 후보 유지 방향 | mid 계산 | 결과 |
|---|---|---|---|
| `mid-1`과 비교, 오르막이면 `left=mid` | `left` | `+1`(올림) 필요 | 정답 |
| `mid-1`과 비교, `+1` 없이 | `left` | 내림 그대로 | 무한루프 (제자리걸음) |
| `mid+1`과 비교, 내리막이면 `right=mid` | `right` | `+1` 불필요 | 정답 |

### 재확인된 원칙: "후보를 유지하며 이동하는 방향"과 "`//`(내림)가 쏠리는 방향(`left`)"이 같으면 `+1` 보정이 필요하고, 다르면 필요 없다
`left=mid`(후보를 `left`쪽에 유지)일 때 내림 그대로 두면, 구간이 2칸 남을 때 `mid`가 항상 `left`와 같아져 갱신이 "제자리걸음"이 되어 무한루프에 빠진다(회전 배열에서는 "자기 자신 비교로 판단이 무의미해지는 것"이었다면, 여기서는 "갱신 자체가 진전이 없어지는 것"이라는 다른 증상으로 나타났으나 원인은 동일). `right=mid`로 후보 유지 방향을 반대로 두면 내림 그대로도 안전하다.

### 최종 정답 코드
```python
def find_peak(numbers):
    left = 0
    right = len(numbers) - 1
    while left < right:
        mid = (left + right) // 2
        if numbers[mid] > numbers[mid + 1]:
            right = mid
        else:
            left = mid + 1
    return left
```

### 메타 결론: 이 유형에서 요구되는 진짜 능력
회전 배열, 산 모양 배열 모두에서 확인된 것은, 이 문제군이 "패턴 하나를 이해하면 바로 적용되는" 유형이 아니라 **"이진 탐색이라는 근원적 틀만 고정이고, 나머지(비교 대상·후보 유지 방향·mid 보정)는 매번 그 문제의 구조에 맞춰 가설을 세우고 실패하면 원인을 진단해 최소 수정하는" 유형**이라는 것. 요구되는 실력은 하나의 정답을 아는 것이 아니라, 이 가설-검증-수정 사이클을 얼마나 빠르고 정확하게 도는가(좋은 반례를 스스로 설계하는 능력, 실패에서 정확한 원인을 뽑아내는 능력)이다.

---

## 문제 2: 정렬된 두 배열에서 K번째로 작은 값 찾기 (투 포인터 버전)

**예상 난이도: 실버 3~2 (이 버전 기준. 이진 탐색으로 O(log(min(n,m)))에 푸는 버전은 골드~플래티넘급으로, 별도 문서에서 다룸)**

```python
find_kth([1, 3, 5], [2, 4, 6], 3)   # 3
find_kth([1, 2], [3, 4, 5], 4)      # 4
find_kth([], [1, 2, 3], 2)          # 2
```

### 시행착오: `if`를 두 번 쓴 것과 `elif`를 쓴 것의 차이
```python
# 실패 버전: if를 두 번
while left + right < len(a) + len(b) - 1:
    if a[left] < b[right]:
        ...
        left = left + 1
    if a[left] > b[right]:   # 여기서 IndexError
        ...
```
`left`를 갱신한 직후, 그 늘어난 `left`로 바로 다음 `if` 조건을 다시 평가하려다 `IndexError`가 발생했다. `elif`였다면 첫 블록이 실행된 순간 두 번째 조건 자체를 평가하지 않으므로 이 문제가 발생하지 않는다.

**핵심 교훈**: "두 조건이 논리적으로 동시에 참일 수 없다"는 것과 "한 블록의 실행이 다음 블록이 참조할 상태를 바꿔버릴 수 있다"는 것은 별개의 문제다. `elif`가 안전한 이유는 논리적 배타성 때문이 아니라, 첫 블록의 부작용(상태 변경) 이후 두 번째 조건이 재평가되지 않기 때문이다. 다만 `elif`는 이 특정 버그를 우회하는 차선책이고, 근본적으로는 반복 조건 자체를 재설계해 애초에 범위 초과 상황이 발생하지 않게 만드는 것이 정답이다.

### 최종 정답 코드 (`merge_sorted` 구조를 그대로 재적용)
```python
def find_kth(a, b, k):
    combined_array = []
    left = 0
    right = 0

    while left < len(a) and right < len(b):
        if a[left] < b[right]:
            combined_array.append(a[left])
            left = left + 1
        else:
            combined_array.append(b[right])
            right = right + 1

    combined_array = combined_array + a[left:] + b[right:]
    return combined_array[k - 1]
```
`len(a)==0`, `len(b)==0`을 미리 확인하는 사전 조건문은 불필요함을 확인 — `while` 조건과 이어지는 슬라이싱이 이미 그 경우들을 자동으로 흡수한다. 3000개 랜덤 케이스로 검증 완료.

### 부수 개념: 빈 리스트 슬라이싱
```python
empty = []
empty[0:]      # []
empty[100:]    # []
```
"범위를 벗어난 슬라이싱이 에러 없이 처리된다"는 원칙(`palindrome_three_problems.md`)은 "빈 리스트에 대한 슬라이싱"까지 포함하는 더 넓은 원칙의 일부다. 슬라이싱은 항상 "그 구간에 실제로 존재하는 원소만 모아서 반환"하며, 존재하는 게 하나도 없으면(범위 초과든 리스트 자체가 비었든) 그냥 빈 리스트를 반환할 뿐, 별도의 두 가지 규칙이 있는 게 아니다.
