# "지나온 흔적 기록하고 대조하기" 패턴 — 3문제 풀이 정리

브론즈~실버급 문제 3개를 연달아 풀며, 겉보기엔 서로 다른 문제들이 사실 같은 뼈대(패턴)를 공유한다는 것과, 그 뼈대의 시간복잡도 한계까지 확인한 과정을 정리한다.

---

## 문제 1: 문자 빈도 짝 찾기 (Character Pair Count)
**예상 난이도: 브론즈 3~2**

문자열 `s`가 주어졌을 때, 정확히 2번 등장하는 문자가 몇 종류인지 세는 함수를 작성한다.
- 대소문자 구분, 공백/특수문자도 하나의 문자로 취급
- 3번 이상 등장하거나 1번만 등장하는 문자는 세지 않음

```python
count_paired_chars("banana")  # b:1, a:3, n:2 → 정확히 2번인 건 n 하나 → 1
```

### 시도했던 방법들과 실패 이유
- **문자 치환**: 처리한 문자를 다른 값으로 바꿔 표시하려 함 → 원본 데이터 자체를 바꾸면 이후 다른 문자의 카운트에 부작용이 생겨 실패.
- **시작 인덱스를 매번 한 칸씩 미루기**: "이미 지나간 부분은 다시 안 본다"는 취지 → 뒤에서 세는 범위 자체가 줄어들어, 3번 이상 등장하는 문자가 2번으로 잘못 집계되는 문제 발생.
- **이전 문제(최장 증가 구간/최대 부분합)의 "상태 유지+매 스텝 갱신" 패턴 재활용 시도**: 그 패턴은 "연속된 구간"을 다루는 문제 전용인데, 이 문제는 문자 등장이 연속적이지 않아 애초에 적용 대상이 아니었음.
- **순열/조합 같은 수학적 경우의 수 계산**: 이 문제는 "존재하는 경우의 수"가 아니라 "실제 데이터를 순회하며 세는" 문제라 공식으로 환원되지 않음.

이 네 가지가 다 막힌 원인은 발상력 부족이 아니라, **"원본을 안 건드리고 처리 이력을 별도로 기록해두는" 방식 자체(빈 리스트 + `append` + `in`)를 그때까지 배운 적이 없었기 때문**이었다. (몇 시간을 고민해도 안 풀릴 때, "발상이 안 떠오르는 것"과 "애초에 안 배운 도구가 필요한 것"을 구분하는 게 중요하다는 교훈을 여기서 얻음.)

### 최종 정답 코드
```python
def count_paired_chars(s):
    checked_chars = []
    total = 0
    for value in s:
        if value not in checked_chars:
            checked_chars.append(value)
            if s.count(value) == 2:
                total = total + 1
    return total
```

### 사용된 핵심 도구
- 빈 리스트 생성 (`checked_chars = []`)
- `.append()` — 리스트 맨 뒤에 값 추가
- `in` / `not in` — 포함 여부 확인 (참/거짓을 반환하는 비교 연산자. `for ... in ...`의 `in`과는 문법적으로 완전히 다른 별개의 용법)
- `.count()` — 특정 값이 몇 번 등장하는지 세기

### 시간복잡도
- `not in checked_chars`: 문자열의 모든 위치(n번)에서 실행되며, `checked_chars` 크기가 최악의 경우(전부 다른 문자) n까지 커짐 → 누적 O(n×d)
- `s.count(value)`: 서로 다른 문자 종류(d)만큼만 호출되지만, `.count()`는 **위치와 무관하게 항상 문자열 전체를 끝까지 훑음(조기 종료 없음)** → O(d×n)
- 두 비용은 곱해지는 게 아니라 더해지는 관계이므로 전체는 **O(n×d)**
- **d(서로 다른 문자 종류 수)가 상수로 제한된다는 보장이 없다면**, 최악의 경우 d=n이 되어 **O(n²)**
- 실측: 전부 다른 문자로 구성한 입력에서, n이 2배가 될 때 실행 시간이 약 4배씩 증가 → O(n²) 패턴과 일치 (O(n³)이라면 8배가 나와야 함)

---

## 문제 2: 공통 문자 개수 (Common Characters)
**예상 난이도: 브론즈 2~1**

두 문자열 `s1`, `s2`가 주어졌을 때, 둘 다에 등장하는 문자의 종류 수를 세는 함수를 작성한다.
- 대소문자 구분, 한 번이라도 둘 다에 등장하면 1종류로만 카운트

```python
count_common_chars("banana", "orange")  # 공통 문자: a, n → 2
```

### 접근과 진행
문제 1에서 배운 도구(빈 리스트, `append`, `in`/`not in`)를 그대로 재활용할 수 있음을 확인하고 바로 적용. 새로 필요한 도구는 없었음 — `in`을 "리스트 안에 있나"뿐 아니라 "다른 문자열 안에 있나"로 응용하는 것이 핵심이었다.

### 최초 버전과 개선
**최초 버전** (append 위치가 조건 안쪽):
```python
def count_common_chars(s1, s2):
    checked_chars = []
    count_chars = 0
    for value in s1:
        if value not in checked_chars:
            if value in s2:
                count_chars = count_chars + 1
                checked_chars.append(value)
    return count_chars
```
이 버전은 정답이지만, `s2`에 없는 문자는 `checked_chars`에 등록되지 않아 `s1`에서 여러 번 나올 때마다 `value in s2` 검사를 매번 다시 하는 비효율이 있었음 (결과는 틀리지 않지만 불필요한 재검사 발생).

**개선 버전** (append를 조건 안쪽에서 바깥쪽, `not in` 체크 바로 다음으로 이동):
```python
def count_common_chars(s1, s2):
    checked_chars = []
    count_chars = 0
    for value in s1:
        if value not in checked_chars:
            checked_chars.append(value)
            if value in s2:
                count_chars = count_chars + 1
    return count_chars
```
"한 번이라도 본 문자는 결과(`s2`에 있든 없든)와 상관없이 즉시 명단에 등록한다"로 바뀌면서, 같은 문자를 반복해서 재검사하는 낭비가 사라졌다. (앞선 최대 부분합 문제에서 "비교를 먼저 하고 리셋은 나중에" 순서를 바꿔 변수를 줄였던 것과 같은 종류의 개선 — 로직 안에서 순서/위치만 바꿔도 불필요한 재작업이 사라지는 사례.)

### 사용된 핵심 도구
문제 1과 동일 (`.count()`는 사용하지 않음). `in`이 **문자열**에도 그대로 적용된다는 점을 확인.

### 시간복잡도
- `value not in checked_chars`: 문제 1과 동일한 방식으로 누적 O(n1×d1)
- `value in s2`: **조기 종료되는 연산.** 찾는 문자가 `s2`의 앞쪽에 있으면 금방 끝나지만, `s2`의 뒤쪽에 있거나 아예 없으면 `s2` 전체(길이 n2)를 거의 다 훑어야 함 → 최악의 경우 O(n2)
- 두 비용의 합 → 최악의 경우 **O(n²)** (n1, n2가 비슷한 크기일 때)
- 문제 1과 원인이 다름: 문제 1은 "**종류 수(d)**가 커서" O(n²), 문제 2는 "**매치 위치**가 뒤쪽이거나 없어서" O(n²) — 겉보기엔 둘 다 O(n²)지만 그 원인이 서로 다르다는 점을 확인.

---

## 문제 3: 두 수의 합 (Two Sum / has_pair_sum)
**예상 난이도: 실버 5~4**

정수 리스트 `numbers`와 목표값 `target`이 주어졌을 때, 합이 정확히 `target`이 되는 서로 다른 인덱스의 두 원소 조합이 존재하는지 판별한다.

```python
has_pair_sum([2, 7, 11, 15], 9)  # 2+7=9 → True
has_pair_sum([3, 3], 6)          # 서로 다른 인덱스의 3+3=6 → True
```

### 시도 1: 브루트포스 (즉시 떠오름)
```python
def has_pair_sum(numbers, target):
    for i in range(len(numbers)):
        for j in range(len(numbers)):
            if i != j and numbers[i] + numbers[j] == target:
                return True
    return False
```
정답이지만 O(n²)이며, 이중 반복문 구조 자체가 앞선 두 문제의 "명단 기록" 패턴과 무관.

### 시도 2: 원본 리스트를 직접 대조 (정답이지만 의도된 패턴은 아니었음)
```python
def has_pair_sum(numbers, target):
    for value in numbers:
        pair_numbers = target - value
        if pair_numbers in numbers and pair_numbers != value:
            return True
        elif numbers.count(pair_numbers) >= 2:
            return True
    return False
```
정답이고 반복문은 한 겹이지만, **앞선 두 문제와 달리 별도의 "명단(빈 리스트)"을 안 만들고 원본 `numbers`를 그때그때 직접 검사**하는 방식이었다. 이 때문에 값이 같은 경우(`[3,3]`)를 처리하려고 `!= value`와 `.count() >= 2`라는 별도의 예외 처리를 추가해야 했다.

### 의도된(더 정제된) 정답: "지나온 것만 기록하는" 패턴 적용
```python
def has_pair_sum(numbers, target):
    seen = []
    for value in numbers:
        if target - value in seen:
            return True
        seen.append(value)
    return False
```
**핵심 차이:** `seen`에는 **현재 값보다 앞서 지나온 원소만** 들어있다. 그래서 `[3,3]` 같은 케이스도 예외 처리 없이 자연스럽게 맞는다 — 두 번째 `3`을 볼 시점에 `seen`엔 이미 첫 번째 `3`이 들어있으므로, "같은 인덱스를 두 번 쓰는" 문제가 구조적으로 발생할 수 없다.

이 발상(찾아 나서지 않고, 지나온 흔적을 기록해뒀다가 새 값이 나올 때 거꾸로 대조한다)은 보통의 직관("하나 뽑고 그 짝을 찾아 나서자")과 반대 방향이라 스스로 떠올리기 어려웠던 부분. 다만 문제 1, 2에서 이미 손으로 짜본 "명단 기록" 뼈대와 구조가 동일하다는 걸 확인한 뒤에는 자연스럽게 이해됨.

### 사용된 핵심 도구
문제 1, 2와 동일 (`in`, `not in`, 빈 리스트, `append`). 추가로 `return`이 호출 즉시 함수 실행을 멈춘다는 것을 확인 (반복문 안에 있어도 예외 없이 적용됨).

### 시간복잡도
- `target - value in seen`: 문제 2의 `value in s2`와 같은 이유로 조기 종료되지만, 최악의 경우(정답이 없거나 뒤쪽에 있는 경우) `seen`을 거의 다 훑어야 함 → O(n)
- 이게 매 반복(n번)마다 일어남 → 전체 **O(n²)**
- 실측: 정답이 존재하지 않는 최악의 케이스에서, n이 2배가 될 때 시간이 약 4배씩 증가 → O(n²) 확인
- `seen`이 리스트인 이상 이 한계를 못 벗어남. **`seen`을 셋(set)이나 딕셔너리로만 바꾸면, 코드 구조는 거의 그대로 유지한 채 O(n)까지 줄어든다** — 아직 안 배운 도구지만, 이 구조가 그 도구를 만났을 때 바로 확장될 수 있는 정확한 형태라는 점이 중요.

---

## 세 문제의 공통 골격: "지나온 흔적을 기록하고, 현재와 대조하는 패턴"

세 문제 모두 다음과 같은 동일한 뼈대를 공유한다.

```python
기록 = []  # 혹은 빈 리스트
for 원소 in 대상:
    if 원소(혹은 관련된 값) not in 기록:
        (필요한 처리)
        기록.append(원소)
```

**패턴의 본질:** 무언가를 "찾으러 나가는" 대신, **지나가는 길에 흔적을 남겨두고, 새로운 것을 마주칠 때마다 그 흔적과 대조한다.** 이는 "목표를 향해 탐색해 나가는" 보통의 직관과 반대 방향의 사고다 — "미래의 답을 찾으러 가기"가 아니라 "과거의 기록을 현재와 비교하기".

### 문제별 변형 비교

| | 무엇을 기록하는가 | 무엇과 대조하는가 | O(n²)의 원인 |
|---|---|---|---|
| 1. 문자 빈도 짝 찾기 | 이미 처리한 문자 | 자기 자신 (`not in`) + `.count()`로 전체 재확인 | 서로 다른 문자 종류 수(d)가 n에 가까워질 때 |
| 2. 공통 문자 개수 | 이미 처리한 `s1`의 문자 | 다른 문자열 `s2` 안에 있는지 (`in`) | `s2` 안에서 매치 위치가 뒤쪽이거나 없을 때 |
| 3. 두 수의 합 | 지나온 숫자들 | 지금 필요한 짝(`target - value`)이 기록에 있는지 | `seen` 안에서 매치 위치가 뒤쪽이거나 없을 때 |

### 왜 셋 다 O(n²)를 벗어나지 못하는가
지금까지 배운 도구(리스트 + `in`/`not in`/`.count()`)로 "이 값이 안에 있는지"를 확인하려면, 결국 리스트를 하나씩 훑어보는 것 말고는 방법이 없다. 이 훑는 동작이 반복문 안에서 매번 일어나기 때문에 최악의 경우 항상 O(n²)로 수렴한다. 이 한계를 실제로 벗어나려면 "훑지 않고 즉시 포함 여부를 확인할 수 있는" 자료구조(딕셔너리/셋)가 필요하며, 이는 다음 학습 단계의 과제로 남겨둔다.

---

## 부수적으로 정리된 개념들

- **`for value in s`와 `value in s`는 같은 단어 `in`을 쓰지만 문법적으로 완전히 다르다.** 전자는 반복문(순회) 문법의 일부이고, 후자는 참/거짓 값을 돌려주는 독립적인 비교 연산자다.
- **`if`는 참/거짓 값을 만드는 도구가 아니라, 이미 계산된 참/거짓 값을 보고 실행 흐름을 가르는 도구다.** `조건식` 자체는 `if` 없이도 독립적으로 계산되어 값을 만들어낸다.
- **`in`과 `.count()`는 둘 다 "포함 여부/개수를 확인"하지만 동작 방식이 다르다.** `in`은 찾는 즉시 멈추는 조기 종료가 있고, `.count()`는 정확한 개수를 세야 하므로 항상 끝까지 훑는다. 이 차이가 각 문제에서 O(n²)이 되는 서로 다른 원인을 만든다.
- **반복문이 한 겹이라고 해서 시간복잡도가 항상 O(n)인 것은 아니다.** 반복문 안에서 그 자체로 O(n)이 걸리는 연산(`in`, `.count()` 등)을 호출하면, 그것이 여러 번 더해지거나 곱해지는 구조에 따라 O(n²) 이상이 될 수 있다.
- **변수 이름은 "무엇을 하는 동작"보다 "왜 존재하는지/무엇이 들어있는지"를 드러낼 때 더 읽기 좋다.** (`save_chars`보다 `checked_chars`/`seen`이 더 명확한 이유)

## 학습 방법론에 대한 메모: 반직관적 발상은 어떻게 다루는가
`seen` 패턴처럼 직관과 반대 방향에 있는 발상은, 무한정 혼자 고민해서 재발명해야 하는 것이 아니라 아래 방식으로 다루는 게 현실적이다.
1. 타임박싱: 적당한 시간(예: 30분~1시간) 진지하게 혼자 부딪혀본다.
2. 시간이 다 되면 답을 보되, 답을 본 뒤 "왜 이 방식이 되는지", "내 방식과 뭐가 다른지"를 반드시 파고든다 — 이 과정이 실제 학습이 일어나는 지점이다.
3. 이런 패턴들은 개수가 무한하지 않다. 하나씩 손에 익히며 "패턴 목록"을 쌓아가는 것이 알고리즘 학습의 실체에 가깝다.
