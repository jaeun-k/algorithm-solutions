# H-Index: 정렬로 전역 조건을 지역 조건으로 압축하기

짧은 코드 한 줄 안에 여러 통찰이 겹쳐 있어서, 이해와 체화 사이의 간극을 가장 크게 느꼈던 문제. 세 단계 풀이의 발전 과정과, 이 과정에서 정리하게 된 학습 방식에 대한 원칙을 함께 기록한다.

---

## 문제

논문별 인용 횟수 배열이 주어질 때, "h번 이상 인용된 논문이 h편 이상이고 나머지는 h번 이하 인용된" h의 최댓값(H-Index)을 구한다.

```python
solution([3, 0, 6, 1, 5])  # 3
```

---

## 풀이 1: 브루트포스 (처음 푼 풀이)

```python
def solution(citations):
    h = 0
    count = 0
    for i in range(10000):
        for j in citations:
            if j >= i:
                count += 1
        if count >= i and i >= h:
            h = i
        count = 0
    return h
```
가능한 h 후보(0~10000)를 전부 대입하며, 그 h를 만족하는(인용 수가 h 이상인) 논문 개수를 매번 처음부터 다시 세는 방식. 정답은 나오지만 `10000 × len(citations)`만큼 계산해야 해서 비효율적이다.

---

## 풀이 2: 정렬 + 이중 루프 (직접 최적화 시도)

```python
def solution(citations):
    c = sorted(citations, reverse=True)
    
    for i in range(c[0], -1, -1):
        count = 0
        for j in range(i):
            if len(c) < i or c[j] < i:
                break
            else:
                count += 1
        if count == i:
            return i
```
정렬은 도입했지만, 여전히 "각 h 후보마다 상위 i편을 매번 다시 검사"하는 이중 루프 구조. `len(c) < i` 체크가 처음엔 불필요해 보였으나, `i`가 인용 횟수(최대 10000) 기준으로 돌기 때문에 논문 개수(`len(c)`)보다 커질 수 있어 실제로 `c[j]` 인덱스 초과를 막아주는 필수 조건이었다.

---

## 풀이 3: 정석 최적화 (한 번의 순회)

```python
def solution(citations):
    c = sorted(citations, reverse=True)
    for i, cite in enumerate(c):
        if cite < i + 1:
            return i
    return len(c)
```

### 핵심 통찰: 정렬이 "세는 것"을 없애준다
H-Index 정의의 "h번 이상 인용된 논문이 h편 있다"에서, 뒤의 "h편"은 개수를 세야 알 수 있는 값이다. 그런데 내림차순 정렬을 해두면, "지금까지 몇 편을 봤는가"가 곧 배열의 위치(인덱스+1)와 정확히 같아진다. 그리고 i번째(0-indexed) 원소가 자기 순번(`i+1`) 이상이면, 정렬 순서상 그 앞의 원소들은 이미 더 크거나 같은 값들이므로 자동으로 조건을 만족한다는 것이 보장된다. 즉 매번 전체를 세는 대신, 지금 보고 있는 그 지점 하나만 확인하면 충분하다 — 정렬이 "전역 조건(개수를 센다)"을 "지역 조건(이 지점 하나만 본다)"으로 압축해준 것이다.

### 왜 `i`가 아니라 `i+1`인가
`i`는 0부터 시작하는 인덱스지만, "지금까지 몇 편을 봤는가"는 1부터 세는 개수다. `i=0`(첫 번째 원소)까지 왔다면 실제로는 1편을 본 것이므로, 비교 기준은 `i`가 아니라 `i+1`이어야 한다. `i`로 비교하면 기준이 하나 느슨해져서, 예를 들어 `[1, 1]` 같은 케이스에서 실제로는 h=1이 정답인데 h=2라는 오답을 낸다.

### 왜 `<`이지 `<=`가 아닌가
정의 자체가 "h번 **이상** 인용"이므로, 정확히 자기 순번만큼 인용된 논문도 조건을 만족(성공)해야 한다. `cite <= i+1`로 쓰면 "정확히 순번만큼 인용된 경우"까지 실패로 오판하게 되어, 예를 들어 `[3,3,3,2,1]`에서 정답 3 대신 2가 나오는 오류가 생긴다. `<`는 "인용 수가 순번보다 작다(=순번에도 못 미친다)"는, 정확히 "정의를 위반하는 경우"만을 실패로 잡아낸다.

### 마지막 `return len(c)`가 필요한 이유
`for` 루프가 한 번도 조건에 걸리지 않고 끝까지 돈다는 건, 모든 논문이 자기 순번 이상으로 인용됐다는 뜻이다. 이 경우 h는 이론상 더 커질 수 있어 보이지만, H-Index는 논문 편수(`len(c)`)를 절대 넘을 수 없으므로(그 이상의 h를 만족하려면 그만큼의 논문 편수 자체가 있어야 하는데 없음), 이 경우의 정답은 `len(c)`가 된다. 값이 모두 같은 경우(`[6,6,6,6]`)뿐 아니라 값이 전부 다르더라도 순번 조건만 만족하면(`[10,8,5,4]`) 같은 이유로 `len(c)`가 반환된다. 반대로 값이 크더라도 중간에 순번 조건이 깨지는 경우(`[10,1,1,1]`)는 이 분기까지 가지 않고 훨씬 이른 지점에서 반환된다.

---

## 이 문제를 통해 정리하게 된 학습 원칙

### 이해했다는 것과 손에 붙었다는 것은 다르다
로직을 완전히 설명할 수 있고 손으로 트레이싱까지 다 해봤어도, 그 로직 전체가 하나의 그림으로 머릿속에서 자유롭게 굴러가는 느낌(다른 사람에게 바로 증명해 보일 수 있을 정도)과는 별개일 수 있다. 짧은 코드 한 줄에 여러 요소(정렬의 필요성, 인덱스와 개수의 대응, 부등호의 방향, 종료 조건의 의미)가 동시에 얽혀 있을 때 이 간극이 특히 크게 느껴진다.

### 새로 배운 로직에 의문이 생기면, 조건을 직접 바꿔보고 반례를 만들어보는 것이 가장 효과적이다
`<`를 `<=`로 바꿔서 직접 반례(`[3,3,3,2,1]`에서 오답이 남)를 만들어본 순간이, 설명을 듣는 것보다 실제 이해에 훨씬 크게 기여했다. 말로 된 설명보다, 코드를 스스로 비틀어서 무엇이 깨지는지 확인하는 과정이 체화에 더 가깝다.

### 짧고 완결적인 코드는 대체로 문제의 정의 자체와 한 몸으로 설계되어 있다
H-Index처럼 여러 예외 케이스가 자동으로 처리되는 짧은 코드는, 우연히 여러 트릭이 겹친 게 아니라 하나의 불변식(정렬된 상태에서 "지금까지 본 것이 전부 순번 이상"이라는 조건)에서 자연스럽게 파생된 결과인 경우가 많다. 이런 코드가 낯설게 느껴지는 것은 아직 이 패턴군(정렬로 전역 조건을 지역 조건으로 압축하는 유형)의 다른 문제를 만나보지 못했기 때문일 수 있고, 비슷한 문제를 몇 개 더 겪으면 뒤늦게 연결점이 생기는 경우가 많다.

### 완벽한 이해를 목표로 삼으면 비효율이 배로 커진다
어떤 개념을 70~80% 정도 잡았을 때, 남은 20~30%를 마저 채우는 데 드는 시간은 앞의 70~80%를 잡는 데 든 시간만큼, 혹은 그 이상 걸릴 수 있다. 시간이 제한적인 상황에서는, 손으로 트레이싱하고 반례까지 만들어보는 등 할 수 있는 시도를 다 해봤는데도 완전히 체화된 느낌이 안 든다면, 그 상태로 일단 넘어가고 다음에 비슷한 유형을 만났을 때 이해도가 자연스럽게 올라가길 기다리는 것이 더 합리적인 선택이다.

### 난이도가 오르면 학습 곡선의 형태 자체가 바뀐다
쉬운 난이도에서는 문제 하나하나가 바로 다음 문제에 재사용되는 직선적 성장이 느껴지지만, 난이도가 오르면 각 문제가 그 문제만의 고유한 관찰을 요구하는 경우가 많아져 당장은 성장이 잘 느껴지지 않는 계단식 구간에 들어서게 된다. 이 구간에서는 하루에 소화할 수 있는 문제 수 자체가 줄어드는 것이 자연스러운 현상이며, 이전 난이도의 속도를 기준으로 스스로를 평가하면 실제보다 정체된 것처럼 느껴질 수 있다.
