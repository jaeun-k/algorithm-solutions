# 최대 부분 배열 합 회상과 예외처리

이전에 스스로 풀었던 최대 부분합(Max Subarray Sum) 문제를, md 문서를 전혀 보지 않고 다시 풀어보며(회상) 겪은 과정을 정리한다. 코드를 그대로 복원하려던 시도, 거기서 발견된 새로운 실수, 그리고 그 과정에서 오히려 예전보다 더 나은 풀이에 도달한 경험까지 담는다.

---

## 문제 재확인

정수 리스트가 주어졌을 때, 연속된 부분 배열 중 합이 최대인 값을 구한다. (자세한 문제 정의와 O(n²)→O(n) 최적화 과정 전체는 `max_subarray_sum_notes.md`에 기록되어 있음)

```python
max_subarray_sum([1,2,6,1,2,-20,-5,2,1,-1,-2,2,20,-20])  # → 22
```

---

## 회상 시도 1: "음수면 0으로 리셋" 버전 재현

md를 안 보고 예전에 스스로 떠올렸던 핵심 아이디어("연속합이 음수가 되면 0으로 초기화하고 새출발")를 그대로 재현했다.

```python
def max_subarray_sum(numbers):
    current_sum = numbers[0]
    max_sum = max(numbers)
    for n in numbers[1:]:
        if max(numbers) >= 0 and current_sum < 0:
            current_sum = 0
        current_sum = current_sum + n
        if current_sum > max_sum:
            max_sum = current_sum
    return max_sum
```

로직 자체는 정확히 재현했고 정답도 맞았지만(브루트포스 대비 5000개 랜덤 케이스 전부 일치), **예전에는 없던 새로운 비효율을 스스로 만들어냈다.**

### 발견된 문제: `max(numbers)`를 반복문 안에서 매번 재계산
```python
if max(numbers) >= 0 and current_sum < 0:
```
이 줄이 반복문의 매 스텝마다 실행되면서, `max(numbers)`(그 자체로 O(n)인 연산)가 n번 호출되어 전체가 **O(n²)로 퇴화**했다. 원본 버전은 `max_sum = max(numbers)`를 반복문 **밖에서 단 한 번만** 계산해서 그 결과를 재사용했었다.

실측: n이 2배씩 커질 때 "매번 재계산" 버전은 시간이 약 4배씩 증가(O(n²)), "한 번만 계산" 버전은 거의 변화 없음(O(n)).

### 수정
```python
if max_sum >= 0 and current_sum < 0:   # max(numbers) -> max_sum
```
`max_sum`은 `max(numbers)`로 시작한 뒤 자기보다 큰 값을 만났을 때만 갱신되므로, **그 부호(양수/음수)가 반복문 내내 `max(numbers)`의 부호와 항상 일치**한다. 따라서 매번 재계산할 필요 없이 이미 계산해둔 `max_sum`을 그대로 재사용해도 결과가 같다. 수정 후 O(n)으로 복귀함을 실측으로 확인.

---

## 회상 시도 2: 브루트포스 버전 재현 — 지저분해진 사례

브루트포스(O(n²)) 버전도 다시 풀어보았는데, 원본과 비교했을 때 접근 방식 자체가 달라져 있었고, 그 결과 코드가 지저분해졌으며 스스로는 정리하지 못했다.

### 원본 버전 (깔끔함)
```python
def max_subarray_sum(numbers):
    max_sum = max(numbers)
    subarray_sum = 0
    for i in range(len(numbers)):
        for n in range(len(numbers) - i):
            subarray_sum = subarray_sum + numbers[i + n]
            if max_sum < subarray_sum:
                max_sum = subarray_sum
        subarray_sum = 0
    return max_sum
```

### 회상 버전 (지저분함 — if문 중복)
```python
def max_subarray_sum(numbers):
    max_sum = max(numbers)
    for i in range(len(numbers)):
        current_sum = numbers[i]
        for j in range(len(numbers) - i - 1):
            current_sum = current_sum + numbers[j + i + 1]
            if current_sum > max_sum:
                max_sum = current_sum
        if current_sum > max_sum:      # 반복문 밖에서 같은 비교를 한 번 더 해야 했음
            max_sum = current_sum
    return max_sum
```

### 원인 분석: 왜 지저분해졌고, 왜 스스로 못 고쳤는가

두 버전의 근본적인 차이는 **"시작 원소를 어디서, 어떻게 처리하는가"**였다.

- **원본**: `subarray_sum = 0`으로 시작 → 안쪽 반복문의 첫 스텝(`n=0`)에서 `numbers[i]` 자기 자신이 더해지는 것까지 반복문 **안에서** 자연스럽게 처리됨. 그래서 시작 원소도 반복문 안의 비교에서 자동으로 커버된다.
- **회상 버전**: `current_sum = numbers[i]`로 시작값을 반복문 **밖에서** 미리 앵커처럼 박아둠 → 안쪽 반복문은 그 이후 원소들만 처리하므로, "시작 원소 하나짜리 구간"이 반복문 안에서 `max_sum`과 비교될 기회가 전혀 없음 → 반복문이 끝난 뒤 별도의 if문으로 그 기회를 억지로 만들어줘야 했음.

**왜 이런 접근으로 회귀했는가:** "시작점을 먼저 고정하고 거기서부터 확장해나간다"는 사고가 "0에서 시작해서 모든 원소를 반복문 안에서 균일하게 처리한다"는 사고보다 사람에게 훨씬 직관적이기 때문. 처음 풀 때는 어쩌다 후자(덜 직관적이지만 경계 조건이 자동으로 처리되는 구조)에 도달했었는데, 시간이 지나 회상할 때는 뇌가 더 자연스러운 기본 사고방식(전자)으로 되돌아간 것으로 보인다.

**왜 스스로 못 고쳤는가:** 중복된 if문은 증상일 뿐, 원인은 "시작값을 반복문 밖에 앵커로 박아둔 구조" 자체였다. 그 구조를 유지한 채 증상(중복 if문)만 없애려 하면 해결되지 않는다 — 구조 자체(시작 원소도 반복문 안에서 처리되도록 재설계)를 바꿔야만 풀리는 문제였다.

---

## 회상 시도 3: 최적화 버전 — 오히려 예전보다 더 나은 형태에 도달

Kadane 방식(`current_sum = max(current_sum + n, n)`)을 다시 시도하는 과정에서, 예전 코드를 그대로 복원하려 한 게 아니라 "0으로 시작해도 정말 괜찮은가"를 스스로 다시 검토하며 아래에 도달했다.

```python
def max_subarray_sum(numbers):
    max_sum = max(numbers)
    current_sum = 0
    for n in numbers:
        current_sum = max(current_sum + n, n)
        if current_sum > max_sum:
            max_sum = current_sum
    return max_sum
```

### 왜 `current_sum = 0`으로 시작해도 안전한가
첫 반복(`n = numbers[0]`)에서 `current_sum + n`은 `0 + numbers[0]`이므로 `n` 자신과 같은 값이 된다. 즉 `max(n, n)`이 되어, 초기값이 무엇이었든 결과가 자동으로 `numbers[0]`으로 맞춰진다. 예전에 겪었던 버그(리스트 기반 버전에서 `subarray_sum`이 실제 원소 없이도 `0`이라는 값으로 `max_sum`과 직접 비교되어 생긴 문제)와 달리, 여기서는 `0`이 `max_sum`과 직접 비교되는 일이 없고 항상 첫 원소로 즉시 대체된 뒤에만 비교가 일어난다.

이 버전은 별도의 가드(`max_sum >= 0` 같은 것)도, 사후 if문도 필요 없이 리스트 전체를 균일하게 처리하는 가장 깔끔한 형태였다.

---

## 예외 처리 원칙 정리

오늘 겪은 것들을 통해 예외 처리(경계 조건)를 다루는 방식이 두 층위로 나뉜다는 것을 확인했다.

| 층위 | 내용 | 예시 |
|---|---|---|
| ① if문 위치/순서 조정 (국소적 패치) | 비교를 리셋보다 먼저 하기, append 위치 조정 등 | 최장 증가 구간, 공통 문자 개수 |
| ② 시작값(초기화) 설계 (구조적 해결) | 시작값과, 그 시작값을 비교할 대상이 서로 같은 기준으로 초기화되었는지 확인 | 최대 부분합 회상 과정 |

①만으로 해결이 안 될 때(중복 if문을 위치만 옮겨서는 안 풀렸던 경우), ②로 거슬러 올라가야 한다. 앞으로 지저분한 예외 처리를 만나면 "이 if문의 위치만 바꿔도 되는가 → 안 된다면, 시작값과 비교 대상이 같은 기준으로 초기화됐는가"를 순서대로 점검하기로 함.

**핵심 원칙(일반화):** "0부터 시작해야 한다"가 원칙이 아니라, **"모든 후보값(특히 시작점)이 최종 비교 대상과 언젠가 한 번은 비교될 기회를 반드시 가져야 한다"**가 원칙이다. 그 비교가 반복문 안에서 자연스럽게 일어나든, 초기화 자체가 그 비교를 대신하든 상관없다.

---

## 부수적으로 정리된 개념들

- **함수 호출은 `()`, 인덱싱은 `[]`.** `함수이름[인자]`처럼 쓰면 파이썬이 이를 인덱싱 시도로 해석해 에러가 난다.
- **`for n in 정수:`는 에러가 난다** (`'int' object is not iterable`). 반면 원소가 0개인 빈 리스트나 `range(0)`을 순회하는 것은 에러 없이 반복문 몸체가 그냥 0번 실행되고 넘어간다. 둘은 다른 상황이다.
- **`range()`가 만드는 것은 리스트가 아니라 별도의 `range` 자료형이다.** 리스트처럼 `for`로 순회는 가능하지만, 값을 미리 다 만들어 저장해두는 리스트와 달리 "시작/끝/간격"만 기억해뒀다가 필요할 때 계산해서 내놓는 방식이라 메모리 효율이 좋다. `list(range(...))`로 실제 리스트로 변환 가능.
- **`a = a + n`처럼 자기 자신을 참조하는 대입문이 헷갈리는 이유**는, `=`을 수학의 등식(같다)으로 읽으려는 습관과, 프로그래밍에서 `=`이 실제로는 "오른쪽을 계산해서 왼쪽에 다시 저장하라"는 대입 명령이라는 것 사이의 충돌 때문이다. 이 누적 패턴(읽고 → 갱신하고 → 다시 저장)이 최대 부분합, 빈도수 세기, 최댓값 추적 등 오늘 푼 모든 문제의 공통 뼈대였다.

---

## 메타 결론: 회상은 "코드 복원"이 아니라 "재유도(re-derivation)"

오늘 겪은 두 종류의 회상을 비교하면:
- 예전 코드를 최대한 그대로 재현하려던 시도(시도 1)는, 정확히 재현은 했지만 오히려 새로운 실수(`max()` 반복 호출)를 만들어냈다.
- 예전 걸 그대로 복사하려 하지 않고 "왜 이렇게 해도 되는가"를 스스로 다시 사고한 시도(시도 3)는, 결과적으로 오늘 나온 것 중 가장 깔끔한 버전에 도달했다.

**결론:** 회상 훈련의 목적은 "예전 코드를 토씨 하나 틀리지 않고 복원하는 것"이 아니라, **"이 문제를 지금 다시 마주치면 나는 어떻게 풀 것인가"를 매번 새로 사고하는 것**이다. 코드는 그 사고 과정의 결과물일 뿐이며, 중요한 것은 그 코드에 도달하는 사고 과정을 스스로 다시 거치는 것이다. 이 과정에서 예전보다 못한 실수를 하더라도(시도 1의 `max()` 반복 호출처럼) 그 실수를 진단하는 과정 자체가 학습이 되고, 때로는 예전보다 더 나은 풀이(시도 3)에 도달하기도 한다.
