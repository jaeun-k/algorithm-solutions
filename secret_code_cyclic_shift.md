# 순환하는 문자 이동 시뮬레이션 (둘만의 암호)

문자열의 각 알파벳을 정해진 칸수만큼 뒤로 이동시키되, 특정 알파벳은 건너뛰고 z를 넘어가면 다시 a로 순환하는 문제를 풀며 겪은 세 번의 실패와 최종 해법, 그리고 그 과정에서 정리된 개념들을 기록한다.

---

## 문제: 알파벳을 index칸 뒤로, 단 skip 문자는 건너뛰고 z는 순환

문자열 `s`의 각 알파벳을 `index`칸만큼 뒤로 이동시킨다. 이동하는 도중 `skip`에 포함된 알파벳을 지나치면 그 알파벳은 이동 칸수에 포함시키지 않고 계속 지나친다. `z`를 넘어가면 다시 `a`로 돌아간다.

```
s = "aukks", skip = "wbqd", index = 5
→ "happy"
```

`'a'`에서 5칸 뒤인 `'f'`가 정답처럼 보이지만, `b~f` 구간에서 `'b'`, `'d'`가 skip 문자라 카운트되지 않으므로, 유효한 칸이 5개가 될 때까지 `c, e, f, g, h`로 계속 나아가 최종적으로 `'h'`가 된다.

---

## 시행착오 1: 정해진 범위 안의 skip 개수만 세고 나중에 한 번에 보정

```python
def solution(phone_number):  # (실제로는 solution(s, skip, index))
    for i in s:
        count = 0
        for j in skip_list:
            if ord(i) < j <= ord(i) + index:
                count += 1
        total = ord(i) + index + count
        ...
```

`ord(i)`부터 `ord(i) + index`까지의 원래 범위 안에 있는 skip 문자 개수만 세고, 그 개수만큼 한 번에 더 이동시키는 방식이다. 문제는 두 가지였다.

1. skip 개수만큼 **더 이동한 새로운 구간**에 또 다른 skip 문자가 있는지는 전혀 검사하지 않는다.
2. `z`를 넘어가는 순환 상황을 단순 정수 비교로 처리하고 있어 순환 구간의 skip 판별이 깨진다.

---

## 시행착오 2: 포인터로 한 칸씩 이동하지만 순환 시 위치가 고정됨

```python
while left <= index:
    if ord(i) + left > ord("z"):
        current = ord("a")
    else:
        current = ord(i) + left
    if current in skip_list:
        count += 1
    left += 1
```

한 칸씩 이동하는 포인터(`current`)를 도입했지만, `z`를 넘는 순간 `current`를 무조건 `ord("a")`로 고정해버려서 그 이후 몇 칸을 더 가야 하는지가 반영되지 않았다. 게다가 `count`(skip 개수)는 별도로 누적만 하다가 마지막에 `current + count`로 한 번에 더하는 구조라, 시행착오 1과 같은 문제(추가로 이동한 구간의 skip 미검사)가 그대로 남아 있었다.

---

## 시행착오 3: 한 칸씩 정확히 순환시키지만 skip 보정은 여전히 나중에 한 번에

```python
while a <= index:
    if current + left > ord("z"):
        current = ord("a")
    else:
        current += left
    if current in skip_list:
        count += 1
    a += 1
if current + count > ord("z"):
    answer += chr(ord("a") + current + count - ord("z") - 1)
else:
    answer += chr(current + count)
```

순환 자체는 `current`를 계속 갱신하는 방식으로 바로잡았지만, 여전히 "index칸을 raw하게 이동하며 만난 skip 개수(`count`)를 마지막에 한 번에 더한다"는 구조는 그대로였다. 반례로 확인됐다:

```
s = "a", skip = "bc", index = 1
```
`'a'`에서 1칸 이동한 `'b'`가 skip이라 `count = 1`이 되고, 최종적으로 `current(b) + count(1) = 'c'`를 답으로 내놓는다. 그런데 `'c'` 역시 skip 문자다. skip 문자가 연속으로 이어질 때, "건너뛴 개수만큼 나중에 한 번에 보정"하는 방식은 그 보정으로 새로 도달한 자리를 다시 검증하지 않기 때문에 실패한다. 실제로 이 코드는 랜덤 테스트 2,000건 중 1,455건에서 오답을 냈다.

문제 예시(`aukks/wbqd/5`)가 이 코드로도 우연히 맞았던 이유는, `count`만큼 추가로 이동한 구간(`g, h`)에 마침 skip 문자가 없었기 때문일 뿐이었다. **테스트케이스 하나를 통과했다는 것이 로직이 맞다는 증거가 되지 않는다**는 것을 확인한 사례다.

---

## 최종 정답: 한 칸 이동할 때마다 즉시 유효성을 확인

```python
def solution(s, skip, index):
    skip_list = [ord(i) for i in skip]
    answer = ""
    for i in s:
        count = 1
        current = ord(i)
        while count <= index:
            current = ord("a") if current + 1 > ord("z") else current + 1
            if current not in skip_list:
                count += 1
        answer += chr(current)
    return answer
```

핵심 변화는, skip 개수를 미리 세어 나중에 한 번에 보정하는 대신 **한 칸 이동할 때마다 그 자리가 유효한지 즉시 판정하며 목표 칸수(`index`)를 채워나가는 것**이다. 이렇게 하면 skip 문자가 몇 개든, 연속으로 나오든 상관없이 루프가 알아서 계속 지나치면서 정확한 위치에서 멈춘다. 랜덤 테스트 5,000건 전부 통과로 검증했다.

이전 시행착오에서 쓰던 `left`(반복 횟수만 세고 실제 이동 거리로는 쓰이지 않던 변수)는 최종 버전에서 완전히 불필요해져 제거했다. 매번 1칸씩만 이동하면 충분했기 때문이다.

---

## 더 널리 쓰이는 정석 풀이: skip을 걸러낸 alphabet 리스트 + 나머지 연산

```python
def solution(s, skip, index):
    alpha = [chr(i) for i in range(ord('a'), ord('z') + 1) if chr(i) not in skip]
    answer = ''
    for x in s:
        answer += alpha[(alpha.index(x) + index) % len(alpha)]
    return answer
```

skip 문자를 아예 처음부터 후보군에서 제거한 알파벳 리스트를 만들어두면, 그 안에서는 skip 여부를 따로 확인할 필요가 없어진다. `alpha.index(x)`로 현재 문자의 위치를 찾고 `index`를 더한 뒤, `% len(alpha)`로 리스트 범위를 넘어가는 경우를 순환시켜준다. `%`가 없으면 `index`가 리스트 끝을 넘는 경우 `IndexError`가 발생한다.

---

## 핵심 개념 정리

### `ord()`와 `chr()`, 그리고 반환 타입

`ord(문자)`는 문자를 아스키/유니코드 코드값(정수)으로 바꾸고, `chr(정수)`는 그 반대다. `int("5")`(문자열 숫자를 정수값으로 변환)와 `ord('5')`(문자의 코드값을 구함)는 이름이 비슷해 보여도 완전히 다른 동작이라는 점을 구분해야 한다.

### 정수와 문자열은 자동으로 형변환되어 비교되지 않는다

```python
45 < 'a' < 60   # TypeError
45 < ord('a') < 60   # 정상 동작 (명시적으로 ord()로 변환해야 함)
```
문자열끼리는 사전순으로 비교가 되지만(`'a' < 'b'`), 정수와 문자열은 타입이 다르면 비교 자체가 거부된다.

### 나머지 연산(`%`)으로 순환을 처리하는 패턴

`(현재위치 + 이동거리) % 전체길이`는 "끝에 도달하면 다시 처음으로 돌아간다"는 상황을 한 줄로 해결한다. 시계, 요일, 원형 배열, 회전하는 순서 등 "일정 범위를 넘으면 처음으로 되돌아가는" 모든 상황에 적용 가능하며, `if`문으로 직접 순환을 처리할 때 흔히 생기는 "몇 바퀴를 도는 경우까지 고려하지 못하는" 버그를 근본적으로 없애준다.

### 이 풀이 방식의 성격: 시뮬레이션

이 풀이는 두 개의 포인터가 서로 다른 규칙으로 움직이며 상호작용하는 투 포인터 기법과는 다르다. 포인터(커서) 하나가 문제에서 설명한 절차(한 칸씩 이동 → skip이면 계속 지나침 → 목표 칸수 도달 시 정지)를 그대로 따라가는 방식이라, 이런 유형은 보통 시뮬레이션(또는 구현) 문제로 분류된다.

---

## 이 문제에서 얻은 교훈

"skip 문자를 만나면 세지 않는다"는 규칙을, "먼저 다 이동해보고 만난 skip 개수만큼 나중에 보정한다"로 잘못 해석하기 쉬웠다. 이 잘못된 해석이 위험한 이유는, 문제가 제공하는 예시 하나 정도는 우연히 통과할 수 있을 만큼 그럴듯하게 작동한다는 데 있다. 실제로 여러 사람이 이 문제를 풀면서 같은 함정(한 번의 순환 보정만 처리하고 여러 번 순환하는 경우나, skip 문자가 연속으로 나오는 경우를 놓치는 것)에 걸렸다가 정정한 기록을 남겼다. 결국 문제의 규칙은 "만나는 즉시, 그 자리에서 바로 판정하며 나아간다"는 것이었고, 이걸 나중에 한꺼번에 처리하려는 시도는 근본적으로 다른 로직이 되어버린다는 것이 이번 문제의 핵심 교훈이다.
