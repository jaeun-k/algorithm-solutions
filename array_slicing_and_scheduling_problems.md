# 규칙 발견 및 시뮬레이션 회피 4문제 정리

전체 자료구조를 실제로 만들지 않고 필요한 값만 계산하는 문제, 조합론 문제, 정의를 정확히 알아야 풀리는 문제, 그리고 "시뮬레이션처럼 보이지만 사실 한 번의 순회로 끝나는" 문제까지 함께 정리한다.

---

## 문제 1: 이차원 배열 잘라서 이어붙이기

n×n 크기의 배열을 정해진 규칙(1×1, 2×2, ... n×n 영역을 순서대로 겹쳐 채움)으로 채운 뒤, 모든 행을 이어붙인 1차원 배열에서 `[left, right]` 구간만 반환한다. `n`은 최대 10^7, `right-left`는 최대 10^5.

```python
solution(3, 2, 5)    # [3,2,2,3]
solution(4, 7, 14)   # [4,3,3,3,4,4,4,4]
```

### 시행착오: 이중 for문으로 배열을 통째로 만들려 함
```python
for i in range(0, n):
    for j in range(n):
        if j <= i:
            arr[i][j] = i
        else:
            arr[i][j] = j
```
이 방식은 O(n²)인데, `n`이 최대 10^7이라 배열 전체 크기(n²)가 최대 10^14에 달해 시간·메모리 모두 감당할 수 없다. `n×n` 배열을 실제로 다 만드는 이상 O(n²)은 절대 벗어날 수 없다 — 칸 수가 이미 n²개이므로 그 칸들을 다 채우려면 최소 n²번의 쓰기가 필요하다는 하한선 자체를 피할 수 없기 때문이다.

### 핵심 전환: "배열을 통째로 만들지 말고, 필요한 자리의 값만 계산한다"
문제가 실제로 요구하는 결과물은 `right-left < 10^5`개뿐이므로, 배열 전체를 만들 필요가 없다는 것이 이 문제의 핵심 트릭이었다.

**규칙 유도**: i 단계에서 "1행1열부터 i행i열까지의 **빈 칸**"을 채운다는 조건을 보면, 어떤 칸 (r, c)는 `r <= i` 이고 `c <= i`를 동시에 만족하는 가장 작은 i일 때 처음 채워지고, 그 이후로는 다시 안 바뀐다. 이 가장 작은 i는 곧 `max(r, c)`다 (r, c 둘 다 만족해야 하는 최소값은 그 둘 중 더 큰 값).

**1차원 인덱스를 2차원으로 되돌리기**: 1차원 배열은 한 행에 n개씩 이어붙인 구조이므로, 인덱스 `k`(0-indexed)에 대해 `row = k // n`, `col = k % n`으로 원래 몇 행 몇 열이었는지 역산할 수 있다.

### 정답 코드
```python
def solution(n, left, right):
    return [max(k // n, k % n) + 1 for k in range(left, right + 1)]
```
`k // n`은 몫이므로 행(같은 n개 묶음에 속하는 순번), `k % n`은 나머지이므로 열(그 묶음 안에서의 위치)이다. 전체 n² 배열을 만들지 않고 `left ~ right` 구간의 개수만큼만(O(right-left)) 순회한다.

---

## 문제 2: 옷 조합 (경우의 수)

카테고리별 옷 목록이 주어질 때, 각 카테고리에서 최대 1개씩 골라(혹은 아예 안 고르고) 만들 수 있는 서로 다른 조합의 수를 구한다 (아무것도 안 입는 경우는 제외).

```python
solution([["yellow_hat","headgear"],["blue_sunglasses","eyewear"],["green_turban","headgear"]])  # 5
```

### 핵심 아이디어: "카테고리별 (고르는 경우 + 안 고르는 경우) 를 곱의 법칙으로 결합"
각 카테고리에 옷이 `value`개 있으면, 그 카테고리에서 선택 가능한 경우의 수는 `value`(그중 하나를 고름) + 1(아예 안 고름) = `value + 1`. 카테고리별 선택은 서로 독립적이므로 전체 조합 수는 각 카테고리의 경우의 수를 모두 곱한 값이고, 마지막에 "모든 카테고리에서 안 고름"을 선택한 경우 1가지를 빼준다.

### 정답 코드
```python
def solution(clothes):
    result = 1
    d = {}
    for part in clothes:
        d[part[1]] = d.get(part[1], 0) + 1

    for value in d.values():
        result *= (value + 1)

    return result - 1
```
`d.items()`로 키까지 받았다가 안 쓰는 실수가 있었는데, 값만 필요하면 `d.values()`를 쓰는 것이 의도를 정확히 드러낸다. 카테고리별 개수를 세는 부분은 `collections.Counter`로도 대체 가능하지만, `dict.get(key, default)`은 어떤 로직에도 부담 없이 끼워 넣을 수 있는 범용 도구인 반면 `Counter`는 "카운팅"이라는 특정 목적에 최적화된 도구라 상황이 딱 맞을 때 자연스럽게 쓰게 된다는 차이를 확인했다.

---

## 문제 3: 행렬의 곱셈

두 2차원 행렬 arr1, arr2를 입력받아 행렬곱 결과를 반환한다.

```python
solution([[1,4],[3,2],[4,1]], [[3,3],[3,3]])  # [[15,15],[15,15],[15,15]]
```

### 시행착오: 정사각형 행렬로만 검증해서 드러나지 않은 축 실수
```python
for k in range(len(arr2)):        # 잘못됨: arr2의 "행 개수"
    ...
answer += [[0] * len(arr2)]       # 잘못됨: 결과의 열 개수 자리에 arr2의 "행 개수"를 씀
```
행렬곱 `arr1(m×n) x arr2(n×p) = 결과(m×p)`에서, 세 축(m, n, p)이 각각 어디에 쓰여야 하는지가 이 문제의 핵심이다.
- 결과의 행 개수 = `len(arr1)` (m)
- 곱셈이 일어나는 공통 축(k의 범위) = `len(arr1[0])` (n, arr1의 열 개수)
- 결과의 열 개수(j의 범위) = `len(arr2[0])` (p, arr2의 열 개수)

`len(arr2)`(arr2의 행 개수, n과 이론상 같아야 하는 값)를 k의 범위나 결과의 열 개수 자리에 잘못 넣는 실수가 있었는데, 테스트 케이스가 정사각형 행렬(행=열)이면 두 값이 우연히 같아서 오류가 드러나지 않는다는 함정이 있었다. 이 문제는 알고리즘적 트릭보다는 행렬곱의 정의(세 축의 의미) 자체를 정확히 아는지를 요구하는 문제였다.

### 정답 코드
```python
def solution(arr1, arr2):
    answer = []
    for _ in range(len(arr1)):
        answer += [[0] * len(arr2[0])]

    for i in range(len(arr1)):
        for j in range(len(arr2[0])):
            for k in range(len(arr1[0])):
                answer[i][j] += arr1[i][k] * arr2[k][j]

    return answer
```
결과 행렬의 칸 개수 자체가 m×p개이고, 각 칸을 채우려면 공통 축(n)을 따라 값을 누적해야 하므로 삼중 for문(O(m×n×p))은 행렬곱 정의상 피할 수 없는 계산량이다. `+=`로 누적하는 방식을 쓰는 이상, 미리 0으로 채워 그 자리를 확보해두는 것이 전제 조건이며, 이는 이 문제의 정석적인 접근이다 (미리 채우지 않고 `sum()`으로 각 칸을 한 번에 계산하는 방식도 가능하나 우열은 없다).

---

## 문제 4: 기능개발 (배포 스케줄링)

작업별 진도(progresses)와 개발 속도(speeds)가 주어질 때, 앞선 작업이 끝나야 뒤 작업도 함께 배포되는 규칙 하에 각 배포마다 몇 개의 기능이 배포되는지 구한다.

```python
solution([93,30,55],[1,30,5])   # [2,1]
```

### 시행착오 1: 하루하루를 실제로 시뮬레이션 (필요 이상으로 복잡)
진도를 매일 실제로 speed만큼 더해가며 100%를 넘는 순간을 stack, end, number 등 여러 상태로 추적하는 방식으로 접근했다. 이 문제는 사실 "각 작업이 완료되기까지 며칠 걸리는지"를 수식으로 한 번에 계산하고, 그 값들을 한 번만 순회하면 되는 문제였는데, 날짜를 실제로 흘려보내며 관찰하는 시뮬레이션으로 풀면서 필요 이상의 상태 관리가 발생했다.

### 시행착오 2: 같은 날 여러 작업이 완료될 때 결과가 쪼개짐
```python
for i in range(len(speeds)):
    progresses[i] += speeds[i]
    if progresses[i] >= 100 and i not in end:
        ...
        count = 0
        while number in stack:
            ...
        if count != 0:
            result += [count]
```
배포 판정(`count`, `result.append`)이 하루치 진도 갱신(`for` 루프) **내부**에 있어서, 같은 날 안에서도 여러 작업이 순차적으로 완료 조건을 만족하면 그때마다 별도로 카운트되어 결과가 쪼개지는 문제가 있었다. "배포는 하루의 끝에 이루어진다"는 조건을 코드가 반영하지 못한 것이었다. 배포 판정 로직을 `for` 루프 **바깥**(하루의 모든 작업 진도를 다 올린 뒤)으로 옮기자 해결됐다.

### 핵심 전환: "누적 최댓값을 기준으로 그룹핑"
"뒤에 있는 기능이 먼저 끝나도 앞의 기능이 배포될 때 함께 배포된다"는 조건은, 각 작업의 완료일(며칠 후 끝나는지)을 순서대로 훑으면서 **지금까지 나온 값 중 최댓값**을 기준선으로 삼아, 그 기준선을 넘지 않는 동안은 계속 같은 그룹으로 묶는 것과 동일하다. 최댓값보다 작거나 같으면 이미 그보다 늦게 끝나는 앞 작업이 있다는 뜻이므로 같은 그룹, 최댓값을 넘으면 그 작업이 새로운 기준이 되어 새 그룹이 시작된다.

### 정답 코드
```python
import math

def solution(progresses, speeds):
    days = [math.ceil((100 - p) / s) for p, s in zip(progresses, speeds)]

    answer = []
    count = 0
    max_day = days[0]
    for d in days:
        if d <= max_day:
            count += 1
        else:
            answer.append(count)
            max_day = d
            count = 1
    answer.append(count)
    return answer
```
시뮬레이션도, 딕셔너리도, 스택도 필요 없이 배열을 한 번(O(n)) 순회하는 것만으로 끝난다.

---

## 이 네 문제에서 얻은 교훈

### "결과물의 크기"와 "원본 자료구조의 크기"는 다를 수 있다
n² 크기의 배열을 실제로 만드는 건 O(n²)에서 벗어날 수 없지만, 문제가 요구하는 최종 결과물이 그 일부(`right-left`)뿐이라면, 원본 자료구조 자체를 만들지 않고 필요한 값만 수식으로 계산하는 방향으로 문제를 재정의할 수 있다. "이 배열을 어떻게 더 빨리 만들까"가 아니라 "애초에 이걸 다 만들 필요가 있는가"를 먼저 물어야 하는 유형이 존재한다.

### 정사각형/특수 케이스로만 검증하면 축 실수가 숨는다
행렬곱처럼 여러 축(행, 열, 공통 축)이 얽힌 문제는, 행과 열의 길이가 같은 테스트 케이스로만 검증하면 서로 다른 축을 잘못 바꿔 써도 우연히 값이 맞아 오류가 드러나지 않는다. 직사각형(행≠열) 케이스로 검증해야 진짜로 축을 정확히 구분해 썼는지 확인할 수 있다.

### "판정 시점이 언제인가"는 반복문의 들여쓰기 위치가 결정한다
하루 단위로 여러 항목을 갱신한 뒤 그 결과를 판정해야 하는 문제에서, 판정 로직이 갱신 루프 안에 있는지 밖에 있는지에 따라 "각 항목마다 즉시 판정"과 "그 시점 전체가 끝난 뒤 한 번만 판정"이라는 서로 다른 의미가 된다. 문제 조건에 "하루의 끝에"처럼 시점을 명시하는 표현이 있다면, 그 판정 로직이 정확히 어느 반복 범위 밖에 있어야 하는지를 먼저 따져야 한다.

### 시뮬레이션처럼 보여도 실제로는 한 번의 순회로 끝나는 문제가 있다
"매일 진도가 바뀐다"는 문제 설명 때문에 하루하루를 실제로 재현해야 할 것 같은 인상을 주지만, 필요한 것이 "각 항목이 특정 조건에 도달하는 시점"뿐이라면 그 시점을 수식으로 한 번에 계산한 뒤 정렬된 순서를 그대로 한 번만 훑는 것으로 충분한 경우가 많다. 특히 "누적 최댓값을 기준으로 그룹을 나눈다"는 패턴은 "지연이 전파되는" 스케줄링류 문제에서 반복적으로 등장한다.
