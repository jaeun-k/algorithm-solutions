# 문법 오류 두 개 + 소수(Prime) 2문제 정리

문법 실수 두 건을 먼저 잡고, 소수 판별 로직을 "약수 개수 세기"에서 "에라토스테네스의 체"까지 파고든 뒤, 그 이해를 곧바로 실전 문제에 적용해본 과정을 정리한다.

---

## 문제 0-A: 옹알이2 (문법 + 로직 두 겹 버그)

주어진 옹알이 문자열들이 `aya`, `ye`, `woo`, `ma` 네 음절만으로 완전히 분해되는지 판별한다. 단 `ayaaya`, `yeye`, `woowoo`, `mama`처럼 같은 음절이 연속되면 무효 처리한다.

### 시행착오 1: 문법 오류
```python
for j range(4):   # 'in' 누락
```
`for j in range(4):`로 수정.

### 시행착오 2: replace 결과를 버림
```python
if ps[j] in i:
    i.replace(ps[j],"")   # 반환값을 버림, i는 그대로임
```
파이썬 문자열은 불변(immutable)이라 `replace()`는 원본을 바꾸지 않고 새 문자열을 반환만 한다. `i = i.replace(ps[j],"")`로 재대입해야 실제로 반영된다.

### 시행착오 3: 지우고 나면 우연히 새 음절이 생겨버림 (진짜 원인)
`replace(ps[j], "")`로 완전히 지워버리면, 지워진 자리 양옆 글자가 서로 붙어버려서 **원래 없던 음절이 우연히 새로 생기는** 문제가 있다.

반례: `"myea"` (원래는 무효한 문자열)
1. `"ye"`가 `"myea"` 안에 있음 → 제거 → `"m"+"a"` = `"ma"` (m과 a가 붙어버림)
2. 방금 우연히 생긴 `"ma"`가 다음 검사에서 진짜 음절인 것처럼 매칭돼서 삭제됨 → `""`
3. 결과: 무효한 문자열인데 유효하다고 잘못 카운트됨

전수조사(길이 1~7, 알파벳 `ayewom`)로 실제 반례 42건을 확인했다: `myea`, `yayae`, `mayaa` 등.

### 정답 코드
```python
def solution(babbling):
    ps = ["aya", "ye", "woo", "ma"]
    imps = ["ayaaya", "yeye", "woowoo", "mama"]
    result = 0

    for i in babbling:
        if any(imp in i for imp in imps):
            continue
        for j in range(4):
            if ps[j] in i:
                i = i.replace(ps[j], " ")   # ""가 아니라 " "로 지워서 경계를 남김
        if i.replace(" ", "") == "":         # strip()이 아니라 전체 공백 제거로 검사
            result += 1

    return result
```
- `""` 대신 `" "`로 치환 → 지워진 자리에 공백이 남아서 양옆 글자가 다시 붙지 않음
- 마지막 검사는 `strip()`(양 끝만 제거)이 아니라 `replace(" ", "")`(전체 공백 제거)로 해야 중간에 남은 공백까지 다 걸러짐

---

## 문제 0-B: 문법 오류 (들여쓰기)

```python
box_list = sorted(score, reverse = True)
    for i in range(len(box_list)):   # 윗줄보다 들여쓰기가 한 단계 더 깊음
```
`IndentationError`. `for`문을 `box_list` 대입문과 같은 들여쓰기 레벨로 맞춰야 한다. 로컬 에디터(VS Code 등)와 달리 코딩테스트 사이트 편집창은 들여쓰기 오류를 눈에 잘 안 띄게 만든다는 점도 확인.

---

## 문제 1: N 이하 소수 개수 구하기

**예상 난이도: 브론즈 1**

### 처음 짠 코드 (약수 개수 세는 방식)
```python
def solution(n):
    numbers = [0] * (n - 1)
    for i in range(2, n+1):
        for j in range(i, n+1, i):
            numbers[j-2] += 1
    return numbers.count(1)
```
`numbers[j-2]`는 "2부터 j까지 중 j를 나누는 약수의 개수"를 센다. 약수가 1개(자기 자신)뿐이면 소수라는 논리로, **정확도 자체는 문제없다.** 다만 `i`가 소수든 합성수든 상관없이 매번 배수를 전부 순회하므로 비효율이 있다.

### 정석: 에라토스테네스의 체
```python
def solution(n):
    sieve = [True] * (n+1)
    sieve[0] = sieve[1] = False
    for i in range(2, int(n**0.5)+1):
        if sieve[i]:
            for j in range(i*i, n+1, i):
                sieve[j] = False
    return sum(sieve)
```

**핵심 원리**: 소수의 배수는 소수가 아니다 → 2부터 배수를 지워나가면, 끝까지 안 지워지고 남는 수가 곧 소수.

**`if sieve[i]:`가 핵심인 이유**: 이미 계산된 결과(그 수가 지금까지 안 지워졌다 = 소수로 확정됐다)를 재활용해서, 합성수일 때는 배수 지우는 작업 자체를 생략한다. "약수 개수 세기" 버전은 이 재활용이 없어서 모든 `i`에 대해 무조건 순회한다는 게 본질적 차이.

**왜 `i*i`부터 시작하는가**: `i*2 ~ i*(i-1)`은 `i`보다 작은 소수의 배수라서 그 소수의 차례에 이미 지워진 상태다. 다시 지우는 건 100% 중복 작업이라 생략 가능하다 (생략해도 정답에는 영향 없음, 속도만 다름).

**왜 `√n`까지만 도는가**: 합성수 `m = a×b` (a ≤ b)에서 `a`는 반드시 `√m` 이하다 (둘 다 `√m`보다 크면 `a×b > m`이 되어 모순). 즉 `n` 이하 모든 합성수는 반드시 `√n` 이하의 소인수를 가지므로, outer loop가 `√n`까지만 돌아도 전부 걸러진다. 그 이상은 새로 지울 게 없어서 도는 게 낭비다.

**`[True] * (n+1)`인 이유**: 인덱스를 실제 숫자값과 1:1로 맞추기 위해 (`sieve[7]` = 숫자 7의 소수 여부). 대신 인덱스 0, 1은 자동으로 걸러지지 않으므로 `sieve[0] = sieve[1] = False`를 명시적으로 해줘야 한다 — 안 해주면 `sum(sieve)` 카운트에 0, 1이 유령처럼 섞여 들어간다.

### 검증
초반 코드(약수 개수 세기)와 에라토스테네스 체 버전 둘 다 로직상 정답. 차이는 정확도가 아니라 순수 효율성.

---

## 문제 2: 세 수를 더해 소수 만들기

**예상 난이도: 실버 4~3**

`nums`에서 서로 다른 3개를 골라 더한 합이 소수인 경우의 개수를 구한다.

### 시행착오 1: 인덱스와 값을 혼동
```python
total = i+j+k   # i,j,k는 인덱스인데 인덱스 자체를 더함
```
`total = nums[i] + nums[j] + nums[k]`로 고쳐야 실제 원소값의 합이 된다.

### 시행착오 2: "2, 3, 5로 안 나눠떨어지면 소수"라는 잘못된 판별
```python
if i % 2 != 0 and i % 3 != 0 and i % 5 != 0:
```
2, 3, 5로 안 걸러지는 합성수가 존재한다는 걸 반례로 확인:
- `49 = 7×7` → 2,3,5로 안 나눠떨어지지만 소수 아님
- `121 = 11×11` → 2,3,5,7로도 안 걸러짐
- `169 = 13×13` → 2,3,5,7,11로도 안 걸러짐

패턴: 나눌 소수를 몇 개 추가해도 그보다 큰 소인수를 가진 합성수(제곱수 등)는 항상 반례로 만들 수 있다. "소수 몇 개 나열해서 검사"하는 방식 자체가 근본적으로 안전하지 않다는 결론.

이 문제 제한사항상 원소가 최대 1000, 세 개 합은 최대 3000까지 나올 수 있어 `√3000 ≈ 54.8` — 2부터 54까지 전부 검사해야 안전하다.

### 정답 코드
```python
def solution(nums):
    def is_prime(n):
        if n < 2:
            return False
        for i in range(2, int(n**0.5)+1):
            if n % i == 0:
                return False
        return True

    result = 0
    for i in range(len(nums) - 2):
        for j in range(i+1, len(nums) - 1):
            for k in range(j+1, len(nums)):
                total = nums[i] + nums[j] + nums[k]
                if is_prime(total):
                    result += 1
    return result
```

### 검증
```
[1,2,3,4]   -> 1   (정답)
[1,2,7,6,4] -> 4   (정답)
```

---

## 이 시리즈에서 얻은 교훈

### 문법 오류와 로직 오류는 다른 층위의 문제다
`for j range(4)`처럼 코드가 아예 안 돌아가는 문법 오류는 눈으로 훑으면 잡히지만, `replace()`가 반환값을 버리는 문제나 `i.replace(ps[j],"")`가 우연히 새 음절을 만들어내는 문제는 **코드가 정상적으로 실행되면서도 조용히 틀린 답을 낸다**는 점에서 훨씬 찾기 어렵다. 후자는 전수조사나 랜덤 테스트로 반례를 직접 만들어봐야 확실히 잡힌다.

### "정답이 나온다"와 "정석이다"는 다른 축
약수 개수를 세는 소수 판별이나, 세 수 합 문제의 초기 접근 모두 "왜 이게 되는지"를 스스로 설명할 수 있으면 그 자체로 유효한 풀이다. 정석(에라토스테네스의 체, `is_prime` 함수)은 효율성과 안전성 문제이지, 논리적 정확성의 문제가 아니다. 다만 "2,3,5로만 나눠서 소수 판별"처럼 **일부 케이스에서만 우연히 맞는 방식**은 정답 축에도 못 낀다 — 이건 정석이냐 아니냐의 문제가 아니라 애초에 틀린 로직이다.
