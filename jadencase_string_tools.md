# JadenCase 문제로 정리하는 문자열 도구들

`split`, `join`, `capitalize`, `isdigit` — 딕셔너리 기반 구현 문제를 풀다 보면 계속 마주치는 문자열 도구들을, JadenCase 문제 하나를 풀어가는 과정에서 원리 단위로 정리한다.

---

## 문제: JadenCase 만들기

모든 단어의 첫 문자는 대문자, 나머지는 소문자로 바꾼다. 단, 첫 문자가 알파벳이 아니면(숫자) 이어지는 알파벳은 그대로 소문자 처리한다. 공백은 연속으로 나올 수 있고, 원본의 공백 구조(개수, 위치)는 그대로 보존되어야 한다.

```python
solution("3people unFollowed me")  # "3people Unfollowed Me"
solution("for the last week")      # "For The Last Week"
```

### 첫 시도: 직접 순회하며 대문자 위치 판단

```python
def solution(s):
    s = s.lower()
    answer = ""
    for i in range(len(s)):
        if i == 0:
            if ord("0") > ord(s[i]) or ord("9") < ord(s[i]):
                answer += s[i].upper()
            else:
                answer += s[i]
        else:
            if s[i-1] == " " and s[i] != " ":
                if ord("0") > ord(s[i]) or ord("9") < ord(s[i]):
                    answer += s[i].upper()
                else:
                    answer += s[i]
            else:
                answer += s[i]
    return answer
```

`s[i-1] == " "` 조건 하나로 "직전 문자가 공백이면 지금이 단어의 시작"이라는 걸 판단했고, 이 조건은 연속 공백이 있어도 자연스럽게 성립해서 별도 처리 없이 통과했다. 다만 숫자인지 아닌지를 `ord()` 비교로 직접 판별하고 있는데, 뒤에서 확인하듯 이 부분은 불필요한 방어 로직이었다.

---

## 도구 1: `split(" ")`이 빈 문자열을 만드는 원리

공백이 연속으로 나올 수 있다는 제약 때문에 `split()`(인자 없음)을 쓰면 공백 구조 정보가 사라진다는 게 이 문제의 핵심 난관이었다. `split(" ")`으로 구분자를 명시하면 이 정보가 보존되는데, 그 원리를 "칼자국" 모델로 정리했다.

### 규칙: 구분자 N개 → 조각 N+1개

문자열에서 구분자(여기선 공백)가 나타나는 **위치 하나하나가 칼자국 하나**다. 칼자국이 N개면, 그 칼자국들 사이사이 + 맨 앞 + 맨 뒤를 합쳐 조각은 항상 N+1개가 나온다.

```python
"a  b".split(" ")   # ['a', '', 'b']       -> 칼자국 2개, 조각 3개
"a   b".split(" ")  # ['a', '', '', 'b']   -> 칼자국 3개, 조각 4개
```

칼자국끼리 바로 붙어있으면(연속 공백) 그 사이에 "아무것도 없다"는 사실 자체가 빈 문자열로 기록된다. 즉 빈 문자열은 예외적으로 발생하는 게 아니라, 매 칼자국 사이 구간을 정직하게 보고하는 과정에서 나오는 자연스러운 결과다.

### 문자열의 시작과 끝도 암묵적인 경계다

이 규칙에서 가장 헷갈렸던 지점은 "칼자국 앞/뒤에 아무 문자도 없는 경우"였다. `split`은 문자열의 시작과 끝을 항상 하나의 경계로 취급하기 때문에, 그 경계와 칼자국 사이에 아무것도 없어도 빈 문자열이 그대로 생긴다.

```python
"a".split(" ")    # ['a']         -> 칼자국 0개, 조각 1개
" a".split(" ")   # ['', 'a']     -> 칼자국 1개, 조각 2개 (맨 앞이 빈 조각)
"a ".split(" ")   # ['a', '']     -> 칼자국 1개, 조각 2개 (맨 뒤가 빈 조각)
```

"뭔가 있어야 그 자리가 기록된다"가 아니라, "그 구간에 아무것도 없었다"는 사실 자체가 유효한 결과로 취급된다는 것이 핵심.

### `split()` vs `split(" ")`

인자 없는 `split()`은 연속 공백과 앞뒤 공백을 전부 알아서 정리해주지만, 그 과정에서 원래 공백이 몇 개였는지 정보가 사라진다. 반대로 `split(" ")`은 그 정보를 빈 문자열의 형태로 보존한다. 이번 문제처럼 공백 구조를 그대로 복원해야 하는 문제에서는 `split(" ")`이 필수였다.

```python
"  hello   world  ".split()      # ['hello', 'world']  (공백 정보 소실)
"  hello   world  ".split(" ")   # ['', '', 'hello', '', '', 'world', '', '']
```

---

## 도구 2: `" ".join(...)`은 "사이에 끼워 넣는다"는 개념

`구분자.join(리스트)`는 리스트 원소들 사이사이에 그 구분자를 끼워 넣으며 이어붙이는 동작이다.

```python
"-".join(["a", "b", "c"])  # "a-b-c"
"".join(["a", "b", "c"])   # "abc"  (빈 문자열을 끼워 넣으니 사실상 다 붙는 것)
```

`split(" ")`으로 쪼갠 뒤 각 조각을 처리하고 다시 `" ".join(...)`으로 합치면, 빈 문자열 조각이 있던 자리에는 공백이 다시 끼워지면서 원래의 연속 공백 구조가 그대로 복원된다. `split(" ")`이 빈 문자열로 인코딩해둔 공백 정보를 `" ".join(...)`이 그대로 디코딩해주는 셈.

---

## 도구 3: `str.capitalize()` — 이 문제에 정확히 맞는 메서드

"첫 글자만 대문자, 나머지는 전부 소문자"를 그대로 구현해주는 내장 메서드.

```python
"unFollowed".capitalize()  # 'Unfollowed'
"3people".capitalize()     # '3people'  (숫자는 대문자화 대상이 아니므로 그대로)
"".capitalize()            # ''         (빈 문자열이 들어와도 에러 없이 그대로 반환)
```

`capitalize()`가 빈 문자열에 대해 안전하게 동작한다는 점이 중요하다. `split(" ")`으로 쪼갠 리스트에는 빈 문자열 조각이 섞여 있을 수밖에 없는데, `capitalize()`를 그대로 적용해도 에러 없이 통과하기 때문에 별도의 "빈 문자열인지 확인" 로직 없이 한 줄로 문제를 풀 수 있다.

```python
def solution(s):
    return " ".join(w.capitalize() for w in s.split(" "))
```

---

## 도구 4: `isdigit()`과, `upper()`/`lower()`가 숫자에 안전한 이유

### `isdigit()`: 코테에서 쓸 건 이거 하나로 충분

`isdigit()`, `isdecimal()`, `isnumeric()` 세 메서드는 원문자(`②`)나 한자 숫자(`一二三`) 등 특수한 유니코드 숫자를 다루는 범위에서만 차이가 나고, 일반적인 코테 문제에서는 사실상 `isdigit()` 하나로 충분하다.

주의할 함정 두 가지:
- `"-123".isdigit()` → `False`. 마이너스 부호가 붙으면 숫자 문자열로 인식하지 않는다.
- `"".isdigit()` → `False`. 빈 문자열은 "1글자 이상 + 전부 숫자"라는 조건을 만족하지 못해 항상 `False`다.

### `upper()`/`lower()`는 바꿀 수 없는 문자를 에러 없이 그대로 통과시킨다

숫자에 `isdigit()`으로 분기해서 예외 처리를 해줘야 한다고 생각했지만, 실제로는 `upper()`/`lower()` 자체가 "대소문자 개념이 없는 문자"를 만나면 에러를 던지지 않고 원래 모습 그대로 통과시킨다.

```python
"3".upper()                                  # '3'
"3people"[0].upper() + "3people"[1:].lower() # '3people'
```

즉 첫 시도에서 짰던 `ord("0") > ord(s[i]) or ord("9") < ord(s[i])` 같은 숫자 판별 로직은 이 문제에서 실제로는 필요 없는 방어 코드였다. `upper()`가 이미 숫자를 안전하게 처리해주기 때문이다.

---

## 실전에서 걸렸던 지점: `!= " "` 와 `!= ""`는 다른 조건

`capitalize()`를 안 쓰고 직접 인덱싱으로 구현하려던 시도에서 다음과 같은 코드가 `IndexError`로 터졌다.

```python
def solution(s):
    s = s.split(" ")
    for i in range(len(s)):
        if s[i] != " ":                                   # 버그: 공백 문자와 비교
            s[i] = s[i][0].upper() + s[i][1:].lower()
    return " ".join(s)
```

`split(" ")`이 만들어내는 건 `""`(빈 문자열)이지 `" "`(공백 문자 하나)가 아니다. 그런데 조건문은 `s[i] != " "`으로, "공백 문자 하나와 다른가"만 검사하고 있었다. `"" != " "`는 `True`이기 때문에 빈 문자열도 "단어"로 취급되어 `s[i][0]`에서 인덱스 에러가 발생한 것.

```python
if s[i] != "":   # 공백이 아니라 '빈 문자열'을 걸러내야 한다
    s[i] = s[i][0].upper() + s[i][1:].lower()
```

`split(" ")`이 실제로 리스트에 채워 넣는 값이 무엇인지(공백이 아니라 빈 문자열) 정확히 알아야 이런 조건문 버그를 피할 수 있다는 걸 확인한 사례.

---

## 최종 코드 두 버전

```python
# 버전 1: 직접 순회, 로직이 눈에 다 보이는 버전
def solution(s):
    s = s.split(" ")
    for i in range(len(s)):
        if s[i] != "":
            s[i] = s[i][0].upper() + s[i][1:].lower()
    return " ".join(s)

# 버전 2: capitalize 활용, 문제 의도를 그대로 코드로 옮긴 버전
def solution(s):
    return " ".join(w.capitalize() for w in s.split(" "))
```

두 버전은 결과가 완전히 동일하다. 버전 1은 "무슨 일이 일어나는지"가 코드에 다 드러나 있고, 버전 2는 `capitalize()`라는 이름 자체가 문제 설명("첫 글자만 대문자")을 그대로 코드로 옮긴 형태다. 실전에서는 내장 메서드를 아는 만큼 코드가 짧아지고 버그 낼 여지(오늘 겪은 `!= " "` 버그 같은)도 줄어든다.

---

## 곁가지로 정리한 것들

### `zip` 없이 `for i, j in (A, B)`를 쓰면 안 되는 이유

`(A, B)`는 리스트 두 개를 담은 튜플 하나일 뿐이라, 순회하면 원소가 `A`, `B` 딱 2개뿐이다. `for i, j in (A, B)`처럼 변수 2개로 언패킹하려 하면 첫 원소인 `A`(리스트 전체)를 2개로 쪼개려다 `ValueError`가 난다. `A`와 `B`의 원소를 인덱스별로 짝지어 순회하려면 `zip(A, B)`가 필요하다.

### `sum`의 곱셈 버전은 `math.prod`

내장함수 `sum`처럼 곱을 구해주는 건 `math.prod`(파이썬 3.8+)다.

```python
import math
math.prod([1, 2, 3, 4])  # 24
```
