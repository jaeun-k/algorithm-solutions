# 우연한 안전장치에서 정석 구조로: 세 문제 정리

"짝지어 제거하기", "피보나치 수", "카펫" 세 문제를 풀면서 반복적으로 나타난 패턴이 하나 있다. **처음엔 뭔가 부수적인 이유로 붙여둔 코드 한 줄이, 알고 보면 문제의 핵심 안전장치였고, 그걸 명시적인 조건으로 바꾸는 과정에서 정석 구조(스택, DP, 약수 검증)에 도달했다**는 것. 그 과정을 문제별로 정리한다.

---

## 문제 1: 짝지어 제거하기

문자열에서 같은 문자 2개가 연속으로 붙어 있는 짝을 찾아 제거하고, 그 앞뒤를 이어 붙이는 과정을 반복한다. 이 과정으로 문자열을 완전히 제거할 수 있는지 판별한다.

```python
solution("baabaa")  # 1  (b aa baa -> bb aa -> aa -> "")
solution("cdcd")     # 0
```

### 2시간 고민 끝의 첫 풀이

```python
def solution(s):
    l = []
    for i in s:
        if i not in l:
            l.append(i)
        else:
            if l[-1] == i:
                l.pop()
            else:
                l.append(i)
    return int(l == [])
```

랜덤 케이스 대조로 정답임을 확인했다.

### "찌꺼기"인 줄 알았던 `not in l`이 사실 안전장치였다

`if i not in l:` 조건은 사실상 로직에 영향을 주지 않는다는 걸 확인했다 — `l and l[-1] == i`로 단순화해도 결과가 완전히 동일했다. 그런데 이 조건을 그냥 지워버리면 `l[-1]`에서 `IndexError`가 난다: **`l`이 비어 있을 때 `l[-1]`에 바로 접근하면 에러가 나는데, `not in l`이 빈 리스트에서는 항상 `True`가 되는 성질 덕분에 이 접근 자체를 우연히 막아주고 있었던 것.**

같은 안전성을 의도가 드러나게 표현하면:
```python
if l and l[-1] == i:   # l이 비어있으면 뒤는 확인도 안 하고 False로 단락 평가
    l.pop()
else:
    l.append(i)
```

더 다듬은 최종형:
```python
def solution(s):
    l = []
    for i in s:
        if l == [] or l[-1] != i:
            l.append(i)
        else:
            l.pop()
    return int(l == [])
```

### 이게 스택(Stack)이었다

"짝이 맞으면 없애고, 아니면 쌓아둔다"는 규칙을 스스로 만들어 리스트의 맨 끝에서만 넣고 빼는 방식으로 구현했는데, 이게 정확히 스택의 핵심 동작이다. 개념을 배운 적은 있지만 한동안 실전에서 안 쓰다 보니, "이거 스택 문제니까 스택을 쓰자"가 아니라 **필요에 의해 규칙을 만들다 보니 결과적으로 스택 구조에 도달**했다. 정형화된 틀에 사고를 끼워 맞추는 게 아니라, 사고력으로 정형화된 틀을 스스로 불러오는 것 — 이 방향이 맞다는 확인이었다.

(참고로 `l == []`보다 `not l`이 근소하게 더 빠르지만, 이 문제 규모에서는 어느 쪽이든 무방한 수준의 차이였다.)

---

## 문제 2: 피보나치 수

`n`번째 피보나치 수를 1234567로 나눈 나머지를 구한다. `n`은 2 이상 100,000 이하.

### 첫 시도: 재귀 — RecursionError

```python
def solution(n):
    if n == 2:
        return 1
    if n == 3:
        return 2
    return solution(n-1) + solution(n-2)
```

`n`이 커지면 `RecursionError`가 발생했다. 원인을 파고들며 확인한 것들:

- **호출 횟수 자체가 지수적으로 폭발한다.** `n=30`짜리 하나 계산하는 데 함수 호출이 약 100만 번 일어났다. `solution(n-1)`과 `solution(n-2)`가 각각 내부에서 겹치는 하위 문제(예: `solution(3)`)를 계속 중복 계산하기 때문이다.
- 이 폭발은 O(n²)보다도 훨씬 크다. n²은 n=35에서 겨우 1,225인데, 실제 호출 횟수는 1,140만 번이었다 — 이건 다항식(O(n²))이 아니라 지수(O(2ⁿ) 근사) 계열의 증가다.
- **"재귀는 원래 다 이렇다"는 건 아니다.** 팩토리얼이나 이진탐색처럼 매번 가지가 1개만 뻗는 재귀는 n=900이든 백만개 중 탐색이든 문제없이 빠르다. 병합정렬처럼 가지가 2개로 나뉘어도, 나뉜 조각들이 서로 겹치지 않고 확실히 작아지면(O(n log n)) 괜찮다. **위험한 건 "같은 하위 문제를 겹쳐서 다시 계산하는" 구조(overlapping subproblems)뿐**이며, 피보나치의 순수 재귀가 정확히 이 함정이다.
- 재귀 깊이 제한(파이썬 기본 약 1,000)은 이것과는 별개의 문제다. 시간복잡도가 멀쩡한 O(n) 재귀(팩토리얼 등)도 n이 1,000을 넘으면 깊이 제한에 걸려 에러가 난다.

### 복잡한 수식을 찾으려다, 가장 순수한 관계로 돌아가 반복문에 도달

처음엔 변수 여러 개로 수식을 세워 재귀와 같은 효과를 내는 닫힌 형태를 찾으려 했으나 막혔다. 그러다 "지금 값은 이전 값과 전전 값의 합"이라는 문제의 가장 핵심적인 관계 하나에만 집중하니, 그걸 그대로 구현하기 가장 쉬운 도구가 리스트였다.

```python
def solution(n):
    l = [0, 1]
    for i in range(2, n+1):
        l.append((l[i-1] + l[i-2]) % 1234567)
    return l[n]
```

작은 값부터 순서대로 쌓아 올려서 큰 값에 도달하는 이 방식이 동적 계획법(DP)의 바텀업(Bottom-up) 접근이다. 겉모습은 다르지만 "지금 상태의 답을 그보다 작은 상태들의 답으로 표현한다"는 핵심 질문은 계단 오르기, 배낭 문제 등 다른 DP 유형에서도 계속 반복된다.

### 나머지 연산은 마지막이 아니라 매 단계마다

처음엔 `l[n] % 1234567`처럼 마지막에 한 번만 나머지를 취했는데, `n=100,000`에서 약 4초가 걸렸다. 매 단계마다 나머지를 취하도록 바꾸니 0.017초로 줄었다.

원인: 마지막에만 나머지를 취해도 최종 결과값 자체는 작지만, **그 결과에 도달하기까지 중간 계산값들은 나머지 연산 없이 그대로 커진다.** `F(31)`부터 이미 1234567을 넘기 시작해서, `n=10,000`쯤이면 자릿수가 2,000자리를 넘고 `n=100,000`이면 수만 자리까지 불어난다. 큰 수끼리의 덧셈은 자릿수가 클수록 더 오래 걸리므로(2만 자리 수 덧셈이 5자리 수 덧셈보다 실측 20배 가까이 느렸다), 반복 횟수(n)와 별개로 매 반복의 비용 자체가 계속 커지는 이중 부담이 걸린다.

이 최적화가 성립하는 이유는 모듈러 연산의 분배 성질 때문이다:
```
(a + b) % m == ((a % m) + (b % m)) % m
```
`a = m×p + r_a`, `b = m×q + r_b`로 쓰면 `a+b = m×(p+q) + (r_a+r_b)`가 되고, `m`의 배수인 부분은 나머지 계산에 영향을 주지 않으므로 미리 나머지만 남기고 계산해도 최종 나머지는 동일하다. (곱셈에도 같은 성질이 성립하지만, 뺄셈/나눗셈은 이렇게 단순하지 않다.)

---

## 문제 3: 카펫

테두리는 갈색, 안쪽은 노란색인 격자 카펫에서, 갈색 격자 수(`brown`)와 노란색 격자 수(`yellow`)가 주어졌을 때 카펫의 가로/세로 크기를 구한다.

핵심 관계식:
```
yellow = y_width * y_length
brown = (y_width + y_length) * 2 + 4
```

### 첫 풀이 (전수조사, `/` 사용)

```python
def solution(brown, yellow):
    y_width = yellow
    while y_width >= yellow / y_width:
        if brown == (y_width + yellow / y_width)*2 + 4:
            return [y_width + 2, yellow / y_width + 2]
        y_width -= 1
```

모든 예시를 통과했지만, `/`를 써야 하는지 `//`를 써야 하는지, 그리고 반복 조건이 왜 이 형태여야 하는지는 확신 없이 넘어간 지점이었다.

### `/`를 `//`로 바꾸면 대량으로 틀린다

실제 가로/세로 값으로 `brown`, `yellow`를 역산해 대량 테스트한 결과, `//`로 바꾼 버전은 수천 건이 오답으로 나왔다. 원인: `y_width`가 `yellow`의 진짜 약수가 아닌 경우(예: `yellow=4`, `y_width=3`), `/`는 `1.333...`처럼 지저분한 소수를 그대로 남겨서 `brown` 공식이 정수와 절대 같아질 수 없게(=자동으로 걸러지게) 만든다. 반면 `//`는 이 나머지를 그냥 버려버려서, **애초에 성립할 수 없는 조합을 성립하는 것처럼 둔갑시켜 가짜 정답을 채택**해버린다. 즉 `/`가 하던 일은 사실 "이 후보가 약수인지 검증"하는 역할을 우연히 겸하고 있었던 것이다.

### 반복 종료 조건을 `yellow / 2`로 잘못 바꾸면 정답을 못 찾는다

원래 조건 `y_width >= yellow / y_width`는 양변에 `y_width`를 곱하면 `y_width >= √yellow`와 같다. "가로가 세로 이상"이라는 제약상, `y_width`는 정사각형이 되는 지점(`√yellow`)까지만 내려가면 충분하기 때문이다. 이걸 `yellow / 2`로 바꾸면 훨씬 큰(대체로 `√yellow`보다 한참 큰) 값에서 탐색이 멈춰버려서, 진짜 정답이 `√yellow`와 `yellow/2` 사이에 있을 경우 탐색이 거기 도달하기도 전에 종료돼 버린다 (`yellow=24` 예시에서 실제로 재현됨).

### 간결화: 우연한 안전장치를 명시적 조건으로

```python
def solution(brown, yellow):
    for y_width in range(yellow, 0, -1):
        if y_width * y_width < yellow:   # y_width < √yellow 지점부터는 탐색 중단
            break
        if yellow % y_width == 0:         # 약수인지 명시적으로 검증
            y_length = yellow // y_width
            if brown == (y_width + y_length)*2 + 4:
                return [y_width+2, y_length+2]
```

- 종료 조건을 제곱근/나눗셈 없이 `y_width * y_width < yellow`(정수 비교)로 표현해 부동소수점 걱정을 없앴다.
- `yellow % y_width == 0`으로 약수 여부를 직접 검증해, `/`가 우연히 대신해주던 역할을 명시적으로 드러냈다.
- 검증을 마친 뒤의 `//`는 더 이상 위험하지 않다 — 나누어떨어짐이 이미 보장된 상태이기 때문이다.

---

## 세 문제를 관통하는 패턴

세 문제 모두 같은 구조를 가진다: **뭔가 부수적인 이유로 넣어둔 코드(`not in l`, `/`)가 사실은 "잘못된 후보를 걸러내는" 핵심 안전장치였고, 그 이유를 정확히 진단해서 명시적인 조건(`l and l[-1]==i`, `yellow % y_width == 0`)으로 바꾸는 과정에서 더 안전하고 명확한 코드에 도달했다.** 우연히 통과한 코드를 "왜 통과했는지" 되짚어보는 습관이, 결국 정석 구조(스택, DP, 검증 로직)를 스스로 재발명하는 경로가 됐다.

---

## 곁가지: 사고력만으로 도달하기 어려운 알고리즘들

스택, 이진탐색, 탐욕법처럼 "문제를 계속 관찰하다 보면 자연스럽게 재발명 가능한" 것들과 달리, 아래 항목들은 그 형태 자체가 독립적인 발명품이라 배우지 않으면 순수 사고력만으로 도달하기 사실상 불가능하다:

- **동적계획법(DP)** — 특히 상태 정의 자체가 비자명한 비트마스크 DP, 트리 DP, 구간 DP
- **그래프 알고리즘** (다익스트라, 플로이드-워셜, 크루스칼/프림) — 그리디 선택이 항상 최적임을 보장하는 수학적 증명이 설계에 내재됨
- **유니온-파인드** — 경로 압축, union by rank 같은 최적화는 완전히 별개의 발명
- **세그먼트 트리 / 펜윅 트리** — 구간 합/최솟값을 이진트리 구조로 O(log n)에 해결하는 방식
- **문자열 고급 알고리즘** (KMP의 실패 함수, 라빈-카프, 트라이, 접미사 배열)
- **수학적 트릭** (모듈러 역원, 조합론 + 소수 모듈러, 행렬 거듭제곱)
- **네트워크 플로우** (포드-풀커슨, 디닉)

이런 것들은 "필요성은 느껴도 해결책의 형태 자체를 유추하기 어려운" 영역이라, 스스로 발상해보려 애쓰기보다 정석 패턴을 의도적으로 학습해두는 편이 효율적이다.
