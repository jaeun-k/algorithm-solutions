# 대문자만 뒤집기 (Reverse Uppercase Only) — 풀이 과정 정리

**예상 난이도: 실버 5~4** (처음엔 브론즈 5로 제시됐으나, 실제 체감 난이도를 반영해 조정)

## 문제 정의

문자열 `s`가 주어졌을 때, **대문자로 이루어진 부분만 순서를 뒤집고 나머지(소문자, 숫자, 공백 등)는 원래 자리와 값 그대로 유지**하는 함수 `reverse_upper(s)`를 작성한다.

- 대문자가 아닌 문자(공백, 숫자 등)는 위치와 값 그대로 둔다.
- 대문자가 아예 없으면 원본을 그대로 반환한다.

```python
reverse_upper("aBCdEfG")
# 대문자만 모으면: B, C, E, G → 뒤집으면: G, E, C, B
# 결과: "aGEdCfB"

reverse_upper("Hello World")
# 대문자: H, W → 뒤집으면: W, H
# 결과: "Wello Horld"
```

이 문서는 같은 문제를 세 가지 방식으로 풀어본 과정과, 그중 두 번째에서 세 번째로 넘어가며 겪은 시행착오(약 2시간 분량)를 정리한 것이다. 두 번째 접근까지는 각각 별도의 실패 없이 도달했고, 세 번째 접근(투 포인터)에서 방향을 여러 번 잘못 잡으며 오래 걸렸다.

---

## 버전 1: 리스트로 변환 + "명단에 모아뒀다 재배치" 방식

기존에 써왔던 "대문자만 따로 모아두고, 뒤집은 뒤 원래 자리에 순서대로 끼워 넣는다"는 발상으로 접근했다.

### 막혔던 지점: "원래 자리에 순서대로 넣는 법"
대문자를 뽑아서 뒤집는 것까지는 바로 됐지만, 그 뒤집힌 값들을 원래 문자열의 정확한 위치에 다시 넣는 방법에서 막혔다. `append`로는 항상 리스트 맨 뒤에만 추가되지, 특정 인덱스에 끼워 넣을 수 없다는 것을 이 과정에서 확인했다 (`insert()`는 아직 배우지 않은 도구). 결국 "인덱스와 값을 같이 기록해두는" 접근도 고려했으나, 실제로는 원본 문자열을 **리스트로 변환**해서 인덱스 위치에 직접 대입하는 방식으로 해결했다.

### 겪은 문법 오류들
1. **`len[uppers]`**: `len`은 함수이므로 `()`로 호출해야 하는데 `[]`(인덱싱 문법)를 써서 에러 발생. `len(uppers)`로 수정.
2. **`s[j] = uppers[count]`에서 에러**: 문자열은 불변(immutable)이라 특정 인덱스의 값을 직접 대입해서 바꿀 수 없다는 것을 다시 확인. (이전에 이미 배운 개념이지만 실전에서 다시 부딪힘)
3. **`list(s)`만 쓰고 반환값을 저장하지 않음**: `list(s)`는 새 리스트를 만들어 반환할 뿐 `s` 자체를 바꾸지 않는다. `list_s = list(s)`처럼 결과를 변수에 담아야 실제로 반영된다. (`simple_text_game.md`에서 겪었던 "함수 호출 결과를 저장하지 않는" 실수와 같은 종류)
4. **`char_s = " "`로 초기화**: 빈 문자열(`""`)이 아니라 공백(`" "`) 하나로 초기화해서, 최종 결과 앞에 불필요한 공백이 매번 붙는 버그가 있었다. `""`으로 수정.

### 최종 정답 코드 (변수명 정리 완료)
```python
def reverse_upper(s):
    letters = list(s)
    uppercase_letters = []

    for value in s:
        if 'A' <= value <= 'Z':
            uppercase_letters.append(value)

    for i in range(len(uppercase_letters) // 2):
        uppercase_letters[i], uppercase_letters[len(uppercase_letters) - 1 - i] = uppercase_letters[len(uppercase_letters) - 1 - i], uppercase_letters[i]

    count = 0
    for j in range(len(letters)):
        if 'A' <= letters[j] <= 'Z':
            letters[j] = uppercase_letters[count]
            count = count + 1

    result = ""
    for char in letters:
        result = result + char

    return result
```

### 최적화 포인트
- `for j in range(len(s))` → `for j in range(len(letters))`로 수정: 값은 같지만(`len(s) == len(letters)`), 이 반복문에서 실제로 다루는 대상이 `letters`이므로 기준을 `letters`로 맞추는 게 논리적으로 일관됨.
- 마지막 합치는 반복문에서 `for n in range(len(list_s))`로 인덱스를 만들어 값을 꺼내던 것을, `for char in letters`로 값 자체를 바로 순회하도록 변경 (인덱스가 실제로 필요 없는데 굳이 만들 필요 없음).

---

## 버전 2: 리스트 변환 없이 문자열만으로 풀기

"문자열도 인덱싱(읽기)이 가능한데, 굳이 리스트로 바꿔야 하는가?"라는 질문에서 출발했다. 결론적으로 **원본을 직접 수정하는 대신, 처음부터 끝까지 새 문자열을 하나씩 만들어가는 방식**이라면 리스트 변환이 필요 없다는 것을 확인했다.

### 핵심 확인 사항
- 문자열 인덱싱은 "읽기"만 가능하고 "쓰기(대입)"는 불가능하지만, 읽어온 값을 다른 변수에 저장하거나 다른 문자열에 이어붙이는 것은 자유롭다 — 즉 `revers_uppers[1] = 'b'`는 안 되지만 `x = revers_uppers[1]`이나 `result = result + revers_uppers[1]`은 아무 문제 없다.
- 문자열 뒤집기는 `[::-1]` 슬라이싱으로 간단히 처리 가능 (`palindrome_three_problems.md`에서 정리했던 슬라이싱 표의 "간격 -1이면 뒤집힘" 항목 재사용).

### 겪은 실수
- `full_chars`로 선언해놓고 반복문 안에서는 `full_char`(오타)를 사용해 에러 발생. 변수명 오타는 파이썬이 "존재하지 않는 새 변수"로 인식해버려서 에러 메시지만 봐서는 원인이 바로 안 드러났다.

### 최종 정답 코드 (변수명 정리 완료)
```python
def reverse_upper(s):
    uppercase_letters = ""
    for value in s:
        if 'A' <= value <= 'Z':
            uppercase_letters = uppercase_letters + value

    reversed_uppercase = uppercase_letters[::-1]
    result = ""
    count = 0

    for char in s:
        if 'A' <= char <= 'Z':
            result = result + reversed_uppercase[count]
            count = count + 1
        else:
            result = result + char

    return result
```

이 버전은 별도의 최적화 없이도 처음부터 깔끔했다 (인덱스를 불필요하게 만든 곳도, 이름이 애매한 변수도 없었음).

### 변수명에 대한 메모
- 함수 이름(`reverse_upper`)과 한 글자 차이인 변수명(`revers_uppers`)을 썼던 것은, 실제로 `full_char`/`full_chars` 오타 사고와 같은 종류의 실수를 유발할 수 있어 지양하기로 함 → `reversed_uppercase`로 변경.
- `char`(단수, 문자 하나) vs `letters`/`chars`(복수, 문자 여러 개) — 관례상 단/복수로 구분해서 이름 짓는다는 것을 확인. `letters`, `chars`, `string_list`, `char_list` 등이 모두 통용되는 이름이며 엄격한 규칙은 아님.

---

## 버전 3: 투 포인터 방식 — 시행착오 끝에 도달

`palindrome` 문제에서 썼던 "양 끝에서 다가가며 비교"하는 투 포인터 패턴을 적용해보려는 시도. 이 버전에서 방향을 잘못 잡은 채로 약 2시간을 소비했다.

### 막혔던 지점 1: "왼쪽/오른쪽이 각자 다른 속도로 전진"을 어떻게 표현할지
palindrome 문제와 달리 이 문제는 왼쪽 포인터와 오른쪽 포인터가 매 스텝 똑같이 한 칸씩 움직이며 항상 서로 대문자로 마주치는 게 아니다. "대문자가 아니면 계속 전진하다가, 대문자를 만나면 멈춘다"는 동작이 필요했는데, 이는 `for`문(매 반복 무조건 1씩 증가)으로는 표현할 수 없고 `while`문(조건이 참인 동안만 반복)이 필요하다는 것을 확인했다.

### 막혔던 지점 2 (방향을 잘못 잡은 원인): "안쪽 while 두 개 + 바깥 반복문" 구조로 접근
- `left` 전용 while, `right` 전용 while을 각각 조건을 만족할 때까지 따로 완주시킨 뒤, 그다음에 두 값을 교환하는 구조로 접근했다.
- 이 구조로 진행하다 "한 쌍을 교환하고 나면, 다음 바깥 반복에서 안쪽 while이 아예 실행되지 않는" 문제에 부딪혔다. 원인은 `left_count`/`right_count`가 대문자를 찾은 자리에서 멈춘 채로 남아있어서, 다음 반복에서도 "이미 대문자인 자리"이므로 while 조건이 처음부터 거짓이 되어 버렸기 때문.
- 이를 고치려고 값 할당(`if 'A'<=...: left = ...`)이 while 루프 몸통 안, if문에 조건부로 종속된 채로 있는 구조를 유지한 상태에서 인덱스를 한 칸 미리 이동시키는 시도도 해봤으나, 이 경우 "미리 이동한 자리가 이미 대문자인 경우" 값 할당 자체가 통째로 스킵되는 문제가 새로 생겼다.
- **이 시행착오의 실질적 원인:** "안쪽 while 두 개 + 바깥 반복문"이라는 구조 자체가 이 문제에 불필요하게 복잡한 접근이었다. 실제로 필요했던 것은 이중 반복 구조가 아니라, **하나의 while 안에서 매 반복마다 딱 한 가지 경우만 판단하고 넘어가는** 훨씬 단순한 구조였다.

### 최종 정답 코드 (최적/정석 풀이)
```python
def reverse_upper(s):
    letters = list(s)
    left = 0
    right = len(letters) - 1

    while left < right:
        if not ('A' <= letters[left] <= 'Z'):
            left = left + 1
        elif not ('A' <= letters[right] <= 'Z'):
            right = right - 1
        else:
            letters[left], letters[right] = letters[right], letters[left]
            left = left + 1
            right = right - 1

    result = ""
    for char in letters:
        result = result + char

    return result
```

### 메커니즘
매 바깥 반복(`while left < right`)마다 아래 세 가지 경우 중 **정확히 하나만** 처리하고 바로 다음 반복으로 넘어간다.
1. `left` 위치가 대문자가 아니면 → `left`만 한 칸 전진
2. (`left`가 대문자라면) `right` 위치가 대문자가 아니면 → `right`만 한 칸 후퇴
3. 둘 다 대문자면 → 그 자리에서 즉시 교환(튜플 언패킹 스왑, `palindrome` 브루트포스 버전에서 이미 쓴 문법)하고 `left`, `right`를 동시에 한 칸씩 좁힘

이렇게 "찾기"와 "교환"을 분리된 두 단계로 나누지 않고 한 바퀴 안에서 즉시 처리하기 때문에, 이전 시도에서 겪었던 "이미 처리한 자리에 머무는 문제" 자체가 구조적으로 발생하지 않는다.

### 검증
```
'asAsfBasVBN' -> 'asNsfBasVBA'
'aBCdEfG'     -> 'aGEdCfB'
'Hello World' -> 'Wello Horld'
'no upper here' -> 'no upper here'
''            -> ''
'A'           -> 'A'
'ABCD'        -> 'DCBA'
'aBCbbKddd'   -> 'aKCbbBddd'
```
전부 정확히 일치함을 확인했다.

### 왜 이 방식이 최적인가 (버전 1·2와 비교)
- **시간복잡도**: `left`, `right` 모두 각자 전체 구간을 한 번씩만 훑고 절대 되돌아가지 않으므로 O(n). 버전 1·2도 O(n)이라 시간복잡도상 우위는 없다.
- **공간복잡도**: 대문자만 따로 모아두는 별도 자료구조(`uppercase_letters`)가 필요 없어, 버전 1·2보다 메모리를 덜 사용한다.
- **구조의 단순함**: 순서를 추적하는 별도 카운터(`count`)나, 몇 번째 대문자인지 세는 로직 없이, 포인터 두 개의 위치만으로 전체 로직이 처리된다.

---

## 세 버전 비교 요약

| 버전 | 방식 | 시간복잡도 | 공간복잡도 | 상태 |
|---|---|---|---|---|
| 1. 리스트 변환 + 명단 재배치 | 대문자를 리스트에 모아 뒤집은 뒤 인덱스로 재배치 | O(n) | O(n) 추가 공간 | 정답 |
| 2. 문자열 전용 + 명단 재배치 | 버전 1과 동일 로직, 리스트 변환 없이 문자열만으로 | O(n) | O(n) 추가 공간 | 정답 |
| 3. 투 포인터 | 양 끝에서 좁혀가며 즉시 판단·교환 | O(n) | O(1) 추가 공간 | **정답, 최적** |

---

## 이 문제에서 얻은 교훈

1. **"찾기"와 "처리"를 억지로 두 단계(각각 완주 후 처리)로 분리하려 하면 오히려 로직이 복잡해질 수 있다.** 매 반복마다 "지금 당장 할 수 있는 판단 하나"만 내리고 다음으로 넘어가는 구조가, 두 단계를 분리해서 각각 끝까지 진행시키려는 구조보다 더 단순하고 안전한 경우가 있다는 것을 이번에 확인했다.
2. **가이드(설명)를 따라가다 막히면, 그 가이드 자체가 부정확하거나 불필요하게 복잡한 방향이었을 가능성도 있다.** 이번 문제에서 시행착오가 길어졌던 원인 중 하나는 처음에 제시된 접근 방향(이중 while 구조) 자체가 실제 최적해보다 복잡한 방향이었기 때문이었다. 막혔을 때 "내가 놓친 부분"뿐 아니라 "애초에 제시된 방향이 맞는가"도 함께 점검할 필요가 있다.
3. **난이도 체감과 실제 난이도가 다를 수 있다.** 문제 자체는 "브론즈 5"로 제시됐으나, 실제로 필요한 조건 분기(양쪽 포인터가 다른 속도로 움직이며 조건에 따라 갈라지는 로직)는 이전에 다룬 투 포인터 문제(palindrome)보다 한 단계 더 까다로웠다. 문제 난이도는 사전에 매겨진 값보다, 실제로 부딪혀보고 판단하는 것이 더 정확할 수 있다.
