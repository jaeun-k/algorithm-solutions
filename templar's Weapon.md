# 기사단원의 무기

## 문제 설명

기사단원의 수를 나타내는 정수 `number`와 이웃나라와 협약으로 정해진 공격력의 제한수치를 나타내는 정수 `limit`와 제한수치를 초과한 기사가 사용할 무기의 공격력을 나타내는 정수 `power`가 주어졌을 때, 무기점의 주인이 무기를 모두 만들기 위해 필요한 철의 무게를 return 하는 solution 함수를 완성하시오.

각 기사단원의 무기 공격력은 그 단원 번호(1부터 number까지)의 **약수의 개수**로 정해진다. 단, 약수의 개수가 `limit`을 초과하면 해당 기사는 정해진 `power`만큼의 공격력을 가진 무기를 대신 사용한다.

## 제한사항

- 1 ≤ number ≤ 100,000
- 2 ≤ limit ≤ 100
- 1 ≤ power ≤ limit

## 입출력 예

| number | limit | power | result |
|--------|-------|-------|--------|
| 5      | 3     | 2     | 10     |
| 10     | 3     | 2     | 21     |

### 입출력 예 설명

**예 #1**
1부터 5까지의 약수의 개수는 순서대로 [1, 2, 2, 3, 2]개. 모두 제한수치 3을 넘지 않으므로 필요한 철의 무게는 합인 10.

**예 #2**
1부터 10까지의 약수의 개수는 순서대로 [1, 2, 2, 3, 2, 4, 2, 4, 3, 4]개. 제한수치가 3이므로 6, 8, 10번 기사는 공격력 2(=power)인 무기를 구매. 따라서 합은 21.

## 풀이 코드

```python
def solution(number, limit, power):
    
    num = [0] * (number + 1)
    
    for i in range(1, number + 1):
        for j in range(i, number + 1, i):
            num[j] += 1
    
    total = 0
    for i in num:
        if i <= limit:
            total += i
        else:
            total += power
            
    return total
```

## 접근 방법

### 1. 시행착오 - 이중 for문으로 직접 약수 확인 (시간 초과)

```python
for i in range(1, number + 1):
    count = 0
    for j in range(1, i + 1):
        if i % j == 0:
            count += 1
```

각 숫자 `i`마다 1부터 `i`까지 전부 나눠보며 약수인지 확인하는 방식. 안쪽 루프가 `i`만큼 도는데, 이를 1부터 number까지 다 더하면 `1+2+3+...+n = n(n+1)/2`, 즉 **O(n²)**. `number`가 최대 100,000이면 연산량이 약 50억에 달해 시간 초과 발생.

### 2. 최적화 - 배수에 미리 카운트하는 방식 (에라토스테네스 체와 유사)

**핵심 발상의 전환**: "각 숫자의 약수가 무엇인지" 직접 찾는 대신, "각 숫자(약수 후보)가 누구의 약수가 되는지"를 거꾸로 생각한다.

예를 들어 2라는 숫자는 2, 4, 6, 8, 10, ... (2의 배수들)의 약수다. 그러므로 1부터 number까지 각 숫자 `d`를 약수 후보로 삼아, `d`의 배수들(`d, 2d, 3d, ...`)에게 "너의 약수 중 하나는 나(d)야"라고 카운트를 1씩 더해준다.

```python
num = [0] * (number + 1)   # num[i] = i의 약수 개수

for d in range(1, number + 1):              # d = 약수 후보
    for multiple in range(d, number + 1, d):  # d의 배수들에게
        num[multiple] += 1                     # 카운트 +1
```

**예시 (number=6)**

| d(약수 후보) | 카운트가 늘어나는 대상 |
|---|---|
| 1 | 1, 2, 3, 4, 5, 6 (전부) |
| 2 | 2, 4, 6 |
| 3 | 3, 6 |
| 4 | 4 |
| 5 | 5 |
| 6 | 6 |

최종 `num` = [0, 1, 2, 2, 3, 2, 4] → 각 인덱스가 해당 숫자의 약수 개수와 정확히 일치.

### 시간복잡도 분석

`d`가 배수를 도는 횟수는 `number/d`번이다. 이를 d=1부터 number까지 다 더하면:

```
n + n/2 + n/3 + ... + n/n = n × (1 + 1/2 + 1/3 + ... + 1/n)
```

괄호 안은 조화급수(harmonic series)로, n이 커질수록 대략 `log(n)`에 수렴한다. 따라서 전체 시간복잡도는 **O(n log n)**.

- O(n²): number=100,000일 때 약 100억 회 연산 → 시간 초과
- O(n log n): number=100,000일 때 약 170만 회 연산 → 충분히 빠름

### range의 세 번째 인자 (step)

```python
range(start, stop, step)
```

`range(d, number+1, d)`는 `d`부터 시작해서 `d`씩 건너뛰며 `number`까지의 값(=d의 배수들)만 뽑아낸다. 이 덕분에 나머지 연산(`%`)으로 배수 여부를 일일이 확인할 필요 없이, 애초에 배수인 값들만 순회할 수 있다.

## 배운 개념 정리

- **관점 전환**: "A가 B의 약수인가?"를 "B가 A의 배수인가?"로 뒤집어 생각하면 반복 구조를 효율화할 수 있음 (에라토스테네스의 체와 동일한 발상)
- **`range(start, stop, step)`**: step을 지정하면 특정 간격으로 값을 건너뛸 수 있음
- **등차수열의 합 공식**: `1+2+...+n = n(n+1)/2`
- **조화급수**: `1 + 1/2 + 1/3 + ... + 1/n ≈ log(n)` → O(n log n) 시간복잡도의 근거
- **리스트 인덱스와 길이 관계**: 인덱스 `k`까지 접근하려면 리스트 길이는 최소 `k+1`이어야 함 (0부터 시작하므로)
