# 비트 연산으로 두 지도 합치기 (비밀지도)

정수 배열로 인코딩된 두 개의 지도를 하나로 합치는 문제를 풀면서, 이진수 변환·`zip`·비트 연산·문자열 불변성이라는 네 가지 개념이 어떻게 하나의 문제 안에서 맞물리는지 정리한다.

---

## 문제: 두 지도 합치기

각 지도는 정수 배열로 주어지며, 배열의 각 원소를 `n`자리 이진수로 바꿨을 때 `1`은 벽, `0`은 빈 공간을 의미한다. 두 지도를 겹쳤을 때, 어느 한쪽이라도 벽이면 최종 지도에서도 벽으로 표시해야 한다. 결과는 벽을 `#`, 빈 공간을 공백(` `)으로 표시한 문자열 리스트로 반환한다.

```python
secret_map(4, [9, 20, 28, 18, 11], [30, 1, 21, 17, 28])
```

---

## 1차 접근: 이진수 문자열로 변환 후 자리별 비교

### 정수를 n자리 이진수 문자열로 바꾸는 함수

```python
def converter(num, n):
    if num == 0:
        return "0" * n
    result = ""
    while num > 0:
        result = str(num % 2) + result
        num //= 2
    if len(result) < n:
        result = (n - len(result)) * "0" + result
    return result
```

`num % 2`로 이진수의 각 자리를 뒤에서부터 구하고, 앞쪽에 이어 붙이는 방식이다. 여기서 주의할 점 하나: `num % 2`로 나머지만 구해서는 다음 반복으로 넘어갈 수 없고, 반드시 `num //= 2`로 **원래 변수 자체를 갱신**해야 한다. `num // 2`만 쓰고 재할당을 빼먹으면 `num` 값이 그대로라 반복문이 끝나지 않는다.

### 두 리스트를 짝지어 비교

```python
def secret_map(n, arr1, arr2):
    arr1_list = [converter(i, n) for i in arr1]
    arr2_list = [converter(i, n) for i in arr2]

    result = []
    for one, two in zip(arr1_list, arr2_list):
        answer = ""
        for i, j in zip(one, two):
            if i != j:
                answer += "#"
            elif i == "1":
                answer += "#"
            else:
                answer += " "
        result.append(answer)
    return result
```

---

## 핵심 개념 1: 튜플 순회와 `zip`의 차이

`for one, two in (arr1_list, arr2_list):` 는 두 리스트를 나란히 순회하는 코드처럼 보이지만 실제로는 그렇지 않다. `(arr1_list, arr2_list)`는 **원소가 2개인 튜플**이며, `for`문은 이 튜플의 원소(즉 리스트 자체)를 하나씩 꺼내 `one, two`에 언패킹하려 시도한다. 결과적으로 "두 리스트를 인덱스별로 짝짓기"가 아니라 "리스트 하나의 내부 원소 2개를 변수 2개에 나눠 담기"가 되어버려 의도와 전혀 다르게 동작한다.

`zip(a, b)`는 여러 개의 반복 가능한 객체를 **같은 인덱스끼리 짝지어 튜플로 묶어주는** 함수다.

```python
a = ['1', '2', '3']
b = ['a', 'b', 'c']
list(zip(a, b))   # [('1', 'a'), ('2', 'b'), ('3', 'c')]
```

길이가 다른 경우 짧은 쪽 기준으로 멈춘다. 두 리스트를 딕셔너리로 묶거나(`dict(zip(keys, values))`), 2차원 리스트를 전치(`zip(*matrix)`)하는 등 실무·코딩테스트 양쪽에서 반복적으로 등장하는 패턴이다.

---

## 핵심 개념 2: 논리 연산자 `or`와 비트 연산자 `|`

- `or`: True/False 값을 대상으로 하며, 결과도 True/False다.
- `|`: 숫자를 이진수로 놓고 **자릿수 하나하나를 비교**하며, 결과도 숫자다. 각 자리에서 둘 중 하나라도 1이면 1, 둘 다 0이면 0을 반환한다.

```
  0101   (5)
| 0011   (3)
------
  0111   (7)
```

이 문제에서 "두 지도 중 한쪽이라도 벽(1)이면 벽"이라는 조건은 정확히 비트 OR의 정의와 같다. 즉 자리별 비교 로직 전체를, 정수 상태에서 `a | b` 한 번으로 대체할 수 있다.

다른 비트 연산자로는 `&`(AND, 둘 다 1이어야 1), `^`(XOR, 서로 다르면 1), `~`(NOT, 비트 반전), `<<`/`>>`(시프트, 곱셈/나눗셈 2의 배수 효과)가 있다.

주의할 점은 `a | b`가 이진수 형태를 그대로 반환하는 게 아니라, 비트 연산 결과에 해당하는 **10진수 정수**를 반환한다는 것이다. 따라서 이 정수를 다시 이진수 문자열로 바꾸고, 자릿수(n)까지 맞춰주는 후처리가 별도로 필요하다.

---

## 핵심 개념 3: `bin()`과 슬라이싱, `zfill()`

`bin()`은 정수를 2진수 문자열로만 변환하는 함수다(다른 진수는 `oct()`, `hex()`처럼 별개의 함수를 쓴다). 결과 앞에는 "이진수"임을 표시하는 접두사 `'0b'`가 붙는다.

```python
bin(13)      # '0b1101'
bin(13)[2:]  # '1101'
```

`[2:]`는 진수를 바꾸는 문법이 아니라 문자열의 인덱스 2번부터 끝까지 잘라내는 **슬라이싱**이며, 여기서 콜론(`:`)은 "시작:끝" 범위를 나타내는 구분자다. 이 슬라이싱으로 `'0b'` 접두사만 제거하는 것이다.

접두사 없는 이진수 문자열이 필요하면 `format(num, 'b')`를 쓰면 슬라이싱 없이 바로 얻을 수 있다.

이렇게 얻은 이진수 문자열의 자릿수를 n에 맞추는 데는 `zfill(n)`을 쓴다. 문자열 길이가 n이 될 때까지 **왼쪽에 '0'만** 채워주며, 이미 n자리 이상이면 그대로 반환한다.

```python
"101".zfill(5)   # '00101'
```

`zfill`은 `rjust(n, '0')`의 0 채우기 전용 버전이다. 이 문제 외에도 시:분:초 포맷팅, 파일명 순번 맞추기, 학번·사번처럼 자릿수가 고정된 ID를 다룰 때 흔히 쓰인다.

---

## 핵심 개념 4: 문자열 불변성과 메서드 체이닝

파이썬 문자열은 불변(immutable)이라 `replace()`를 포함한 문자열 메서드는 전부 원본을 바꾸지 않고 **변경된 내용의 새 문자열을 반환**한다. 그래서 아래처럼 메서드를 연속으로 이어 쓰는 것(메서드 체이닝)이 가능하다.

```python
binary.replace('1', '#').replace('0', ' ')
```

첫 번째 `replace()`가 반환한 새 문자열에 대고 두 번째 `replace()`를 바로 호출하는 구조다. `replace(old, new)`는 한 번에 한 쌍만 치환하도록 설계되어 있어서, 두 종류의 문자를 각각 다른 문자로 바꾸려면 두 번 호출해야 한다.

체이닝이 항상 가능한 건 아니다. 기준은 "그 메서드가 값을 반환하는가, 아니면 원본을 직접 바꾸고 `None`을 반환하는가"이다. 예를 들어 `list.sort()`는 리스트를 제자리에서 정렬하고 `None`을 반환하므로 `lst.sort().sort()`는 에러가 난다. 원본을 바꾸지 않고 새 리스트를 반환하는 `sorted()`를 쓰면 이런 문제가 없다.

---

## 정리된 최종 풀이: `|` + `bin()` + `zfill()` + `replace()`

```python
def secret_map(n, arr1, arr2):
    answer = []
    for a, b in zip(arr1, arr2):
        merged = a | b
        binary = bin(merged)[2:].zfill(n)
        answer.append(binary.replace('1', '#').replace('0', ' '))
    return answer
```

이진수 문자열을 직접 만드는 `converter` 함수와 자리별 비교 로직을, 비트 OR 연산 한 줄과 문자열 내장 함수 조합으로 대체한 버전이다. 로직의 정확성이나 시간복잡도는 최초 접근과 동일하지만, "두 지도 중 하나라도 벽이면 벽"이라는 문제의 본질을 `|` 연산자로 직접 드러낸다는 점에서 더 간결하다.

---

## 대안 풀이: 비트를 직접 추출하며 문자로 변환

```python
def secret_map(n, arr1, arr2):
    result = []
    for a1, a2 in zip(arr1, arr2):
        merged = a1 | a2
        binary = ""
        for i in range(n):
            if merged % 2 == 0:
                binary = " " + binary
            else:
                binary = "#" + binary
            merged //= 2
        result.append(binary)
    return result
```

`bin()`, `zfill()`, `replace()` 같은 내장 도구를 전혀 쓰지 않고, "2로 나눈 나머지를 구하고 몫으로 갱신하는" 이진수 변환의 원리를 그대로 반복문에 풀어써서 **비트 판별과 문자 변환을 한 번에** 처리한다. `merged % 2`로 마지막 비트를 확인해 공백 또는 `#`을 앞쪽에 쌓고, `merged //= 2`로 다음 비트를 향해 이동한다.

---

## 세 가지 풀이 비교

| | 자리별 문자열 비교 (1차 접근) | `bin`+`zfill`+`replace` | 비트 직접 추출 |
|---|---|---|---|
| 핵심 도구 | 문자열 변환 함수 직접 구현, 자리별 조건문 | 내장 함수 조합 | 나머지·몫 연산 반복문 |
| 문제 본질 노출 | 자리별 비교 로직 안에 묻혀 있음 | `\|` 연산자로 명시적 | 비트 추출 과정에 녹아 있음 |
| 코드 길이 | 가장 김 | 가장 짧음 | 중간 |
| 내장 함수 의존도 | 낮음 | 높음 | 낮음 |
| 시간복잡도 | 동일 | 동일 | 동일 |

세 방식 모두 정답이며 속도 차이도 무의미한 수준이다. `bin`·`zfill`·`replace` 조합은 짧고 파이썬다운 코드를, 비트 직접 추출 방식은 내장 함수 없이도 비트 연산의 원리를 스스로 구현할 수 있다는 것을 보여준다.

---

## 이 문제에서 얻은 교훈

같은 문제라도 "정수 → 이진수 문자열 → 자리별 비교"라는 문자열 중심의 접근과, "정수 상태에서 비트 연산으로 먼저 합친 뒤 마지막에만 문자열로 변환"하는 비트 중심의 접근이 모두 가능했다. 두 접근의 차이는 정답 여부가 아니라, 문제의 조건(둘 중 하나라도 1이면 벽)이 이미 비트 연산자의 정의와 정확히 일치한다는 것을 알아채느냐에 있었다. 또한 `zip` vs 튜플 순회, 문자열의 불변성과 메서드 체이닝, `list.sort()`처럼 반환값이 `None`인 메서드의 존재처럼, 이 문제 하나를 계기로 여러 개의 독립적인 파이썬 기본기를 한 번에 짚어볼 수 있었다.
