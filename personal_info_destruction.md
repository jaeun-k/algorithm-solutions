# 파기해야 할 개인정보 찾기 (Personal Info Destruction Date)

날짜에 유효기간(개월 수)을 더해 만료일을 구하고, 오늘 날짜와 비교해 파기 대상을 골라내는 문제. 연/월/일을 따로 계산하다가 자리올림이 겹겹이 얽히는 함정에 부딪혔고, "날짜를 하나의 정수로 환산한다"는 발상으로 노선을 전환하면서 풀렸다. 이 문제를 통해 얻은 건 코드 자체보다 **학습 전략**에 관한 것이었다.

---

## 문제 설명

수집일과 약관별 유효기간(개월 수)이 주어질 때, 오늘 날짜 기준으로 파기해야 할(= 유효기간이 지난) 개인정보의 번호를 구한다. 모든 달은 28일까지 있다고 가정한다.

```python
today = "2022.05.19"
terms = ["A 6", "B 12", "C 3"]
privacies = ["2021.05.02 A", "2021.07.01 B", "2022.02.19 C", "2022.02.20 C"]
solution(today, terms, privacies)  # [1, 3]
```

---

## 시행착오: 연/월/일을 따로 계산하다가 자리올림이 겹겹이 얽힘

처음엔 수집일에 유효기간(개월 수)을 더해 만료일의 연/월/일을 각각 따로 계산하는 방식으로 접근했다.

```python
ad = int(prt_list[i][1]) + terms_dict[prt_list[i][-1]]
if ad > 12:
    prt_list[i][0] = int(prt_list[i][0]) + (ad // 12)
    prt_list[i][1] = ad % 12
```

여기서 두 가지 문제가 연쇄적으로 드러났다.

1. **월이 12를 여러 번 넘는 경우를 처리 못함**: 유효기간이 최대 100개월까지 가능하다는 제한사항 때문에, 월을 더한 값이 12를 여러 바퀴 넘을 수 있는데 `// 12`, `% 12`만으로는 "정확히 12의 배수"일 때(예: `ad=24`) 월이 0이 되어버리는 경계 오류가 있었다.
2. **일(day) 계산이 통째로 빠져있었음**: 만료일은 "수집일 + 유효기간 개월"의 그 날이 아니라 **하루 전**이어야 하는데(문제 예시: 1/5 수집, 12달 유효기간 → 만료일은 다음해 1/4), 이 "일에서 1을 빼는" 계산이 없었다. 게다가 일에서 1을 뺀 값이 0이 되면 전달 28일로 넘어가야 하고, 그 전달이 1월이면 연도까지 다시 건드려야 하는, **일→월→연도로 이어지는 자리내림의 연쇄**가 발생했다.

이 지점에서 "월까지는 그러려니 했는데, 일을 건드리다 다시 월과 연도까지 건드리게 되는" 구조 자체가 정석과는 다른 방향이라고 판단하고, 이 방식을 폐기했다.

---

## 노선 전환: 날짜를 하나의 정수(총 일수)로 환산

연/월/일을 따로 들고 다니며 자리올림을 그때그때 처리하는 대신, **날짜 전체를 하나의 숫자로 환산해서 계산**하는 방식으로 전환했다.

이 발상은 "진법은 모든 자리가 같은 기준으로 나뉘어야 한다"는 전제 때문에 처음엔 막혀 있었다. 이 전제를 "시:분:초(60진법·24진법이 섞인 혼합 진법)"라는 반례로 깨고 나서야, 날짜(연/월/일)도 같은 방식(자리마다 기준이 다른 혼합 진법)으로 다룰 수 있다는 데 도달했다.

이 문제는 "모든 달이 28일"이라는 가정이 있으므로 자릿값이 다음과 같이 정해진다.

- 1달 = 28일
- 1년 = 12달 × 28일 = 336일

```python
def converter(year, month, day):
    return int(year) * 336 + int(month) * 28 + int(day)
```

날짜를 이 함수로 환산하면, 만료일 계산은 "수집일(환산값) + 유효기간(개월) × 28"이라는 단순 덧셈이 되고, 오늘과의 비교도 정수 크기 비교 하나로 끝난다. 자리올림/자리내림을 손으로 처리할 필요가 사라졌다.

### "연/월/일을 이어붙인 정수(예: 20210502)"는 왜 안 되는가
날짜 두 개의 순서를 비교만 하는 용도로는 이어붙인 정수도 맞아떨어지지만, 여기에 개월 수를 더하는 연산에는 쓸 수 없다는 것도 확인했다. `20211201`에 `100`을 더하면 `20211301`처럼 존재하지 않는 "13월"이 나오는데, 이는 자릿값(10000, 100)이 실제 날짜의 자리올림 기준(12개월, 28일)과 무관하게 그냥 사람이 읽기 좋게 이어붙인 표기일 뿐이기 때문이다. 진짜 자릿값(336, 28)을 가진 환산만이 산술 연산에 안전하게 쓰일 수 있다.

### 비교 조건: 만료일이 오늘과 같은 날이면 파기 대상 아님
"수집일 + 유효기간"으로 계산한 값은 "만료일"이 아니라 "보관 가능한 마지막 날의 다음날"이라는 점도 검산 과정에서 확인했다(문제 예시: 1/5 수집, 12달 유효기간 → 이 값을 그대로 더하면 다음해 1/5가 나오지만, 실제 만료는 1/4까지). 따라서 비교 조건은 "수집일 환산값 + 유효기간 ≤ 오늘 환산값"이어야 하루 차이 오류 없이 정확히 판정된다.

---

## 최종 코드

```python
def converter(year, month, day):
    return int(year) * 336 + int(month) * 28 + int(day)

def solution(today, terms, privacies):
    result = []
    p = []
    terms_dict = {}
    today = today.split(".")
    today_days = converter(today[0], today[1], today[2])

    for privacy in privacies:
        p.append(privacy.replace(" ", ".").split("."))
    for term in terms:
        key, value = term.split(" ")
        terms_dict[key] = int(value)

    for i in range(len(privacies)):
        if converter(p[i][0], p[i][1], p[i][2]) + terms_dict[p[i][-1]] * 28 <= today_days:
            result.append(i + 1)
    return result
```

다른 사람들의 풀이(`datetime` 모듈 사용, `map(int, ...)` 사용 등)는 도구만 다를 뿐, "날짜를 하나의 통합된 정수로 환산해 비교한다"는 핵심 원리는 동일했다.

---

## 이 문제에서 얻은 교훈

### 날짜/시간 계산은 자리를 나눠서 계산하지 않는다
연/월/일(혹은 시/분/초)처럼 자리마다 최대값이 다른 값을 다룰 때, 각 자리를 따로 계산하고 넘치면 그때그때 옆자리를 보정하는 방식은 자리올림이 두 단계 이상 얽히는 순간 급격히 복잡해진다. 애초에 하나의 정수(총 일수, 총 초 등)로 환산해서 계산하고, 필요할 때만 다시 각 자리로 분해하는 방식이 이런 종류의 문제 전반에 적용되는 원칙이라는 것을 확인했다.

### "그럴듯한 연결"이 있는지가 혼자 풀지 답을 볼지의 기준이 된다
문제를 처음 마주쳤을 때 기존에 갖고 있던 지식·패턴·경험이 조합되어 "이렇게 하면 될 것 같다"는 실마리가 하나라도 잡히면, 시간을 들여 끝까지 밀어붙이는 쪽이 맞다 — 결국 뼈대는 스스로 나온다. 반면 문제가 요구하는 것이 새로운 도구(예: 날짜를 혼합 진법으로 보는 것) 자체이고, 기존에 가진 재료들을 아무리 재배열해도 그 방향이 후보에 조차 떠오르지 않는다면, 시간을 더 들여도 그 발상이 나올 확률은 낮다. 이 경우는 빠르게 답을 보되, 그 답을 그대로 베끼지 않고 "왜 이 자릿값을 쓰는지", "왜 다른 방식은 안 되는지"를 검증하며 흡수하는 쪽이 더 효율적인 학습이다.

이 판단은 문제를 처음 마주친 시점에 이미 상당히 신뢰할 만한 정확도로 서며, 조금 더 시도해본다고 크게 뒤집히는 경우는 드물었다.
