# 힙 자료구조와 "다시 넣기" 패턴 정리

스택·큐·DFS/BFS·DP까지 다룬 뒤 처음 배운 힙(heap)의 원리와, 더 맵게 문제를 풀며 겪은 시행착오를 정리한다.

---

## 힙 개념 정리

힙은 "지금까지 넣은 것들 중 최솟값(또는 최댓값)을 빠르게 꺼낼 수 있는" 자료구조다. 리스트에 넣고 매번 `min()`을 쓰면 O(n), 매번 정렬하면 O(n log n)인데, 힙은 삽입·삭제가 각각 O(log n)이라 "계속 넣으면서 그때그때 최솟값을 알아야 하는" 상황에서 유리하다.

파이썬은 `heapq` 모듈로 리스트를 힙처럼 다룬다. 기본은 최소힙이며, `heap[0]`이 항상 최솟값이라는 것만 보장한다(전체가 정렬된 상태는 아님).

```python
import heapq

heap = []
heapq.heappush(heap, 5)
heapq.heappush(heap, 1)
heapq.heappop(heap)   # 1
heap[0]                # 최솟값 조회 — 일반 인덱싱, heapq 함수 아님
```

**힙에서 log n이 나오는 원리**: 힙은 이진트리 구조라, 한 층마다 원소 수가 2배로 늘어난다. K개를 담은 트리의 높이가 log K가 되고, push/pop은 이 트리 높이만큼만 이동(원소를 부모와 비교하며 오르내림)하면 되므로 O(log K)다. 반면 정렬(병합정렬 기준)은 n개를 절반씩 나누는 데 log n단계가 걸리고, 각 단계마다 전체 n개를 훑어 합치므로 O(n log n)이 된다. 둘 다 log가 등장하지만 "몇 번 쪼개야 1이 되나"(정렬)와 "K개를 담으려면 트리가 몇 층이어야 하나"(힙)라는 서로 다른 질문에서 나온다.

**`len()`은 O(1)이다.** 리스트·힙 등은 원소 개수를 내부적으로 별도 카운터로 들고 있어서, 매번 세지 않고 바로 값을 반환한다.

---

## 문제: 더 맵게 (반복적으로 최솟값 두 개를 뽑아 재투입하기)

스코빌 지수가 가장 낮은 두 개의 음식을 `첫번째 + 두번째*2` 공식으로 계속 섞어서, 모든 음식의 스코빌 지수를 K 이상으로 만드는 데 필요한 최소 섞기 횟수를 구한다. 불가능하면 -1을 반환한다.

```python
solution([1,2,3,9,10,12], 7)   # 2
```

### 최종 코드
```python
from heapq import heapify, heappush, heappop

def solution(scoville, K):
    heapify(scoville)
    count = 0
    while scoville[0] < K:
        if len(scoville) < 2:
            return -1
        first = heappop(scoville)
        second = heappop(scoville)
        heappush(scoville, first + second * 2)
        count += 1
    return count
```

### 시행착오와 정리한 개념

**섞은 결과를 별도 변수(`total`)에 담아 재활용하려다가, 나머지 원소들과의 비교가 끊겨서 틀렸다.** `total`을 힙 밖에 따로 들고 있으면, 그 값이 힙 안의 다른 원소들과 더 이상 비교 대상에 들지 못한다. `[1,2,3,4,5,6]`, K=10 같은 반례에서, 아직 안 섞인 더 작은 원소들이 남아있는데도 "방금 만든 total 하나가 K를 넘었다"는 이유만으로 잘못 종료됐다. 힙 문제에서 "이번에 만든 결과도 다음 비교에 반드시 참여해야 하는 구조"라면, 그 결과를 조건 없이 다시 힙에 넣는 것이 선택이 아니라 필수다.

**"결과값이 K 이상이면 굳이 다시 넣지 않아도 되지 않을까"라는 시도도 같은 이유로 틀렸다.** 조건부로 push를 생략하면 그 음식이 최종 목록에서 통째로 사라진 것처럼 취급되어, 다른 원소와 다시 섞일 기회 자체가 차단된다. 게다가 마지막 두 개를 뽑아 만든 결과를 안 넣으면 힙이 완전히 빈 상태에서 `scoville[0]`을 조회해 인덱스 에러가 난다. "K 이상이 된 음식도 여전히 전체 목록의 일부"라는 문제 조건상, 무조건 다시 넣어야 한다.

**힙을 안 쓰고 total을 직접 관리하며 풀 수도 있지만, 그러면 힙을 쓰는 의미가 없어진다.** total과 힙에 남은 원소들을 매번 직접 비교하려면 결국 힙이 내부적으로 하는 최솟값 관리를 손으로 재구현하는 셈이라, 코드는 더 복잡해지고 시간복잡도상 이점도 사라진다.

**시간복잡도 O(n log n), 공간복잡도 O(n)이 이 문제의 이론적 최선이다.** "반복적으로 최솟값 두 개를 뽑아 재투입"하는 구조 자체가 힙(또는 정렬) 없이는 더 빠르게 풀 수 없고, `heapify`가 원본 배열을 그대로 재사용(in-place)하므로 추가 공간도 최소한이다.

**"값이 커서 계산이 느려질 것"이라는 추측은 이 문제엔 해당하지 않는다.** 파이썬 정수 연산은 10억 수준에서는 자릿수와 무관하게 거의 일정한 속도다. 에라토스테네스의 체 등에서 "나머지 출력"을 요구하는 이유는 계산 속도 때문이 아니라, 값이 반복적으로 곱해지며 수백~수천 자리까지 폭발적으로 커지는 것을 막기 위한 모듈러 연산이었다. 이 문제는 덧셈 위주라 값이 완만하게만 커져서 애초에 그런 상황이 발생하지 않는다. 또한 나눗셈으로 값을 미리 축소하는 시도는 정수 나눗셈에서 나머지가 버려지며 대소관계가 뭉개져 오답 위험만 키운다.

**`heapq.heappush`/`heapq.heappop`처럼 매번 모듈명을 붙이는 게 번거로우면 `from heapq import heappush, heappop`으로 이름만 가져와 쓸 수 있다.** 성능 차이는 미미하지만 가독성과 타이핑 편의성이 좋아진다.

---

## 이 문제에서 얻은 교훈

**힙이라는 도구 자체보다 "결과를 다시 통합된 후보 풀에 편입시키는가"가 이 문제의 진짜 핵심이었다.** 힙의 O(log K) 연산은 "왜 빠른가"를 설명할 뿐, "어떻게 풀어야 하는가"는 새로 만든 값을 원본 데이터와 차별 없이 계속 순환시키는 구조에 달려 있었다.

**조건부로 값을 저장하지 않으면, 그 정보가 필요한 시점에 이미 사라져 있다.** "이 값은 이미 조건을 만족했으니 굳이 남길 필요 없다"는 판단이 오히려 최종 결과의 일부를 누락시키는 원인이 됐다.

**"숫자가 크다"는 사실만으로 최적화 방향을 잡기 전에, 그 숫자가 실제로 어떤 연산에 어떻게 쓰이는지부터 확인해야 한다.** 같은 "큰 수"라도 가만히 비교만 되는 경우와 반복적으로 곱해져 폭발적으로 커지는 경우는 대응 방법이 전혀 다르다.
