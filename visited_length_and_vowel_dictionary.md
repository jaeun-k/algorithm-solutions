# 경로 정규화와 백트래킹 2문제 정리

리스트 참조 함정에서 튜플/set으로 정제해나간 방문 길이 문제와, 매개변수 전달 방식으로 백트래킹을 단순화한 모음 사전 문제를 정리한다.

---

## 문제 1: 방문한 길이 (경로를 방향 없이 정규화하기)

캐릭터가 이동한 경로 중, 처음 걸어본 길(구간)의 길이를 구한다. 좌표평면 경계를 벗어나는 이동은 무시하며, 이미 지나간 길을 반대 방향으로 되돌아가도 새로운 길로 세지 않는다.

```python
solution("ULURRDLLU")   # 7
solution("UUD")          # 2 (U,U로 갔다가 D로 되돌아가는 구간은 중복)
```

### 최종 코드
```python
def solution(dirs):
    record = set()
    d = {"U":(0,1), "D":(0,-1), "R":(1,0), "L":(-1,0)}
    current = (0,0)

    for i in dirs:
        x, y = d[i]
        nx, ny = current[0]+x, current[1]+y
        if -5 <= nx <= 5 and -5 <= ny <= 5:
            record.add(tuple(sorted([current, (nx, ny)])))
            current = (nx, ny)

    return len(record)
```

### 시행착오와 정리한 개념

**리스트를 기록용 컨테이너에 그대로 넣으면 참조 공유 문제가 생긴다.** `record.append(start)`처럼 원본 리스트 객체를 그대로 추가하면, 그 이후 `start`가 계속 바뀌면서 `record` 안의 모든 기록이 동시에 오염된다. `start[:]`(복사본)를 넣거나 애초에 불변 타입(튜플)을 쓰면 해결된다.

**set에는 리스트를 넣을 수 없다.** set과 딕셔너리 키는 "해시 가능한(hashable)" 값만 허용하는데, 리스트는 가변(mutable)이라 해시 계산이 불안정해 `TypeError: unhashable type`이 난다. 튜플은 불변이라 가능하다. 단, 튜플 안에 리스트가 섞여 있으면(`(0,0,[0,0])`) 그 튜플도 다시 해시 불가능해진다.

**"길에는 방향이 없다"는 조건이 이 문제의 핵심 함정이다.** A→B로 간 길과 B→A로 되돌아온 길은 같은 길이므로, 이동 전/후 좌표 쌍을 저장할 때 정방향과 역방향을 모두 확인하거나(`(current,before) not in record and (before,current) not in record`), 애초에 저장 시점에 `tuple(sorted([before, current]))`처럼 순서를 정규화하면 조회를 한 번만 해도 된다.

**"사전 검증 후 실행"이 "실행 후 사후 검증"보다 안전한 패턴이다.** 시행착오 버전은 이동을 축(x, y)별로 따로 실행한 뒤 `current != before`로 "결과적으로 움직였는지"를 사후 확인해야 했다. 반면 `nx, ny`(이동 후 좌표 후보)를 먼저 계산해 범위를 통째로 검증하고, 통과했을 때만 실행하는 방식은 그 블록에 진입한 것 자체가 이미 "유효한 이동"임을 보장하므로 사후 확인이 필요 없다.

**튜플 대입은 항상 새 객체를 만드는 것이라 복사가 불필요하다.** `current = (current[0]+x, current[1])`는 기존 튜플을 수정하는 게 아니라 완전히 새로운 튜플을 만들어 재할당하는 것이므로, `before = current`로 참조를 넘겨도 이후 `current`가 바뀌어도 `before`는 안전하다(리스트였다면 반드시 복사가 필요했을 상황).

---

## 문제 2: 모음 사전 (백트래킹 + 조기 종료)

모음 5개로 만들 수 있는 길이 5 이하의 모든 문자열을 사전순으로 나열했을 때, 주어진 단어가 몇 번째인지 구한다.

```python
solution("AAAAE")  # 6
solution("I")       # 1563
```

### 최종 코드
```python
def solution(word):
    words = ["A","E","I","O","U"]
    found = False
    count = 0
    result = []

    def backtrack(current, length):
        nonlocal count, found
        if found:
            return
        if current == word:
            result.append(count)
            found = True
        count += 1
        if length == 5:
            return
        for w in words:
            backtrack(current + w, length+1)

    backtrack("", 0)
    return result[0]
```

### 시행착오와 정리한 개념

**`nonlocal`과 매개변수 이름이 겹치면 에러가 난다.** `def backtrack(current): nonlocal current`처럼 같은 이름을 매개변수와 nonlocal 대상으로 동시에 쓰면, 파이썬이 그 이름을 지역변수로 볼지 외부변수로 볼지 판단하지 못해 충돌한다.

**`current`를 매개변수로 넘기는 방식으로 바꾸면 되돌리기 로직이 사라진다.** `backtrack(current + w, length+1)`처럼 매번 새 문자열을 만들어 다음 호출에 넘기면, 각 재귀 호출은 자기만의 독립된 `current`를 갖는다. 그 값은 해당 호출이 살아있는 동안만 존재했다가 함수가 끝나면 사라지는 "유령" 같은 성격을 갖는다. 문자열은 불변이라 리스트의 `.pop()` 같은 되돌리기 메서드가 없는데, 이 방식에서는 애초에 되돌릴 필요가 없어 그 부재가 문제되지 않는다.

**"방문 상태 저장(record/visited)"이 필요한지는 트리인지 그래프인지로 판단한다.** 이 문제는 매번 새 글자를 덧붙이기만 하는 트리 구조라 같은 문자열이 두 경로로 만들어질 수 없다. 즉 "이 상태에 도달하는 경로가 항상 유일한가"가 방문 체크 필요 여부를 가르는 기준이며, 네트워크·최단거리 같은 그래프형 문제와 달리 이 문제는 트리형이라 방문 체크를 통째로 제거해도 정답이 유지되고 메모리 사용이 크게 줄었다.

**조기 종료는 `nonlocal` 플래그가 이 수준에서는 가장 직관적이다.** 재귀 호출이 값을 직접 반환해 조합하는 방식(`return count, found` 형태)으로도 조기 종료를 구현할 수 있지만, return 지점이 여러 곳으로 늘어나고 튜플 언패킹이 필요해 오히려 복잡도가 올라간다. `found` 플래그 하나로 "이후 모든 호출이 즉시 return"하게 만드는 방식이 return이 한 곳뿐이라 가독성이 더 낫다.

**카운트 오프셋은 손으로 검증해야 확신할 수 있다.** `count += 1`이 "빈 문자열(`""`)"부터 실행되기 때문에, 빈 문자열이라는 존재하지 않는 상태가 실제로는 "0-indexed를 1-indexed로 보정"하는 역할을 한다. `"A"`, `"AA"` 같은 구체적 사례를 손으로 추적해야 이 상쇄가 의도한 대로 작동함을 확신할 수 있다.

---

## 이 두 문제에서 얻은 교훈

### 리스트-튜플-set의 관계는 "속도"가 아니라 "가변성과 용도"로 판단한다
값이 나중에 바뀌면 안 되거나 set/딕셔너리 키로 써야 한다면 튜플, 계속 값을 추가·삭제해야 한다면 리스트를 쓴다. "튜플이 더 빠르다"는 식의 성급한 일반화는 오히려 잘못된 적용으로 이어질 수 있다.

### 트리와 그래프를 구분하는 것이 방문 체크 여부를 결정한다
"이 상태에 도달하는 경로가 유일한가"를 문제를 읽는 시점에 먼저 판단해야 한다. 순열·조합·사전순 나열처럼 매번 새 선택을 계속 덧붙이는 구조는 대개 트리형이라 방문 체크가 불필요하고, 격자·인접 관계를 다루는 그래프형 문제는 방문 체크가 필수다.

### "값을 공유하며 되돌리는 방식"과 "매번 새 값을 만들어 넘기는 방식"은 트레이드오프가 있다
전자(리스트 + append/pop, nonlocal 상태 공유)는 메모리를 아끼지만 되돌림 로직이 필요해 복잡해지고, 후자(매개변수로 새 값 전달)는 되돌림이 필요 없어 코드가 단순해지지만 매번 새 객체를 생성하는 비용이 든다. 문제 크기가 작으면 후자의 단순함이 실질적으로 더 유리하다.

### 완벽히 "깔끔한" 형태보다 지금 이해되는 형태가 우선이다
`nonlocal` 플래그 방식이 다소 거슬려도, 대안(반환값 조합 방식)이 오히려 더 추상적이고 return이 많아진다면, 지금 이해 가능한 형태를 최종안으로 삼는 것이 실전적으로 옳은 선택이다.
