# 돗자리: 2D 부분합(Prefix Sum)으로 영역 조회를 O(1)로

완전탐색으로는 통과하지만, "영역이 비었는가"를 매번 순회로 확인하는 대신 O(1)에 답하는 정석 기법을 처음부터 끝까지 손으로 검증하며 익힌 문제. 이해에 도달하기까지의 과정 자체가 중요했던 문제라 그 과정도 함께 기록한다.

---

## 문제

여러 크기의 정사각형 돗자리 중, 사람이 없는 자리에 깔 수 있는 가장 큰 돗자리의 한 변 길이를 구한다.

```python
solution([5,3,2], park)  # 3
```

---

## 풀이 1: 완전탐색 (5중 for문)

```python
def solution(mats, park):
    answer = -1
    for mat in mats:
        for y in range(len(park)-mat+1):
            for x in range(len(park[0])-mat+1):
                count = 0
                valid = True
                for i in range(mat):
                    for j in range(mat):
                        if park[y+i][x+j] == "-1":
                            count += 1
                        else:
                            valid = False
                            break
                    if not valid:
                        break
                if count == mat**2 and mat > answer:
                    answer = mat
    return answer
```
정답이고, 이 문제 제한사항(park ≤ 50×50) 안에서는 시간 초과 없이 통과한다.

### 다듬은 점
- `count`(개수를 세어 `mat**2`와 비교)는 불필요했다. `valid`가 하나라도 틀리면 즉시 `False`로 떨어지는 상태 플래그이므로, `valid`가 끝까지 `True`라는 것 자체가 이미 "완전히 비었다"는 뜻과 동치다. **"몇 개가 맞았는가"를 세는 것보다, "지금까지 실패한 적이 있는가"라는 boolean 하나로 관리하는 것이 거의 항상 더 간단하다.**
- `mats`를 큰 것부터 정렬하고, 맞는 즉시 `return`하면 불필요한 나머지 탐색을 생략할 수 있다.

```python
def solution(mats, park):
    for mat in sorted(mats, reverse=True):
        for y in range(len(park)-mat+1):
            for x in range(len(park[0])-mat+1):
                valid = True
                for i in range(mat):
                    for j in range(mat):
                        if park[y+i][x+j] != "-1":
                            valid = False
                            break
                    if not valid:
                        break
                if valid:
                    return mat
    return -1
```

---

## 풀이 2: 2D 부분합 (정석)

완전탐색의 병목은 "이 영역이 비었는가"를 확인할 때마다 그 영역(`mat×mat`칸)을 매번 순회하는 부분이다. 이걸 미리 계산해둔 표에서 O(1)에 조회하도록 바꾸는 것이 부분합의 역할이다.

```python
def solution(mats, park):
    y_len, x_len = len(park), len(park[0])

    occ = [[0]*x_len for _ in range(y_len)]
    for i in range(y_len):
        for j in range(x_len):
            occ[i][j] = 0 if park[i][j] == "-1" else 1

    prefix = [[0]*(x_len+1) for _ in range(y_len+1)]
    for i in range(1, y_len+1):
        for j in range(1, x_len+1):
            prefix[i][j] = occ[i-1][j-1] + prefix[i-1][j] + prefix[i][j-1] - prefix[i-1][j-1]

    for mat in sorted(mats, reverse=True):
        for i in range(mat, y_len+1):
            for j in range(mat, x_len+1):
                total = prefix[i][j] - prefix[i-mat][j] - prefix[i][j-mat] + prefix[i-mat][j-mat]
                if total == 0:
                    return mat
    return -1
```

### 왜 이렇게 되는가 — 핵심 통찰

- **애초에 "배치"는 중요하지 않다.** 돗자리를 깔 수 있는 조건은 "그 영역이 완전히 비어있는가"라는 이진 질문뿐이다. 사람이 몇 명, 어떤 모양으로 있든 "1명 이상 있으면 못 깐다"는 결론은 동일하므로, "합이 0인가 아닌가"만으로 배치와 무관하게 완벽히 판별된다.
- **`prefix[i][j]`는 항상 "(0,0)부터 그 좌표까지, 오른쪽/아래로 그어지는 큰 사각형 전체의 누적합"이다.** 개별 숫자 하나는 그 자체로는 아무 의미가 없다 — 반드시 정해진 4개를 조합해야만 특정 영역의 합이 나온다.
- 임의의 사각형 영역 합은 **"오른쪽 아래 큰 사각형에서, 왼쪽 띠와 위쪽 띠를 빼고, 두 번 빠진 왼쪽 위 겹침 부분을 다시 더한다"**로 구해진다:
  ```
  total = prefix[y+size][x+size] - prefix[y][x+size] - prefix[y+size][x] + prefix[y][x]
  ```
  1차원 누적합에서 "구간 합 = 끝까지의 합 - 시작 전까지의 합"이었던 것과 원리는 같고, 2차원은 "빼야 할 앞부분"이 위/왼쪽 두 방향으로 나뉘어 겹치는 부분에 대한 보정(+1항)이 추가된다.
- **`prefix` 표는 원본과 별개의, 가로세로 한 칸씩 더 큰 배열로 만들어야 한다** (맨 위 행·왼쪽 열은 항상 0인 여백). 원본을 그대로 덮어쓰면 이 여백을 만들 공간이 없어 인덱스가 꼬인다.
- **좌표 밀림의 정확한 의미**: `occ`(칸)와 `prefix`(경계선)는 다른 좌표 체계다. `occ`에서 "0번 인덱스부터 mat-1번 인덱스까지"가 "길이 mat"인 칸들인데, 이 칸들을 감싸는 오른쪽 아래 경계선은 `mat`(마지막 인덱스+1)이 된다. 따라서 `prefix`에서 mat 크기 영역을 조회할 때는 `range(mat, y+1)`이 맞고, 여기에 한 번 더 밀어서 `range(mat+1, y+1)`로 하면 밀림을 이중으로 적용하는 잘못된 범위가 된다.
- **표를 만들 때도 이전 계산 결과를 재활용해야 한다.** `prefix[i][j]`를 구할 때 `occ`의 해당 영역을 매번 처음부터 다 더하면(예: `sum(occ[a][b] for ...)`) 표를 만드는 과정 자체가 다시 O(n⁴) 수준으로 느려진다. `prefix[i-1][j] + prefix[i][j-1] - prefix[i-1][j-1]`처럼 이미 계산해둔 더 작은 사각형의 결과를 가져다 쓰고, 새로 추가된 한 칸(`occ[i-1][j-1]`)만 얹어야 표 전체를 O(칸 수)에 만들 수 있다.

---

## 이 문제에서 얻은 교훈

- 여러 번 반복되는 이진 판정("성공/실패")은 개수를 세어 나중에 비교하는 것보다, boolean 상태 플래그로 관리하는 것이 항상 더 단순하다.
- "영역의 배치가 어떻든" 결론이 바뀌지 않는 조건(완전히 비어야만 성공)이라면, 배치 정보를 버리고 "합이 0인가"라는 단일 값만으로 판별할 수 있다.
- 부분합 표의 개별 값은 의미가 없고, 정해진 4개의 조합만이 의미를 가진다 — "0,0에서부터의 누적"이라는 하나의 일관된 정의를 공유하기 때문에 이 조합이 항상 성립한다.
- 표를 채우는 점화식(`prefix[i-1][j] + prefix[i][j-1] - prefix[i-1][j-1] + 신규값`)은 "이전 결과를 재활용"하는 것이 핵심이며, 이걸 생략하고 매번 원본을 처음부터 다시 더하면 표를 만드는 과정 자체가 다시 느려진다.
- "칸(원소)"과 "경계선(누적합 좌표)"은 서로 다른 좌표 체계이고, 이 둘을 헷갈리면 정확히 한 칸씩 어긋난 범위 오류가 난다.
- 이 정도 난이도부터는 "이해했다"와 "머릿속에서 전체가 하나로 붙어 자유롭게 다룰 수 있다"는 것이 다르다. 완전히 막막할 때는, 무작정 더 설명을 듣기보다 "지금 정확히 어느 지점에서, 왜 막히는지"를 스스로 좁혀나가는 것이 훨씬 효과적이었다 — 예: "숫자 하나만으로는 판단이 안 되는데 왜 4개 조합은 되는가"처럼 구체적인 반문을 던진 지점들에서 실제로 이해가 진전됐다.
- 손으로 직접 조건을 바꿔보고(부등호를 바꿔본다, 인덱스를 하나 밀어본다) 그 결과가 왜 깨지는지 확인하는 과정이, 설명을 듣는 것보다 체화에 훨씬 효과적이었다.
- 난이도가 오르면 한 문제에 들어가는 요소 수(변환, 여백, 좌표 계산, 탐색)가 늘어나면서 학습 속도 자체가 느려지는 구간에 들어서게 된다. 이 구간에서는 이전 난이도의 진행 속도를 기준으로 스스로를 평가하지 않는 것이 필요하다.
- 시간이 제한적인 상황에서는, 트레이싱과 반례 검증 등 할 수 있는 시도를 다 해봤는데도 완전히 체화된 느낌이 들지 않는다면, 그 상태로 일단 넘어가고 이후 비슷한 유형을 더 만나면서 자연스럽게 이해도가 올라가길 기다리는 것이 합리적이다.
- 등장 빈도가 낮은 도구(2D 부분합 등)에 대해서는 "존재를 알고 원리를 한 번 검증해본" 정도로 충분하며, 완벽한 숙달보다는 더 자주 나오는 패턴에 우선순위를 두는 것이 현실적이다.
