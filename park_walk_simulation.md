# 그리드 위에서 한 칸씩 이동하며 검증하기 (공원 산책)

격자 위의 로봇이 명령에 따라 이동하되, 경로 중간에 장애물이나 경계를 만나면 그 명령 전체를 무시해야 하는 문제를 풀며 정리한 접근법과, 4방향 분기를 하나로 압축하는 방법을 기록한다.

---

## 문제: 명령 하나가 막히면 그 명령 전체를 취소

로봇의 시작 위치(`S`)에서 출발해, `"E 5"`(동쪽으로 5칸)처럼 방향과 칸수로 된 명령을 순서대로 수행한다. 이동 도중 공원 경계를 벗어나거나 장애물(`X`)을 만나면, **이미 이동한 칸이 있어도 전부 취소하고 명령 수행 전 위치로 되돌아간 뒤** 다음 명령으로 넘어간다.

---

## 접근 방식: 한 번에 점프하지 않고 한 칸씩 이동하며 즉시 검증

목적지 좌표를 한 번에 계산해서 이동하는 방식은, 그 경로 중간에 장애물이 있었는지를 **별도로 다시 훑어야 하는 반복문이 어차피 필요**해서 오히려 로직이 두 겹으로 복잡해진다. 반면 "한 칸 이동 → 그 자리에서 즉시 유효한지 확인 → 문제없으면 계속, 문제 있으면 그 자리에서 멈추고 되돌림"은 이동과 검증이 하나로 합쳐져 있어 구현이 단순해진다. 이 방식은 장애물이 있는 그리드에서 경로를 확인하며 이동하는 문제 전반에서 표준적으로 쓰이는 패턴이다.

### 초기 버전: 4방향을 각각 분기로 처리

```python
def solution(park, routes):
    width = len(park[0])
    length = len(park)
    for i in park:
        if "S" in i:
            c = park.index(i)
            break
    d = park[c].index("S")
    yx = [c, d]

    for i in routes:
        count = 0
        if i[0] == "E":
            for i in range(int(i[-1])):
                if yx[1]+1 < width and park[yx[0]][yx[1]+1] != "X":
                    yx[1] += 1
                    count += 1
                else:
                    yx[1] -= count
                    break
        elif i[0] == "W":
            for i in range(int(i[-1])):
                if yx[1]-1 >= 0 and park[yx[0]][yx[1]-1] != "X":
                    yx[1] -= 1
                    count += 1
                else:
                    yx[1] -= count   # 버그: 이동은 -=1인데 되돌림도 -=count로 남아있음
                    break
        elif i[0] == "S":
            for i in range(int(i[-1])):
                if yx[0]+1 < length and park[yx[0]+1][yx[1]] != "X":
                    yx[0] += 1
                    count += 1
                else:
                    yx[0] -= count
                    break
        elif i[0] == "N":
            for i in range(int(i[-1])):
                if yx[0]-1 >= 0 and park[yx[0]-1][yx[1]] != "X":
                    yx[0] -= 1
                    count += 1
                else:
                    yx[0] += count
                    break
    return yx
```

### 발견된 버그: 복붙하면서 부호를 안 바꿔줌

동서남북 네 블록은 서로 구조가 거의 같아서 복사 붙여넣기로 작성하기 쉬운데, 그 과정에서 `W`(서쪽) 블록의 되돌리기 로직에 `E`(동쪽) 블록의 부호(`-= count`)가 그대로 남아 있었다. `E`는 이동 시 `+= 1`이므로 되돌릴 때 `-= count`가 맞지만, `W`는 이동 시 `-= 1`이므로 되돌릴 때는 `+= count`여야 한다. 이동 방향과 되돌리는 방향은 항상 반대 부호로 짝을 이뤄야 하는데, 그 짝을 맞추는 걸 놓친 것이다.

이 버그는 랜덤 테스트로 확인했다.
```
park = ["OS"], routes = ["W 3"]
→ 버그 있는 코드: [0, -1]  (배열 범위를 벗어난 잘못된 좌표)
→ 정답: [0, 1]  (경계에 막혀 원위치)
```
부호를 고친 뒤 랜덤 케이스 5,000건으로 검증해 통과를 확인했다.

---

## 4방향 분기를 하나로 압축하기

네 블록이 길어진 근본 원인은 "어느 축을, 어느 부호로 바꿀지"를 매번 조건문으로 판단했기 때문이다. 방향 문자를 애초에 "행 변화량, 열 변화량"이라는 숫자 쌍으로 미리 번역해두면, 실행 코드는 방향에 상관없이 단 하나만 있으면 된다.

```python
def solution(park, routes):
    length = len(park)
    width = len(park[0])

    for row, line in enumerate(park):
        if "S" in line:
            yx = [row, line.index("S")]
            break

    move = {"N": (-1, 0), "S": (1, 0), "W": (0, -1), "E": (0, 1)}

    for route in routes:
        op, n = route.split()
        n = int(n)
        dy, dx = move[op]

        count = 0
        for _ in range(n):
            yx[0] += dy
            yx[1] += dx
            count += 1

            if yx[0] < 0 or yx[0] >= length or yx[1] < 0 or yx[1] >= width or park[yx[0]][yx[1]] == "X":
                yx[0] -= dy * count
                yx[1] -= dx * count
                break

    return yx

방향별로 4번 반복되던 분기와, 부호 실수가 났던 되돌리기 로직이 모두 사라졌다. 이동은 실제 위치(`yx`)가 아니라 임시 좌표(`cur_row, cur_col`)에서 시험 삼아 진행되고, `n`칸 전부 성공했을 때만 `yx`에 반영되므로 "되돌린다"는 개념 자체가 필요 없어진다.

---

## 핵심 개념 정리

### 시작 위치를 찾을 때 `enumerate`가 `list.index()`보다 안전한 이유

```python
for i in park:
    if "S" in i:
        c = park.index(i)   # i와 값이 같은 첫 번째 원소의 위치를 찾음
        break
```
`park.index(i)`는 리스트에서 `i`와 **값이 같은 첫 번째 원소의 위치**를 찾는 것이라, 만약 같은 문자열이 여러 번 등장하면 실제로 지금 보고 있는 위치가 아니라 그 값이 처음 등장한 위치를 반환할 수 있다. 이 문제는 시작 지점이 하나만 주어진다는 조건 덕분에 실질적으로 안전하지만, 일반적으로는 순회하면서 인덱스 자체를 직접 받는 `enumerate()`를 쓰는 편이 더 안전하고 명확하다.

```python
for row, line in enumerate(park):
    if "S" in line:
        ...
```

### 명령 문자열을 나눌 때 슬라이싱보다 `split()`이 안전한 이유

```python
i[0]    # 방향 문자
i[-1]   # 이동 칸수 (마지막 한 글자만)
```
이 방식은 이동 칸수가 항상 한 자리 숫자일 때만 정확하다. `i[-1]`은 문자열의 마지막 글자 하나만 가져오므로, 칸수가 두 자리 이상이면 잘못된 값을 읽는다. `route.split()`으로 공백 기준 나누면 칸수 자릿수와 무관하게 항상 정확하게 분리된다.

### `for...else` 문법

`for`문 뒤에 붙는 `else`는 그 반복문이 `break`로 중간에 끊기지 않고 끝까지 다 돌았을 때만 실행된다. "n칸 전부 성공했을 때만 실제 위치를 갱신한다"는 조건을 별도의 성공 여부 플래그 변수 없이 표현할 수 있다.

### 방향을 딕셔너리(또는 dx, dy 벡터)로 미리 정의해두는 패턴

`"E"`, `"W"`, `"S"`, `"N"` 같은 방향 문자를 좌표 변화량으로 미리 매핑해두면, 실행 시점의 코드는 방향이 몇 개든 상관없이 하나로 통일된다. 이는 그리드/격자 기반 이동 문제(특히 상하좌우 탐색이 필요한 BFS/DFS류 문제)에서 반복적으로 쓰이는 표준적인 트릭이다.

---

## 이 문제에서 얻은 교훈

"한 칸씩 이동하며 그 자리에서 즉시 확인한다"는 접근 자체는 처음부터 이 문제의 조건("명령을 무시한다" = 이동분 전체 취소)을 정확히 반영한 정석적인 설계였다. 코드가 길어진 것은 접근 방식의 문제가 아니라, 같은 로직을 방향별로 손으로 네 번 풀어썼기 때문이었다. 특히 그 과정에서 복사 붙여넣기로 인해 방향과 되돌림의 부호가 반대여야 한다는 규칙을 한 곳에서 놓쳤는데, 이런 "쌍으로 반대 부호여야 하는" 구조는 복붙 시 특히 새기 쉬운 지점이라는 것을 확인했다. 방향을 데이터(딕셔너리)로 분리해내면 이런 반복과 그로 인한 실수 가능성 자체가 사라진다는 것이 이번 문제의 핵심 교훈이다.
