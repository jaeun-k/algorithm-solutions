# 지그재그 격자 좌표 변환과 DP 상태 전이 2문제 정리

지그재그로 쌓인 상자의 놓인 순서를 실제 좌표로 정규화한 상자 꺼내기 문제와, 그리디와의 차이를 통해 DP의 상태 전이를 이해한 땅따먹기 문제를 정리한다.

---

## 문제 1: 상자 꺼내기 (지그재그 격자를 실제 좌표로 정규화하기)

1 ~ n번 상자를 왼→오, 오→왼을 번갈아가며 층층이 쌓는다. 특정 상자를 꺼내려면 그 위에 쌓인 상자를 모두 먼저 꺼내야 한다. 꺼내야 하는 상자의 총 개수를 구한다.

```python
solution(22, 6, 8)   # 3
solution(13, 3, 6)   # 4
```

### 최종 코드
```python
def solution(n, w, num):
    row = n // w
    top_boxes = n % w
    num_row = (num-1)//w + 1
    num_col = (num-1) % w
    if num_row % 2 == 0:
        num_col = w-1-((num-1)%w)

    if top_boxes != 0:
        top_index = (range(w - top_boxes, w) if (row + 1) % 2 == 0 else range(0, top_boxes))
        if num_col in top_index:
            return row - num_row + 2

    return row - num_row + 1
```

### 시행착오와 정리한 개념

**지그재그 배치는 "놓인 순서"와 "실제 왼쪽 기준 위치"라는 두 좌표계를 동시에 다뤄야 한다.** 정방향 층은 순서와 실제 위치가 같지만, 역방향 층은 순서가 커질수록 실제 위치가 작아지는 대칭 관계를 갖는다. 이 반전 관계를 `실제열 = w-1-order`라는 수식으로 표현하면, 이후 비교에서 방향별 분기 없이 실제 위치끼리만 비교하면 된다.

**반전 공식이 바로 안 떠오른 것은 실력 부족이 아니라 안 배운 패턴이었기 때문이다.** 문자열 뒤집기, 배열 역순 접근 등에 쓰이는 `w-1-i` 패턴을 한 번도 의식적으로 정리해본 적이 없으면 매번 새로 마주치는 문제처럼 느껴진다. 이런 반전 패턴은 머릿속으로 수식을 굴리기보다, `순서: 0 1 2 3 4 5` / `실제열: 5 4 3 2 1 0`처럼 작은 구체적 사례를 나란히 손으로 써봐야 규칙(더하면 항상 w-1)이 눈에 들어온다.

**변환 없이 우회하는 방식도 정답이지만 실수 지점이 늘어난다.** 순서값을 그대로 두고 "두 층의 방향이 다를 때만 두 순서의 합이 w-1"이라는 관계식으로 비교해도 동일한 결과를 얻을 수 있다. 다만 이 경우 정방향/역방향마다 부등호 방향(이하 vs 이상)이 반대로 바뀌어야 하는데, 값만 방향별로 다르게 계산해두고 비교 연산자를 하나로 통일해버리면 한쪽 방향에서만 맞고 다른 쪽에서 틀리는 실수가 생긴다.

**"경계값과 부등호 비교" 대신 "채워진 범위와 in 비교"가 더 안전하다.** 방향에 따라 실제로 상자가 채워진 열의 범위(`range(0, top_boxes)` 또는 `range(w-top_boxes, w)`)를 통째로 만들고, 그 범위 안에 대상 열이 포함되는지(`in`)만 확인하면 부등호 방향을 방향별로 따로 신경 쓸 필요가 없어진다.

**작은 입력 범위에서는 반복문 제거가 성능보다 표현의 간결함 문제다.** n≤100, w≤10인 이 문제는 반복문이 있어도 성능에 영향이 없다. 반복문을 없애고 좌표를 직접 계산하는 방식은 "더 빠른 코드"가 아니라 "지그재그 구조를 격자 좌표로 통일해 더 직접적으로 드러내는 코드"에 가깝다.

---

## 문제 2: 땅따먹기 (그리디와 DP 상태 전이의 차이)

N행 4열의 땅에서, 한 행씩 내려오며 각 행마다 한 칸을 밟는다. 단 같은 열을 연속해서 밟을 수 없다. 마지막 행까지 내려왔을 때 얻을 수 있는 최대 점수를 구한다.

```python
solution([[1,2,3,5],[5,6,7,8],[4,3,2,1]])   # 16
```

### 최종 코드
```python
def solution(land):
    prev = land[0]

    for row in land[1:]:
        curr = [0,0,0,0]
        best_prev = sorted(prev)
        for col in range(4):
            if best_prev[-1] == prev[col]:
                curr[col] = best_prev[-2] + row[col]
            else:
                curr[col] = best_prev[-1] + row[col]
        prev = curr

    return max(prev)
```

### 시행착오와 정리한 개념

**경우의 수가 N에 대해 지수적으로 자라는지 선형으로 자라는지가 완전탐색 가능 여부를 가른다.** 각 행마다 4가지 선택이 있다고 해서 DFS/백트래킹을 시도하면 4^N이 되는데, N=100,000이면 이미 우주의 원자 수보다 큰 숫자가 되어 시도 자체가 불가능하다. N이 크더라도 경우의 수가 N에 비례(선형)해서 자란다면 완전탐색 스타일도 문제없다.

**"매 행 최댓값을 그리디하게 고른다"는 반례 하나로 바로 깨진다.** `[2,2,6,2]` 다음 `[2,2,7,2]`가 오는 경우, 1행에서 그리디하게 6을 고르면 2행에서 그 열을 못 밟아 6+2=8이 되지만, 6을 거치지 않고 다른 열을 밟았다면 2+7=9로 오히려 더 크다. 당장의 최댓값이 전체 최적을 보장하지 않는다.

**그리디와 DP의 차이는 "어느 방향으로 진행하는가"가 아니라 "몇 개의 상태를 동시에 들고 가는가"다.** DP를 앞에서부터(1행→마지막 행) 진행하든 뒤에서부터(마지막 행→1행) 진행하든 결과는 동일하다. 방향은 구현상의 선택일 뿐이며, 그리디와 DP를 가르는 진짜 기준은 매 단계에서 하나의 값만 확정하고 나머지를 버리는지(그리디), 아니면 가능한 모든 상태(4개 열)의 최댓값을 전부 유지하는지(DP)이다.

**"4개의 독립된 그리디 주자"와 "4개의 유기적인 DP 상태"는 겉보기엔 같은 개수라 헷갈리기 쉽다.** 4개의 시작 열에서 각자 그리디하게 완주하는 방식은 서로 정보를 교환하지 않는 독립된 경로 4개를 만드는 것이고, DP의 4개 값은 다음 행 계산에서 열을 하나 뺀 나머지 3개끼리 매번 새롭게 재조합된다는 점에서 본질적으로 다르다.

**DP는 트리 구조를 부정하지 않고, 같은 지점으로 수렴하는 가지들을 가지치기하는 것이다.** 원본 문제를 모두 펼치면 4^N개 경로를 가진 트리이지만, 같은 지점(k행 x열)에 도달한 여러 경로 중에서는 그 지점까지의 누적 최댓값 하나만 알면 이후 계산에 충분하다(최적 부분 구조). 나머지 경로는 미래 계산에 전혀 영향을 주지 못하므로 즉시 잘라내도 최종 답이 같다.

**정렬을 반복문 밖으로 빼는 것만으로 불필요한 재계산을 없앨 수 있었다.** col 루프 안에서 매번 `prev`를 새로 정렬하던 것을, 루프 밖에서 한 번만 정렬해두고 "1등이 자기 자신이면 2등을, 아니면 1등을 쓴다"는 방식으로 바꿔 행마다 정렬 횟수를 4번에서 1번으로 줄였다.

---

## 이 두 문제에서 얻은 교훈

**두 좌표계(순서 vs 실제 위치, 경로 vs 상태)를 혼용하면 실수하기 쉽다.** 한쪽 기준으로 통일해서 표현하면 방향별 분기나 부등호 실수를 원천적으로 줄일 수 있다.

**간단해 보이는 수식이나 접근이 바로 안 떠오르는 것은 그 패턴을 아직 안 배웠기 때문일 수 있다.** 작은 구체적 예시를 손으로 나열해보는 것이 막힌 상황에서 패턴을 찾는 가장 빠른 길이다.

**완전탐색 가능 여부는 "경우의 수가 N에 대해 어떻게 자라는가"로 먼저 판단해야 한다.** 지수적으로 자라면 N이 크지 않아도 위험하고, 선형으로 자라면 N이 아무리 커도 상관없다.

**그리디가 위험한 이유는 방향이 아니라 "한 번 확정하면 되돌릴 수 없기 때문"이다.** 매 단계 여러 가능성을 동시에 들고 가면서 최종 선택을 맨 뒤로 미루는 것이 DP가 그리디와 근본적으로 다른 지점이다.
