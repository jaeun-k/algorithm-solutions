# 별 찍기 마스터 — 별찍기 시리즈 전체 정리

기본 왼쪽 정렬 삼각형부터 시작해, 오른쪽 정렬 삼각형, 다이아몬드까지 난이도를 올려가며 푼 과정과, 그 과정에서 나눈 핵심 논의(직관성 vs 간결함, 상태 제거, 작업기억)를 함께 정리한다.

---

## 문제 1: 왼쪽 정렬 삼각형

```
*
**
***
****
*****
```

### 방법 1: 문자열 곱셈
```python
def print_star(n):
    for i in range(1, n + 1):
        print("*" * i)
```
`i`가 이미 1부터 n까지 순서대로 나오므로, 별도의 카운터 변수(`number`) 없이 `i` 자체를 곱셈에 바로 사용한다.

### 방법 2: 이중 for문 (`end=""` 활용)
```python
def print_star_2(n):
    for i in range(n):
        for j in range(i + 1):
            print("*", end="")
        print()
```
`print()`가 기본적으로 줄바꿈(`\n`)을 자동으로 붙인다는 점, `end=""`로 그 줄바꿈을 없앨 수 있다는 점, 그리고 안쪽 반복문이 끝난 직후 인자 없는 `print()`를 호출하면 그 지점에서 줄바꿈만 발생시킬 수 있다는 점을 이 문제에서 확인했다.

두 방식 모두 정답이며, 상황(문자열 곱셈을 쓸지 반복문으로 한 글자씩 제어할지)에 따라 선택 가능하다.

---

## 문제 2: 오른쪽 정렬 삼각형

```
    *
   **
  ***
 ****
*****
```

### 최종 코드 (문자열 곱셈, 카운터 변수 제거 버전)
```python
def print_star_right(n):
    for i in range(n):
        print(" " * (n - 1 - i) + "*" * (i + 1))
```
처음엔 `gap_number`, `star_number`라는 별도 변수를 두고 매 반복 증감시키는 방식으로 풀었으나, `i` 하나로 공백(`n-1-i`)과 별(`i+1`) 둘 다 바로 계산 가능하다는 것을 확인하고 변수 두 개와 증감 코드를 전부 제거했다.

### 이중 for문 버전 (공백은 곱셈, 별은 반복문)
```python
def print_star_right(n):
    for i in range(n):
        print(" " * (n - 1 - i), end="")
        for j in range(i + 1):
            print("*", end="")
        print()
```

---

## 문제 3: 다이아몬드

```
  *
 ***
*****
 ***
  *
```

가장 많은 시행착오와 논의가 있었던 문제. 알고리즘(위로 늘고, 정점 찍고, 아래로 준다) 자체는 바로 떠올랐지만, 이를 코드로 옮기는 **구현/계산 단계**에서 오래 걸렸다는 게 이 문제의 특징이었다 (이전 문제들은 반대로 "알고리즘 자체를 못 떠올려서" 막힌 경우가 많았음 — 이 둘은 서로 다른 종류의 어려움으로 구분됨).

### 버전 1: 상태 변수 두 개 + 3갈래 분기 (최초 완성본)
```python
def diamond(n):
    star_number = 1
    gap_number = (n - 1) // 2

    for i in range(n):
        if i < (n // 2):
            print(" " * gap_number + ("*" * star_number) + " " * gap_number)
            star_number = star_number + 2
            gap_number = gap_number - 1
        elif i == (n // 2):
            print(" " * gap_number + ("*" * star_number) + " " * gap_number)
            gap_number = 1
        else:
            star_number = star_number - 2
            print(" " * gap_number + ("*" * star_number) + " " * gap_number)
            gap_number = gap_number + 1
```
`n=1, 3, 5, 11` 전부 정답. 다만 세 갈래 분기 모두에서 거의 동일한 `print`문이 반복되고, `star_number`/`gap_number` 두 상태를 분기마다 다르게 갱신해야 해서 코드가 복잡했다.

### 버전 2: `elif`(가운데 줄) 제거 시도 — 첫 시도는 실패
```python
# 실패 버전: if/else 두 갈래로 병합했으나 가운데 줄(정점)이 통째로 빠짐
if i < (n // 2):
    star_number = star_number + 2
    ...
else:
    star_number = star_number - 2
    ...
```
`elif` 분기를 없애고 `if`/`else` 두 갈래로 합쳤으나, 가운데 줄(`i == n//2`)이 `else`로 빠지면서 늘어나던 별이 정점을 찍지 못하고 바로 줄어들어 버렸다.

### 버전 3: 초기값을 조정해 경계를 흡수 (성공)
```python
def diamond(n):
    star_number = -1
    gap_number = (n - 1) // 2 + 1

    for i in range(n):
        if i <= (n // 2):
            star_number = star_number + 2
            gap_number = gap_number - 1
            print(" " * gap_number + ("*" * star_number) + " " * gap_number)
        else:
            star_number = star_number - 2
            gap_number = gap_number + 1
            print(" " * gap_number + ("*" * star_number) + " " * gap_number)
```
`i <= n//2`로 조건을 바꾸고, 초기값(`star_number = -1`, `gap_number = (n-1)//2 + 1`)을 한 칸 어긋나게 잡아서, 가운데 줄이 `if` 분기에 자연스럽게 포함되도록 만들었다. 이 방식은 **더미 노드/센티넬 기법**, **오프셋 조정(off-by-one shifting)**이라 불리는 정석적인 최적화 기법과 같은 원리다 — "경계에서만 다르게 동작하는 로직을, 초기값을 조정해 일반 로직과 동일하게 흡수시키는" 접근.

### 버전 4: 상태 변수 완전 제거 — `if`/`else` + 인덱스 직접 계산
```python
def diamond(n):
    for i in range(n):
        if i <= (n // 2):
            print(" " * ((n - 1) // 2 - i) + ("*" + (2 * i * "*")) + " " * ((n - 1) // 2 - i))
        else:
            print(" " * (i - (n - 1) // 2) + ("*" + 2 * "*" * (n - i - 1)) + " " * (i - (n - 1) // 2))
```
`star_number`, `gap_number`라는 상태를 아예 들고 다니지 않고, `i` 하나로 그 줄의 공백/별 개수를 그 자리에서 즉시 계산하도록 재설계. "위에서부터 순서대로 내려오며 상태를 누적"하는 사고에서, "각 줄이 서로 독립적으로, 그 자리에서 계산 가능하다"는 사고로 전환한 결과.

### 참고 버전: `abs()`를 이용한 통합형 (처음 소개된 도구)
```python
def diamond(n):
    for i in range(n):
        distance = abs(i - n // 2)
        star_number = n - 2 * distance
        gap_number = distance
        print(" " * gap_number + "*" * star_number + " " * gap_number)
```
`abs()`(절댓값을 구하는 내장 함수)로 "가운데로부터의 거리"라는 개념 하나에 위/아래를 완전히 통합해, `if`/`else` 분기 자체를 없앤 형태. 다만 `abs()`는 이 시리즈에서 처음 소개된 함수라, "지금까지 배운 도구만으로 낼 수 있는 최적해"라기보다는 "도구가 하나 더 있으면 나올 수 있는 이상적인 형태"로 구분된다.

### for문을 두 번 사용한 버전 (위/아래를 아예 분리)
```python
def diamond(n):
    for i in range(n // 2 + 1):
        print(" " * ((n - 1) // 2 - i) + (2 * i + 1) * "*" + " " * ((n - 1) // 2 - i))
    for j in range(n // 2):
        print(" " * (j + 1) + (n - (2 * (j + 1))) * "*" + " " * (j + 1))
```
위쪽 절반과 아래쪽 절반을 하나의 반복문 안에서 분기로 나누는 대신, 아예 별도의 두 반복문을 순서대로 사용하는 형태 (중첩이 아니라 나란히 두 번 사용하는 것). 처음엔 `"*" + 2*i*"*"`(별 하나 + 나머지)로 두 조각으로 나눠 이어붙였던 것을, `(2*i+1)*"*"`로 "이번 줄의 별 개수는 정확히 `2i+1`개"라는 사실을 그대로 반영해 한 조각으로 정리했다.

**검증**: `n = 1, 3, 5, 11` 모든 값에서 위 다섯 가지 버전(3갈래 분기, 초기값 조정 2갈래, 상태 제거 2갈래, `abs()` 통합형, for문 두 번 사용)이 전부 동일한 정답을 냄을 확인했다.

### 수식 검증: for문을 두 번 사용한 버전과 `abs()` 버전은 사실 같은 공식
for문을 두 번 사용한 버전의 두 식(`2*i+1`, `n-2*(j+1)`)과 `abs()` 버전의 공식(`n - 2*distance`)은 실제로 대수적으로 동일하다. 첫 번째 루프에서 `거리 = n//2 - i`로 두면 `n - 2*(n//2 - i) = 2*i + 1`이 정확히 성립한다(`n`이 홀수이므로 `n//2 == (n-1)/2`). 즉 이 방식은 "하나의 공식(`별 개수 = n - 2*거리`)을 위/아래로 나눠 각각 적용한 것"이고, 수식 자체의 효율은 이미 동급이다 — 남은 차이는 "한 줄로 합칠지, 두 반복문으로 나눌지"라는 구조 선택뿐이다.

---

## 사용된 핵심 도구

- `range(시작, 끝)` — 시작값을 직접 지정해 `if`로 걸러내는 절차 없이 원하는 범위부터 순회
- `end=""` — `print()`의 기본 줄바꿈을 없애고 다른 값으로 대체
- 문자열 곱셈(`"문자" * 숫자`) — 문자열을 정수만큼 반복. `"*" * 0`은 에러가 아니라 빈 문자열
- `abs()` — 내장 함수, 절댓값(부호 제거) 반환

---

## 핵심 논의 정리

### f-string의 `{}`는 "값 추출"이 아니라 "문자열로의 변환"
`{}`는 정수냐 아니냐를 구분하는 문법이 아니라, **"지금 이 값이 문자열 안에 삽입되는 중인가, 코드에서 직접 연산에 쓰이는 중인가"**를 구분하는 문법이다. `" " * gap_number`처럼 코드에서 직접 연산에 쓰일 땐 `{}`가 필요 없고, `f"{gap_number}"`처럼 문자열 안에 끼워 넣을 때만 필요하다. `{}` 안에 들어간 값은 실제로 문자열로 변환되어 삽입된다 (`f"{3}"` → `"3"`, 정수 3이 아니라 문자 '3'짜리 문자열).

### "쉬운 문제인데도 막혔다"는 것에 대하여
구구단/별찍기처럼 논리적으로 단순한 문제에서도 마찰이 있었던 이유는, 실력 부족이 아니라 **그날 처음 마주치는 도구/판단이 껴 있었기 때문**이었다 (`print(함수())`가 `None`을 찍는 이유, `range(1,10)`, `end=""`, 문자열 곱셈 등). 문제 난이도는 "필요한 개념의 개수와 깊이"로 매겨지는 것이지 "새로운 도구를 하나도 안 써도 된다"는 뜻이 아니며, 처음 쓰는 도구가 하나라도 있으면 논리 난이도와 무관하게 멈칫하는 게 정상이라는 것을 재확인했다.

### 알고리즘(발상)이 막히는 것과 구현(계산)이 막히는 것은 다른 종류의 어려움이다
- 지금까지(merge_sorted, digit_sum, reverse_upper 투 포인터)는 "무엇을 해야 하는가"라는 **접근 방식 자체**가 안 보여서 막혔다.
- 다이아몬드는 달랐다. "위로 늘고 정점 찍고 아래로 준다"는 알고리즘은 바로 떠올랐고, 그 발상을 **정확한 숫자로 구현**(상태 변수 두 개를 언제 얼마나 바꿀지 동시에 추적)하는 데서 시간이 걸렸다.
- 이 둘은 훈련 방식이 다르다. 발상이 막히는 건 패턴 노출로 해결되고, 구현이 오래 걸리는 건 "상태를 몇 개나 동시에 들고 갈 것인가"를 코드를 짜기 전에 먼저 점검하는 습관으로 줄어드는 종류다.

### 직관적인 방식(상태 변수)과 간결한 방식(계산식) — 결함이 아니라 트레이드오프
상태 변수를 직접 관리하는 방식(당신의 기본 성향)은 "고쳐야 할 습관"이 아니라 **일장일단이 있는 성향**으로 정리됐다.
- 장점: 디버깅이 쉽다. 변수가 지금 뭘 담고 있는지 눈으로 바로 확인 가능해서, 학습 단계에서는 오히려 유리하다.
- 단점: 변수가 늘어날수록 관리 부담과 버그 발생 지점이 늘어난다. 코드 규모가 커지면 "계산으로 대체 가능한 상태는 최대한 안 갖고 다닌다"는 원칙이 더 유리해진다.
- 결론적으로 지금 단계에서는 고쳐야 할 결함이 아니라, 나중에 상황에 따라 "저장 vs 계산"을 선택하는 감각을 얹으면 되는 성향.

### "for문을 두 번 쓰는 건 초보자 코드"라는 인식에 대한 재검토
일반적으로 같은 작업을 불필요하게 반복문 두 개로 나누는 건 실제로 미숙함의 신호로 읽히는 경우가 많다는 것 자체는 맞다. 다만 다이아몬드처럼 **문제 자체가 본질적으로 두 개의 대칭적인 하위 문제(위쪽 만들기, 아래쪽 만들기)로 구성된 경우**, for문을 두 번 나눠 쓰는 것은 "억지로 쪼갠 것"이 아니라 "문제의 실제 구조를 코드에 정직하게 반영한 것"이다. 판단 기준은 "for문 개수"가 아니라 "이 반복이 원래 하나의 흐름인지, 원래 두 개의 하위 문제인지"여야 한다.

### 사고의 기준점을 "순서"에서 "중심"으로 옮기기
상태 변수 버전은 맨 위 줄부터 순서대로 내려오며 "지금까지 얼마나 왔는지"를 누적하는 방식이라, "지금 늘어나는 중인지 줄어드는 중인지"를 알아야 했고 그게 분기의 원인이었다. 반면 `abs()` 버전은 순서를 아예 무시하고, 각 줄이 **가운데로부터 얼마나 떨어져 있는지**만 독립적으로 계산한다. 이렇게 "순서대로 진행하며 상태를 누적하는 사고"에서 "기준점(중심)을 두고 각 위치를 독립적으로 계산하는 사고"로 전환하면, 흐름 정보(상태) 자체가 필요 없어진다는 것을 확인했다.

### 우발적 복잡도 vs 본질적 복잡도
다이아몬드에서 겪은 복잡함은 **우발적 복잡도**(accidental complexity) — 문제 자체는 단순한데 구현 방식 때문에 스스로 늘린 복잡도로, 설계를 바꾸면 사라진다. 반면 이후 배우게 될 DP, 그래프 탐색 등은 **본질적 복잡도**(essential complexity)를 갖는다 — 여러 하위 문제의 상태를 추적하는 것 자체가 알고리즘의 핵심이라, 아무리 코드를 우아하게 짜도 상태 추적 자체는 사라지지 않는다. 지금 연습(우발적 복잡도 제거)은 나중에 본질적 복잡도를 다룰 때, 불필요한 복잡함에 파묻히지 않고 진짜 핵심만 남기는 감각으로 이어진다.

### 작업기억(워킹 메모리)에 대한 논의
- 복잡한 상태를 여러 개 동시에 추적하는 건 숙련도와 무관하게 인간 워킹 메모리의 보편적 한계이며, 실제 개발자들도 종이/화이트보드에 직접 적어가며 시뮬레이션하는 게 표준적인 관행이다 (숙련 부족의 신호가 아님).
- 체스 마스터 연구(Chase & Simon, 1973): 실전 기보에서는 초보자보다 훨씬 많이 기억하지만, 무작위로 배치한 판에서는 그 차이가 사라진다 — 순수 용량이 커진 게 아니라 익숙한 패턴을 "청킹"해서 압축 저장하는 능력이 발달한 것이라는 해석.
- 다만 이 해석이 논쟁의 전부는 아니다: Ericsson의 숫자 기억 폭 훈련 실험(SF)에서는 특정 도메인(숫자)에 한정해 실질적인 향상이 있었고, 다른 도메인(문자)으로는 전이되지 않았다. "순수 용량은 고정, 전략/구조만 향상"이라는 입장이 우세하지만, "특정 도메인에서 실질적으로 더 많은 정보를 처리할 수 있게 됐다"는 사실 자체는 반박되지 않는다 — 메커니즘의 이름(전략 향상 vs 용량 향상)에 대한 논쟁은 남아 있지만, 도메인 한정적 실질 향상은 인정되는 부분.
- 결론: 반복 훈련이 다른 영역으로 전이되지 않더라도, 지금 다루는 영역(코딩 로직 추적)에 한정해서는 실질적인 능력 향상이 있다고 보는 것이 타당하며, 이는 현재 인지과학 증거와도 정합적이다.
