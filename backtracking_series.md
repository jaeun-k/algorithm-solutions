# 백트래킹(Backtracking) 2문제 정리

순열을 만드는 기본 구조부터, 재귀 호출 흐름을 손으로 트레이싱하며 이해한 과정, 그리고 이를 응용해 완전탐색 문제 두 개를 직접 풀어낸 과정을 정리한다.

---

## 개념: 백트래킹의 기본 뼈대

```python
def backtrack(경로):
    if 종료조건:
        결과 처리
        return

    for 다음후보 in 후보들:
        if 조건 (아직 안 썼는지 등):
            선택
            backtrack(경로 + 선택)
            취소   # 선택을 되돌림
```

- **선택 → 재귀호출 → 취소**가 항상 한 세트로 붙어 다닌다. 재귀호출 바로 다음 줄이 취소 코드이므로, 재귀에서 돌아오면 반드시 그 취소 코드가 실행된다.
- for문이 후보를 한 번씩만 순서대로 훑기 때문에, 같은 조합이 무한 반복되지 않는다. 한 후보를 취소하고 나면 for문은 자동으로 "다음 후보"로 넘어간다.
- 재귀 호출이 여러 단계 얽히면 손으로 전체 흐름을 끝까지 추적하는 것은 원래 어렵다. 대신 "지금 이 함수(이 경로)를 누가 호출했는가"만 따라가면, 복귀 지점은 항상 정확히 계산된다 — depth 번호를 외울 필요는 없다.
- 재귀적 사고의 핵심은 "한 단계(depth)가 책임지는 규칙"만 정의하는 것이다. 그 규칙이 모든 depth에서 동일하게 반복 적용된다는 것을 믿으면, 전체 트리를 미리 다 그려보지 않아도 코드를 짤 수 있다.

### 함수 안에서 재귀 함수를 먼저 호출하고 나중에 정의하면 안 된다
```python
def solution(...):
    return backtrack(...)   # 아직 정의되지 않은 이름을 호출
    def backtrack(...):
        ...
```
파이썬은 위에서 아래로 순서대로 실행되므로, 호출 시점에 아직 정의되지 않은 함수를 부르면 `UnboundLocalError`가 발생한다. 내부 함수 정의는 항상 호출보다 먼저 와야 한다.

### 리스트(가변 타입)와 정수(불변 타입)는 재귀에서 다르게 동작한다
- 리스트(`save`, `visited` 등)를 함수 안에서 매번 새로 만들면, depth마다 독립된 빈 리스트가 생겨 부모가 쌓아온 상태가 자식에게 전달되지 않는다. 이런 경우엔 리스트를 함수 밖에서 한 번만 만들고, 파라미터로 넘겨 계속 같은 객체를 공유해야 한다.
- 반대로 정수(`k`, `stamina` 같은 값)는 재귀 호출 시 값이 복사되어 넘어가므로, 자식에서 값을 변경해도 부모의 값에는 영향을 주지 않는다. 각 경로(순열)마다 독립적으로 계산되어야 하는 값이라면, 이 성질이 오히려 정확히 원하는 동작이다.
- 리스트는 "참조 공유", 정수/문자열은 "값 복사" — 이 차이가 재귀에서 상태를 어떻게 넘길지 결정한다.

### 값(value) 대신 인덱스(index)로 방문을 체크해야 하는 경우
서로 다른 원소가 같은 값을 가질 수 있는 문제에서는, 방문 체크를 값 기준(`x in visited_list`)으로 하면 실제로는 다른 원소인데 "이미 방문했다"고 잘못 판단할 수 있다. 이런 문제에서는 `visited = [False] * n`처럼 인덱스 기반 배열로 방문 여부를 관리해야 한다.

### 결과를 리스트에 누적하는 대신 변수 하나로 최댓값을 그때그때 갱신할 수 있다
매 재귀 호출마다 "지금까지의 경과"를 리스트에 다 쌓아놓고 마지막에 `max()`를 부르는 방식은 직관적이지만, 같은 값이 여러 번 중복 저장된다(결과가 틀리지는 않지만 비효율적). 대신 두 가지 방식으로 개선할 수 있다:
1. `nonlocal` 변수 하나를 두고, 재귀 호출마다 `answer = max(answer, count)`로 즉시 갱신
2. 재귀 함수가 그 가지에서 나온 최댓값을 `return`하고, 부모가 자식의 반환값과 자신의 값을 비교해 취합

두 번째 방식은 별도 키워드(`nonlocal`) 없이도 동일한 효과를 내며, 재귀 자체의 흐름(자식에게 위임하고 결과를 받아 취합)과도 자연스럽게 맞아떨어진다.

---

## 문제 1: 던전 탐험 (최대 탐험 던전 수)

현재 피로도 `k`와, 각 던전의 [최소 필요 피로도, 소모 피로도]가 담긴 2차원 배열 `dungeons`가 주어질 때, 탐험 가능한 던전의 최대 개수를 구한다. 던전은 순서를 자유롭게 정할 수 있고, 그 순서에 따라 탐험 가능한 개수가 달라진다.

```python
def solution(k, dungeons):
    n = len(dungeons)
    visited = [False] * n

    def backtrack(k, count):
        best = count
        for i in range(n):
            if not visited[i] and k >= dungeons[i][0]:
                visited[i] = True
                best = max(best, backtrack(k - dungeons[i][1], count + 1))
                visited[i] = False
        return best

    return backtrack(k, 0)
```

- 던전 개수가 최대 8개로 작고, 순서에 따라 결과가 달라지므로 모든 순열을 시도하는 완전탐색(백트래킹)이 적합하다.
- 입장 가능 조건(`k >= dungeons[i][0]`)을 for문 안에서 미리 체크해서, 애초에 불가능한 경로로는 아예 들어가지 않게 하는 것(가지치기)이 "일단 다 골라놓고 나중에 걸러내는 방식"보다 더 간결하다.
- **서로 다른 던전의 [최소 필요 피로도, 소모 피로도]가 같을 수 있다**는 조건 때문에, 값(`dungeons[i] in save`) 기준 방문 체크는 오답을 만든다. 값이 같은 던전이 여러 개 있으면 실제로 아직 방문하지 않은 던전도 "이미 방문했다"고 잘못 판단해, 결과 리스트가 끝까지 채워지지 못하고 빈 채로 남을 수 있다(`max()` 호출 시 런타임 에러로 이어짐). 인덱스 기준(`visited[i]`) 체크로 해결한다.

---

## 문제 2: 자물쇠와 열쇠 (최대로 열 수 있는 자물쇠 수)

현재 체력 `stamina`, 각 자물쇠 `locks[i]` = [필요 코드, 요구 체력, 소모 체력], 각 열쇠 `keys[j]` = 코드 값이 주어진다. 열쇠 코드와 자물쇠의 필요 코드가 일치하고, 현재 체력이 요구 체력 이상일 때만 그 자물쇠를 열 수 있다. 열쇠와 자물쇠는 각각 1회용이며, 열 수 있는 자물쇠의 최대 개수를 구한다.

```python
def solution(stamina, locks, keys):
    kn = len(keys)
    ln = len(locks)

    key_visited = [False] * kn
    lock_visited = [False] * ln

    def backtrack(stamina, count):
        best = count
        for i in range(kn):
            for j, lock in enumerate(locks):
                if not key_visited[i] and not lock_visited[j] and keys[i] == lock[0] and stamina >= lock[1]:
                    key_visited[i] = True
                    lock_visited[j] = True
                    best = max(best, backtrack(stamina - lock[2], count + 1))
                    key_visited[i] = False
                    lock_visited[j] = False
        return best

    return backtrack(stamina, 0)
```

- 던전 문제와 구조는 유사하지만, **선택해야 할 대상(열쇠)과 조건을 검사할 대상(자물쇠)이 분리**되어 있다는 점이 다르다. 열쇠 인덱스(`i`)와 자물쇠 인덱스(`j`)를 각각 독립적인 변수로 두고, `visited` 배열도 열쇠용/자물쇠용으로 따로 관리해야 한다. 하나의 인덱스 변수를 두 배열에 그대로 재사용하면(예: `lock_visited[i]`), 실제 자물쇠 위치와 무관한 엉뚱한 자리를 체크하게 되는 버그가 생긴다.
- 코드 값이 같은 자물쇠·열쇠 쌍이 여러 개 있을 수 있으므로(문제 1의 교훈과 동일), 열쇠와 자물쇠 모두 인덱스 기준으로 방문을 체크해야 한다.
- 요구 체력(조건 검사용)과 소모 체력(실제로 차감되는 값)이 서로 다른 필드이므로, 조건 체크에는 `lock[1]`을, 실제 차감에는 `lock[2]`를 사용해야 한다 — 조건 검사용 값을 그대로 차감에 써버리는 것은 흔히 나올 수 있는 실수다.

### 검증
```
locks=[[1,50,20],[2,30,10]], keys=[1,2], stamina=50   -> 2
locks=[[1,50,20],[1,20,5]],  keys=[1,1], stamina=50   -> 2  (코드 중복 케이스)
locks=[[1,50,20],[2,30,10]], keys=[1,2], stamina=10   -> 0  (아무것도 못 여는 케이스)
```

---

## 이 학습에서 얻은 교훈

### "전체 흐름을 한 번에 이해해야 코드를 짤 수 있다"는 전제 자체가 틀렸다
재귀 호출이 여러 단계 얽히면, 사람이 그 전체 흐름을 처음부터 끝까지 손으로 정확히 추적하는 것은 원래 잘 안 된다. 필요한 능력은 "그 자리에서, 규칙을 적용해 다음 스텝을 계산하는 것"이지, "전체 트리를 사진처럼 기억하는 것"이 아니다. 짧은 예제를 반복해서 손으로 트레이싱하다 보면, 그 스텝들이 점점 하나의 덩어리로 뭉쳐져 더 이상 한 줄 한 줄 추적하지 않아도 흐름이 보이게 된다.

### 같은 유형의 실수가 문제를 바꿔도 반복해서 나타났다
"값이 같으면 같은 원소로 취급해서 생기는 방문 체크 오류"가 문제 1(던전)과 문제 2(자물쇠·열쇠) 양쪽에서 동일하게 나타났다. 한 번 겪은 함정의 원인(값 비교 vs 인덱스 비교)을 정확히 언어화해두면, 구조가 다른 문제에서도 같은 함정을 더 빨리 알아챌 수 있었다.
