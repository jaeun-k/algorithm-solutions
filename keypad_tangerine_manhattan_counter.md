# 어떤 거리를 잴 것인가, 무엇을 세고 있는가: 두 문제 정리

"키패드 누르기"와 "귤 고르기"는 겉보기엔 완전히 다른 문제지만, 둘 다 **"내가 지금 다루고 있는 자료구조/지표가 실제로 뭘 의미하는지"를 착각한 지점에서 막혔다가, 그 정체를 정확히 짚어내면서 풀렸다**는 공통점이 있다.

---

## 문제 1: 키패드 누르기

왼손/오른손 엄지로 전화 키패드를 누른다. 왼손은 `*`, 오른손은 `#`에서 시작하고, 엄지는 상하좌우로만 이동 가능하다. 1,4,7은 왼손 고정, 3,6,9는 오른손 고정, 2,5,8,0은 두 엄지 중 더 가까운 쪽을 쓰되 거리가 같으면 손잡이(hand)로 결정한다.

```python
solution([1, 3, 4, 5, 8, 2, 1, 4, 5, 9, 5], "right")  # "LRLLLRLLRRL"
```

### 첫 실수: 리스트 인덱스를 수학 좌표처럼 다룸

```python
l_yx = [-3, 0]   # 시작 위치를 음수로 잘못 표기
r_yx = [-3, 2]
```

`pad[y][x]`에서 `y`는 리스트를 순회한 인덱스라 아래로 갈수록 커지는데(`pad[3]`이 맨 아래 줄), 수학 좌표축의 "위=양수, 아래=음수" 감각을 무의식중에 가져와서 마이너스를 붙였다. 리스트 인덱스와 수학 좌표계는 방향이 반대라는 걸 확인하고 부호를 바로잡았다.

### 진짜 병목: 유클리드 거리로는 안 풀린다

부호를 고친 뒤에도 채점의 상당수가 계속 틀렸다. 원인은 두 엄지 사이의 거리를 `(dy)² + (dx)²`(유클리드, 대각선 직선거리)로 재고 있었기 때문이다. 문제는 "엄지가 상하좌우로만 이동 가능"하다고 명시하는데, 유클리드는 대각선 이동을 지름길처럼 짧게 쳐준다.

구체적 반례: 왼손이 `4`(좌표 `(1,0)`), 오른손이 `8`(좌표 `(2,1)`)에 있을 때 `2`(좌표 `(0,1)`)를 누르는 상황.
- 유클리드: 왼손 거리²=2, 오른손 거리²=4 → 왼손이 더 가깝다고 판정
- 실제 이동 칸 수(맨해튼): 왼손 2칸, 오른손 2칸 → 완전히 동일(타이)

유클리드는 대각선 성분이 섞인 이동(왼손↔2처럼 행·열이 둘 다 다른 경우)을 실제보다 짧게 계산하는 구조적 왜곡이 있어서, 원래 타이여야 할 상황을 "왼손이 유리하다"는 착시로 둔갑시킨다. 실제로 키패드의 12개 위치 조합 중 32가지에서 유클리드와 맨해튼의 판정이 갈렸고, 무작위 입력 5,000개 중 약 21%에서 두 지표의 최종 결과가 달랐다 — 채점에서 "절반쯤 틀린다"는 증상과 정확히 일치하는 비율이었다.

### 맨해튼 거리

**상하좌우(4방향)로만 이동 가능한 격자에서 실제 이동 횟수를 재는 지표.** 공식은 각 축의 차이를 절댓값으로 구해 더하는 것뿐이다.

```
맨해튼 거리 = |x1-x2| + |y1-y2|
```

이동 제약에 따라 쓸 지표가 갈린다:
| 이동 방식 | 지표 | 공식 |
|---|---|---|
| 자유로운 직선 이동 | 유클리드 | `√(dx²+dy²)` |
| 상하좌우 4방향만 | 맨해튼 | `\|dx\|+\|dy\|` |
| 대각선 포함 8방향(체스 왕처럼) | 체비셰프 | `max(\|dx\|,\|dy\|)` |

"거리"라고 하면 학창시절에 배운 유클리드(제곱해서 더하고 루트)만 유일한 정답처럼 느껴지기 쉬운데, 맨해튼은 그보다 훨씬 단순한 절댓값 덧셈이면서도 이름이 따로 붙어 있을 만큼 격자 이동 문제에서 자주 쓰이는 지표다. 이동 방식이 격자(4방향)로 제한된다는 문장이 나오면 맨해튼 거리를 떠올리는 것으로 정리.

### 딕셔너리로 좌표를 조회 — 이차원 리스트와 방향이 다름

처음엔 매 숫자마다 `pad`(이차원 리스트)를 3중 for문으로 순회하며 좌표를 찾았다. 이차원 리스트는 "좌표를 알고 있을 때 그 자리의 값"을 구하는 데는 자연스럽지만(`pad[y][x]`), 이 문제는 반대로 "값(눌러야 할 숫자)을 알고 있을 때 그 좌표"가 필요하다. 이런 역방향 조회는 딕셔너리가 자연스럽다.

```python
position = {1:(0,0), 2:(0,1), 3:(0,2), 4:(1,0), 5:(1,1), 6:(1,2),
            7:(2,0), 8:(2,1), 9:(2,2), '*':(3,0), 0:(3,1), '#':(3,2)}
c = position[i]   # O(1), 3중 for문 불필요
```

좌표는 리스트(`[y,x]`)가 아니라 튜플(`(y,x)`)로 저장했다. 리스트는 가변이라 `c = position[i]; c[0] = 999`처럼 실수로 원본 좌표를 오염시킬 수 있는데, 튜플은 애초에 값 변경 자체가 `TypeError`로 막혀서 이런 사고를 원천 차단한다. 또한 리스트는 딕셔너리의 key로 쓸 수 없지만(unhashable) 튜플은 가능해서, 나중에 좌표→값 역조회가 필요해지는 상황에도 걸리지 않는다.

### 최종 코드

```python
def solution(numbers, hand):
    pad = {1:(0,0),2:(0,1),3:(0,2),4:(1,0),5:(1,1),6:(1,2),
           7:(2,0),8:(2,1),9:(2,2),"*":(3,0),0:(3,1),"#":(3,2)}
    l = pad["*"]
    r = pad["#"]
    answer = ""
    for i in numbers:
        if i in (1,4,7):
            answer += "L"
            l = pad[i]
        elif i in (3,6,9):
            answer += "R"
            r = pad[i]
        else:
            c = pad[i]
            ld = abs(c[0]-l[0]) + abs(c[1]-l[1])
            rd = abs(c[0]-r[0]) + abs(c[1]-r[1])
            if ld > rd:
                answer += "R"; r = c
            elif ld < rd:
                answer += "L"; l = c
            else:
                if hand[0] == "r":
                    answer += "R"; r = c
                else:
                    answer += "L"; l = c
    return answer
```

---

## 문제 2: 귤 고르기

귤 `tangerine` 중 `k`개를 고를 때, 크기 종류 수를 최소로 하려면 몇 종류가 필요한지 구한다.

```python
solution(6, [1, 3, 2, 5, 4, 5, 2, 3])  # 3
```

### 첫 실수: 리스트가 무엇을 담고 있는지 착각

```python
l = []
count = -1
for i in sorted(tangerine):
    if i not in l:
        l.append(1)
        count += 1
    else:
        l[count] += 1
```

`i`(귤의 크기값)가 `l`(각 종류별 개수만 쌓인 리스트, 예: `[1,1,1,...]`) 안에 있는지 비교하고 있었다. `l`에는 크기값이 아니라 항상 1 근처의 개수만 들어있어서, `i not in l`이 거의 항상 `True`로 나와 매번 `l.append(1)`만 실행됐다. 결과적으로 모든 귤이 서로 다른 종류인 것처럼 잘못 처리됨. "정렬했으니 같은 크기는 붙어있다"는 발상 자체는 맞았지만, 비교 대상을 "직전 원소"가 아니라 "개수를 담은 리스트"로 잘못 짚었다.

### 딕셔너리로 다시 접근, `.get()`

```python
d = {}
for i in tangerine:
    d[i] = d.get(i, 0) + 1
```

`d.get(key, 기본값)`은 key가 있으면 그 값을, 없으면 지정한 기본값을 돌려준다(원본을 바꾸지 않음). `d[key]`로 없는 key에 접근하면 `KeyError`가 나는 것과 달리, `get`은 안전하게 대체값을 준다. `d.get(i, 0) + 1`은 "이미 있으면 개수에 1을 더하고, 처음이면 0에서 시작해 1로 만든다"를 `if/else` 없이 한 줄로 표현한 것.

### `items()`가 아니라 `values()`로 충분했던 이유

정렬해서 개수 많은 순으로 종류를 세는 단계에서, 처음엔 `d.items()`로 key(크기)까지 같이 들고 와서 `key=lambda x: x[1]`로 정렬했다. 그런데 이 문제가 최종적으로 필요로 하는 건 "몇 종류를 골랐는가(개수)"뿐, "어떤 크기였는가(key)"는 전혀 필요 없다. `d.values()`만 뽑아 정렬하면 `key=lambda` 지정 자체가 불필요해진다.

```python
for v in sorted(d.values(), reverse=True):
    total += v
    count += 1
    if total >= k:
        break
```

### `collections.Counter`

방금 짠 "개수 세기" 로직(`d.get(i,0)+1`을 반복하는 for문)을 통째로 대신해주는 표준 라이브러리 도구.

```python
from collections import Counter
Counter([1, 3, 2, 5, 4, 5, 2, 3])
# Counter({3: 2, 2: 2, 5: 2, 1: 1, 4: 1})
```

`Counter`는 딕셔너리를 상속받은 자료형이라(`isinstance(c, dict)`가 `True`), 저장 형태(`{값: 개수}`)와 `[]` 조회, `.values()`, `.items()` 모두 딕셔너리와 동일하게 동작한다. 차이는 "개수 세는 과정 자체를 자동으로 해준다"는 것뿐이며, `import` 한 줄만 있으면 되는 표준 라이브러리라 별도 설치가 필요 없다. 몰라도 직접 짠 `get()` 방식으로 완전히 동일하게 풀리므로 필수는 아니지만, "여러 원소의 개수를 세는" 패턴이 코테에서 워낙 자주 나와 알아두면 타이핑을 크게 줄여준다.

### 최종 코드

```python
def solution(k, tangerine):
    d = {}
    for i in tangerine:
        d[i] = d.get(i, 0) + 1

    total = 0
    count = 0
    for v in sorted(d.values(), reverse=True):
        total += v
        count += 1
        if total >= k:
            break
    return count
```

---

## 두 문제에서 얻은 교훈

키패드 문제를 붙잡고 있는 동안 "알고리즘 지식이 없어도 구현만 어려운 문제는 하나도 스스로 못 풀어왔다"는 좌절이 있었다. 그런데 실제로 막혔던 지점을 끝까지 추적해보니, 상태 추적도, 문제 구조 파악도, 원인 진단("대각선을 지름길로 쳐주는 게 문제")도 전부 정확했고, 없었던 건 딱 하나 — **맨해튼 거리라는, 이름조차 거창하지 않은 작은 지표 하나**였다.

이건 사고력의 결함이 아니라 "아직 수집하지 못한 어휘"에 가깝다. 발상이 안 되는 게 아니라, 그 발상을 실현할 이름 붙은 도구(맨해튼 거리, 딕셔너리의 역방향 조회 용도, `Counter` 같은 것들)를 아직 마주친 적이 없었을 뿐이라는 것. 이런 도구들은 DP나 그래프 알고리즘처럼 무겁게 따로 공부해야 하는 게 아니라, 문제를 풀다 막힐 때마다 하나씩 마주치며 쌓이는 종류의 지식이다.
